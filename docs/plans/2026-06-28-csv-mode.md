# CSVモード Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** `.csv` を開くとセル単位のナビゲーション・読み上げを重ねる「CSVモード」を追加する（通常のテキスト編集は維持＝オーバーレイ方式）。

**Architecture:** ロジックは `yEdit.Core/Csv`（RFC4180パーサ＝オフセット追跡、ナビゲーション、読み上げ文字列）に置きテストで網羅。UIは薄く `CsvController`＋`MainForm` 配線（モードフラグ・キー・メニュー）。現在セルはキャレットの文字オフセットから毎回導出して状態の陳腐化を避ける。

**Tech Stack:** .NET 9 / C# / WinForms / ScintillaNET / xUnit。設計は `docs/plans/2026-06-28-csv-mode-design.md`。

**Conventions:**
- テスト実行: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~<Class>"`（リポジトリ root = `<repo>` で実行）
- 全テスト: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
- アプリのビルド: `dotnet build src/yEdit.App/yEdit.App.csproj`
- 既存スタイル: `PositionFormatter`（純ロジック静的クラス）、`MarkdownFile`（拡張子判定）に倣う。コメントは日本語。

---

## Task 1: CsvFile（拡張子判定）

**Files:**
- Create: `src/yEdit.Core/Csv/CsvFile.cs`
- Test: `tests/yEdit.Core.Tests/Csv/CsvFileTests.cs`

**Step 1: Write the failing test**

```csharp
using yEdit.Core.Csv;
using Xunit;

namespace yEdit.Core.Tests.Csv;

public class CsvFileTests
{
    [Theory]
    [InlineData("data.csv", true)]
    [InlineData("data.CSV", true)]
    [InlineData(@"C:\a\b\table.Csv", true)]
    [InlineData("data.txt", false)]
    [InlineData("data", false)]
    [InlineData(null, false)]
    [InlineData("", false)]
    public void IsCsvPath_detects_csv_extension(string? path, bool expected)
        => Assert.Equal(expected, CsvFile.IsCsvPath(path));
}
```

**Step 2: Run test to verify it fails**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvFileTests"`
Expected: ビルド失敗（`CsvFile` 未定義）。

**Step 3: Write minimal implementation**

```csharp
namespace yEdit.Core.Csv;

/// <summary>CSV ファイルの判定。メニュー活性化・オープン時検出・テストで共用する。</summary>
public static class CsvFile
{
    /// <summary>パスの拡張子が .csv（大文字小文字無視）なら true。null・拡張子なし・他拡張子は false。</summary>
    public static bool IsCsvPath(string? path)
    {
        if (string.IsNullOrEmpty(path)) return false;
        return string.Equals(
            System.IO.Path.GetExtension(path), ".csv", StringComparison.OrdinalIgnoreCase);
    }
}
```

**Step 4: Run test to verify it passes**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvFileTests"`
Expected: PASS（7 ケース）。

**Step 5: Commit**

```bash
git add src/yEdit.Core/Csv/CsvFile.cs tests/yEdit.Core.Tests/Csv/CsvFileTests.cs
git commit -m "CSV: .csv 拡張子判定 CsvFile を追加"
```

---

## Task 2: モデル型（CsvField / Direction）

下準備。テストは Task 3 のパーサで間接的に検証するので、ここは型のみ追加しビルドが通ることを確認する。

**Files:**
- Create: `src/yEdit.Core/Csv/CsvField.cs`

**Step 1: 実装を書く**

```csharp
namespace yEdit.Core.Csv;

/// <summary>セル移動の方向。</summary>
public enum Direction { Up, Down, Left, Right }

/// <summary>
/// CSV の1フィールド。Start/Length は元テキスト上の UTF-16 文字スパン（引用符込み）で、
/// ScintillaHost.SelectCharRange に直結する。Value は引用符を外し "" を " に復元した論理値（読み上げ用）。
/// </summary>
public sealed class CsvField
{
    public int Start { get; }
    public int Length { get; }
    public string Value { get; }

    public CsvField(int start, int length, string value)
    {
        Start = start;
        Length = length;
        Value = value;
    }
}
```

**Step 2: ビルド確認**

Run: `dotnet build src/yEdit.Core/yEdit.Core.csproj`
Expected: 成功（0 警告）。

**Step 3: Commit**

```bash
git add src/yEdit.Core/Csv/CsvField.cs
git commit -m "CSV: モデル型 CsvField / Direction を追加"
```

---

## Task 3: CsvParser（RFC4180パーサ・オフセット追跡）

**Files:**
- Create: `src/yEdit.Core/Csv/CsvDocument.cs`（パース結果モデル。ナビゲーションは Task 4/5 で追記）
- Create: `src/yEdit.Core/Csv/CsvParser.cs`
- Test: `tests/yEdit.Core.Tests/Csv/CsvParserTests.cs`

**Step 1: Write the failing test**

```csharp
using yEdit.Core.Csv;
using Xunit;

namespace yEdit.Core.Tests.Csv;

public class CsvParserTests
{
    [Fact]
    public void Empty_text_has_no_rows()
    {
        var d = CsvParser.Parse("");
        Assert.True(d.Ok);
        Assert.Empty(d.Rows);
    }

    [Fact]
    public void Single_cell_no_delimiter()
    {
        var d = CsvParser.Parse("hello");
        Assert.True(d.Ok);
        Assert.Single(d.Rows);
        Assert.Single(d.Rows[0]);
        Assert.Equal("hello", d.Rows[0][0].Value);
        Assert.Equal(0, d.Rows[0][0].Start);
        Assert.Equal(5, d.Rows[0][0].Length);
    }

    [Fact]
    public void Two_rows_two_cols_offsets_are_raw_spans()
    {
        // "a,bb\nccc,d"
        var d = CsvParser.Parse("a,bb\nccc,d");
        Assert.True(d.Ok);
        Assert.Equal(2, d.Rows.Count);
        Assert.Equal(new[] { "a", "bb" }, new[] { d.Rows[0][0].Value, d.Rows[0][1].Value });
        Assert.Equal(new[] { "ccc", "d" }, new[] { d.Rows[1][0].Value, d.Rows[1][1].Value });
        // オフセット: a=[0,1) bb=[2,2) ; 改行=4 ; ccc=[5,3) d=[9,1)
        Assert.Equal((0, 1), (d.Rows[0][0].Start, d.Rows[0][0].Length));
        Assert.Equal((2, 2), (d.Rows[0][1].Start, d.Rows[0][1].Length));
        Assert.Equal((5, 3), (d.Rows[1][0].Start, d.Rows[1][0].Length));
        Assert.Equal((9, 1), (d.Rows[1][1].Start, d.Rows[1][1].Length));
    }

    [Fact]
    public void Trailing_newline_does_not_make_empty_record()
    {
        var d = CsvParser.Parse("a,b\r\n");
        Assert.Single(d.Rows);
        Assert.Equal(new[] { "a", "b" }, new[] { d.Rows[0][0].Value, d.Rows[0][1].Value });
    }

    [Fact]
    public void Trailing_empty_field_on_content_line()
    {
        var d = CsvParser.Parse("a,");
        Assert.Single(d.Rows);
        Assert.Equal(2, d.Rows[0].Count);
        Assert.Equal("", d.Rows[0][1].Value);
        Assert.Equal((2, 0), (d.Rows[0][1].Start, d.Rows[0][1].Length));
    }

    [Fact]
    public void Quoted_field_with_comma_and_escaped_quote()
    {
        // a,"b,""c""",d   → b,"c"
        var d = CsvParser.Parse("a,\"b,\"\"c\"\"\",d");
        Assert.True(d.Ok);
        Assert.Single(d.Rows);
        Assert.Equal(3, d.Rows[0].Count);
        Assert.Equal("a", d.Rows[0][0].Value);
        Assert.Equal("b,\"c\"", d.Rows[0][1].Value);     // 論理値は引用符を外し "" を " に
        Assert.Equal("d", d.Rows[0][2].Value);
        // 中央フィールドの Start/Length は引用符込みの元スパン
        Assert.Equal('"', "a,\"b,\"\"c\"\"\",d"[d.Rows[0][1].Start]);
    }

    [Fact]
    public void Quoted_field_with_embedded_newline_is_one_logical_row()
    {
        // "line1\nline2",x  → 1論理行・2セル
        var d = CsvParser.Parse("\"line1\nline2\",x");
        Assert.True(d.Ok);
        Assert.Single(d.Rows);
        Assert.Equal(2, d.Rows[0].Count);
        Assert.Equal("line1\nline2", d.Rows[0][0].Value);
        Assert.Equal("x", d.Rows[0][1].Value);
    }

    [Fact]
    public void Unterminated_quote_is_not_ok()
    {
        var d = CsvParser.Parse("a,\"unterminated");
        Assert.False(d.Ok);
    }
}
```

**Step 2: Run test to verify it fails**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvParserTests"`
Expected: ビルド失敗（`CsvParser` / `CsvDocument` 未定義）。

**Step 3: Write minimal implementation**

`src/yEdit.Core/Csv/CsvDocument.cs`（この Task では Rows/Ok のみ。ナビは後続 Task で追記）:

```csharp
namespace yEdit.Core.Csv;

/// <summary>CSV のパース結果。Rows は論理行×フィールド。Ok=false は不正（引用符未終端）。</summary>
public sealed partial class CsvDocument
{
    public IReadOnlyList<IReadOnlyList<CsvField>> Rows { get; }
    public bool Ok { get; }

    public CsvDocument(IReadOnlyList<IReadOnlyList<CsvField>> rows, bool ok)
    {
        Rows = rows;
        Ok = ok;
    }
}
```

`src/yEdit.Core/Csv/CsvParser.cs`:

```csharp
using System.Text;

namespace yEdit.Core.Csv;

/// <summary>
/// RFC 4180 準拠の CSV パーサ（区切りはカンマ固定）。各フィールドの元テキスト上の
/// UTF-16 スパン（引用符込み）を保持する。引用符内のカンマ・改行・"" エスケープに対応。
/// 引用符が閉じないまま EOF に達した場合のみ Ok=false。
/// </summary>
public static class CsvParser
{
    public static CsvDocument Parse(string text)
    {
        var rows = new List<IReadOnlyList<CsvField>>();
        var row = new List<CsvField>();
        var sb = new StringBuilder();   // 現在フィールドの論理値
        int n = text.Length;
        int i = 0;
        int fieldStart = 0;
        bool inQuotes = false;
        bool ok = true;

        void EndField(int endExclusive)
        {
            row.Add(new CsvField(fieldStart, endExclusive - fieldStart, sb.ToString()));
            sb.Clear();
        }
        void EndRow()
        {
            rows.Add(row);
            row = new List<CsvField>();
        }

        while (i < n)
        {
            char c = text[i];
            if (inQuotes)
            {
                if (c == '"')
                {
                    if (i + 1 < n && text[i + 1] == '"') { sb.Append('"'); i += 2; continue; }
                    inQuotes = false; i++; continue;   // 閉じ引用符
                }
                sb.Append(c); i++; continue;           // 引用符内のカンマ・改行も literal
            }

            if (c == '"' && i == fieldStart) { inQuotes = true; i++; continue; } // 開き引用符（フィールド先頭のみ）
            if (c == ',') { EndField(i); i++; fieldStart = i; continue; }
            if (c == '\r' || c == '\n')
            {
                EndField(i);
                int lb = (c == '\r' && i + 1 < n && text[i + 1] == '\n') ? 2 : 1;
                EndRow();
                i += lb; fieldStart = i; continue;
            }
            sb.Append(c); i++;   // 通常文字（閉じ引用符後の余剰文字も寛容に literal 扱い）
        }

        if (inQuotes) ok = false;   // 引用符未終端

        // 末尾レコードの確定: 直近の改行後に内容が無い「末尾の空レコード」は捨てる。
        if (i > fieldStart || row.Count > 0)
        {
            EndField(n);
            EndRow();
        }

        return new CsvDocument(rows, ok);
    }
}
```

**Step 4: Run test to verify it passes**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvParserTests"`
Expected: PASS（8 ケース）。

**Step 5: Commit**

```bash
git add src/yEdit.Core/Csv/CsvDocument.cs src/yEdit.Core/Csv/CsvParser.cs tests/yEdit.Core.Tests/Csv/CsvParserTests.cs
git commit -m "CSV: RFC4180パーサ（オフセット追跡）を追加"
```

---

## Task 4: ナビゲーション FindCell

**Files:**
- Modify: `src/yEdit.Core/Csv/CsvDocument.cs`（partial に追記）
- Test: `tests/yEdit.Core.Tests/Csv/CsvNavigationTests.cs`

**Step 1: Write the failing test**

```csharp
using yEdit.Core.Csv;
using Xunit;

namespace yEdit.Core.Tests.Csv;

public class CsvNavigationTests
{
    private static CsvDocument Doc(string t) => CsvParser.Parse(t);

    [Fact]
    public void FindCell_caret_inside_field()
    {
        var d = Doc("a,bb\nccc,d");      // bb=[2,2)
        Assert.Equal((0, 1), d.FindCell(3));   // 'b' の上
    }

    [Fact]
    public void FindCell_caret_on_delimiter_belongs_to_left_field()
    {
        var d = Doc("a,bb");             // a=[0,1) , カンマ=1
        Assert.Equal((0, 0), d.FindCell(1));
    }

    [Fact]
    public void FindCell_caret_on_second_row()
    {
        var d = Doc("a,bb\nccc,d");      // ccc=[5,3)
        Assert.Equal((1, 0), d.FindCell(6));
    }

    [Fact]
    public void FindCell_empty_doc_returns_origin()
        => Assert.Equal((0, 0), Doc("").FindCell(0));
}
```

**Step 2: Run test to verify it fails**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvNavigationTests"`
Expected: ビルド失敗（`FindCell` 未定義）。

**Step 3: Write minimal implementation**（`CsvDocument.cs` に追記）

```csharp
public sealed partial class CsvDocument
{
    /// <summary>キャレット（UTF-16 オフセット）を含むセルを返す。含むセルが無ければ直前のセル、無ければ原点。</summary>
    public (int row, int col) FindCell(int caretOffset)
    {
        (int row, int col) best = (0, 0);
        int bestStart = -1;
        bool found = false;
        for (int r = 0; r < Rows.Count; r++)
        {
            var row = Rows[r];
            for (int c = 0; c < row.Count; c++)
            {
                var f = row[c];
                if (caretOffset >= f.Start && caretOffset <= f.Start + f.Length)
                    return (r, c);
                if (f.Start <= caretOffset && f.Start > bestStart)
                {
                    bestStart = f.Start; best = (r, c); found = true;
                }
            }
        }
        return found ? best : (0, 0);
    }
}
```

**Step 4: Run test to verify it passes**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvNavigationTests"`
Expected: PASS（4 ケース）。

**Step 5: Commit**

```bash
git add src/yEdit.Core/Csv/CsvDocument.cs tests/yEdit.Core.Tests/Csv/CsvNavigationTests.cs
git commit -m "CSV: FindCell（キャレット→セル特定）を追加"
```

---

## Task 5: ナビゲーション MoveCell / GetField / Header

**Files:**
- Modify: `src/yEdit.Core/Csv/CsvDocument.cs`
- Modify: `tests/yEdit.Core.Tests/Csv/CsvNavigationTests.cs`

**Step 1: Write the failing test**（既存クラスに追記）

```csharp
    [Fact]
    public void MoveCell_left_right_within_row()
    {
        var d = Doc("a,b,c");
        Assert.Equal((0, 0), d.MoveCell(0, 1, Direction.Left));
        Assert.Equal((0, 2), d.MoveCell(0, 1, Direction.Right));
        Assert.Null(d.MoveCell(0, 0, Direction.Left));    // 左端
        Assert.Null(d.MoveCell(0, 2, Direction.Right));   // 右端
    }

    [Fact]
    public void MoveCell_up_down_between_rows()
    {
        var d = Doc("a,b\nc,d\ne,f");
        Assert.Equal((0, 1), d.MoveCell(1, 1, Direction.Up));
        Assert.Equal((2, 1), d.MoveCell(1, 1, Direction.Down));
        Assert.Null(d.MoveCell(0, 0, Direction.Up));      // 先頭行
        Assert.Null(d.MoveCell(2, 0, Direction.Down));    // 最終行
    }

    [Fact]
    public void MoveCell_down_clamps_to_last_col_on_ragged_row()
    {
        var d = Doc("a,b,c\nx");        // 2行目は1列
        Assert.Equal((1, 0), d.MoveCell(0, 2, Direction.Down));
    }

    [Fact]
    public void GetField_and_Header()
    {
        var d = Doc("name,age\ntaro,20");
        Assert.Equal("20", d.GetField(1, 1)!.Value);
        Assert.Null(d.GetField(5, 0));
        Assert.Equal("age", d.Header(1)!.Value);
        Assert.Null(d.Header(9));
    }
```

**Step 2: Run test to verify it fails**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvNavigationTests"`
Expected: ビルド失敗（`MoveCell` 等未定義）。

**Step 3: Write minimal implementation**（`CsvDocument.cs` に追記）

```csharp
public sealed partial class CsvDocument
{
    /// <summary>row,col から方向 dir に1セル移動した座標。端で動けなければ null。上下移動は列数不足の行で末尾列にクランプ。</summary>
    public (int row, int col)? MoveCell(int row, int col, Direction dir)
    {
        switch (dir)
        {
            case Direction.Left:
                return col > 0 ? (row, col - 1) : null;
            case Direction.Right:
                return col < Rows[row].Count - 1 ? (row, col + 1) : ((int, int)?)null;
            case Direction.Up:
                return row > 0 ? (row - 1, ClampCol(row - 1, col)) : ((int, int)?)null;
            case Direction.Down:
                return row < Rows.Count - 1 ? (row + 1, ClampCol(row + 1, col)) : ((int, int)?)null;
        }
        return null;
    }

    private int ClampCol(int row, int col)
    {
        int last = Rows[row].Count - 1;
        return col > last ? last : col;
    }

    /// <summary>row,col のフィールド。範囲外は null。</summary>
    public CsvField? GetField(int row, int col)
    {
        if (row < 0 || row >= Rows.Count) return null;
        var r = Rows[row];
        if (col < 0 || col >= r.Count) return null;
        return r[col];
    }

    /// <summary>col 列の先頭セル（見出し＝0行目）。範囲外は null。</summary>
    public CsvField? Header(int col) => GetField(0, col);
}
```

**Step 4: Run test to verify it passes**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvNavigationTests"`
Expected: PASS。

**Step 5: Commit**

```bash
git add src/yEdit.Core/Csv/CsvDocument.cs tests/yEdit.Core.Tests/Csv/CsvNavigationTests.cs
git commit -m "CSV: MoveCell/GetField/Header（セル移動・見出し）を追加"
```

---

## Task 6: 読み上げ文字列 CsvAnnounceFormatter

**Files:**
- Create: `src/yEdit.Core/Csv/CsvAnnounceFormatter.cs`
- Test: `tests/yEdit.Core.Tests/Csv/CsvAnnounceFormatterTests.cs`

**Step 1: Write the failing test**

```csharp
using yEdit.Core.Csv;
using Xunit;

namespace yEdit.Core.Tests.Csv;

public class CsvAnnounceFormatterTests
{
    [Fact]
    public void Cell_reads_content_then_position()
        => Assert.Equal("田中 2行2列", CsvAnnounceFormatter.Cell("田中", row: 2, col: 2));

    [Fact]
    public void Cell_empty_value_says_kuu()
        => Assert.Equal("空 2行2列", CsvAnnounceFormatter.Cell("", 2, 2));

    [Fact]
    public void Header_returns_value_or_kuu()
    {
        Assert.Equal("氏名", CsvAnnounceFormatter.Header("氏名"));
        Assert.Equal("空", CsvAnnounceFormatter.Header(""));
    }
}
```

**Step 2: Run test to verify it fails**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvAnnounceFormatterTests"`
Expected: ビルド失敗（未定義）。

**Step 3: Write minimal implementation**

```csharp
namespace yEdit.Core.Csv;

/// <summary>CSV モードの読み上げ文字列を組み立てる純ロジック（UI 非依存・テスト可能）。</summary>
public static class CsvAnnounceFormatter
{
    /// <summary>セル移動時の読み上げ。内容→位置の順（例「田中 2行2列」）。空セルは「空」。row/col は 1 始まり。</summary>
    public static string Cell(string value, int row, int col)
        => $"{(string.IsNullOrEmpty(value) ? "空" : value)} {row}行{col}列";

    /// <summary>見出し読み上げ。空なら「空」。</summary>
    public static string Header(string value)
        => string.IsNullOrEmpty(value) ? "空" : value;

    public const string LeftEdge = "左端です";
    public const string RightEdge = "右端です";
    public const string TopEdge = "先頭行です";
    public const string BottomEdge = "最終行です";
}
```

**Step 4: Run test to verify it passes**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj --filter "FullyQualifiedName~CsvAnnounceFormatterTests"`
Expected: PASS。

**Step 5: Commit**

```bash
git add src/yEdit.Core/Csv/CsvAnnounceFormatter.cs tests/yEdit.Core.Tests/Csv/CsvAnnounceFormatterTests.cs
git commit -m "CSV: 読み上げ文字列 CsvAnnounceFormatter を追加"
```

---

## Task 7: モードフラグ＋CsvController（App 配線・UnitテストなしでBuild検証）

WinForms 依存のため Core テストは付けない。ビルド成功で検証し、実機 SR 検証は最終段で行う。

**Files:**
- Modify: `src/yEdit.App/DocumentState.cs`
- Create: `src/yEdit.App/CsvController.cs`

**Step 1: DocumentState に CsvMode を追加**

`DocumentState.cs` のプロパティ群（`LineEnding` の下）へ追記:

```csharp
    public bool CsvMode { get; set; }              // CSV モード（タブ毎・既定 false）
```

**Step 2: CsvController を作成**

`src/yEdit.App/CsvController.cs`:

```csharp
using yEdit.Core.Csv;
using yEdit.Editor;

namespace yEdit.App;

/// <summary>
/// CSV モードのセルナビゲーション・読み上げ配線（SearchController/GrepController と同型）。
/// 現在セルはキャレットの文字オフセットから毎回導出するため、自由編集後も状態が陳腐化しない。
/// </summary>
public sealed class CsvController
{
    private readonly DocumentManager _docs;
    private readonly Announcer _announcer;

    public CsvController(DocumentManager docs, Announcer announcer)
    {
        _docs = docs;
        _announcer = announcer;
    }

    /// <summary>アクティブが CSV モードならそのエディタ、でなければ null。</summary>
    private ScintillaHost? ActiveCsvEditor()
    {
        var doc = _docs.Active;
        return (doc is not null && doc.State.CsvMode) ? doc.Editor : null;
    }

    /// <summary>方向 dir に1セル移動して選択・読み上げ。端では端アナウンス。</summary>
    public void Move(Direction dir)
    {
        var ed = ActiveCsvEditor();
        if (ed is null) return;

        var csv = CsvParser.Parse(ed.SnapshotText);
        if (!csv.Ok || csv.Rows.Count == 0) { _announcer.Say("CSVとして解析できません"); return; }

        var (row, col) = csv.FindCell(ed.CaretCharOffset);
        var target = csv.MoveCell(row, col, dir);
        if (target is null) { _announcer.Say(EdgeMessage(dir)); return; }

        var (tr, tc) = target.Value;
        var f = csv.GetField(tr, tc);
        if (f is null) { _announcer.Say(EdgeMessage(dir)); return; }

        ed.SelectCharRange(f.Start, f.Length);
        ed.Focus();
        _announcer.Say(CsvAnnounceFormatter.Cell(f.Value, tr + 1, tc + 1));
    }

    /// <summary>現在列の先頭セル（見出し）を読み上げる（カーソル移動なし）。</summary>
    public void ReadColumnHeader()
    {
        var ed = ActiveCsvEditor();
        if (ed is null) return;

        var csv = CsvParser.Parse(ed.SnapshotText);
        if (!csv.Ok || csv.Rows.Count == 0) { _announcer.Say("CSVとして解析できません"); return; }

        var (_, col) = csv.FindCell(ed.CaretCharOffset);
        var h = csv.Header(col);
        _announcer.Say(h is null ? "空" : CsvAnnounceFormatter.Header(h.Value));
    }

    /// <summary>CSV モードを手動でトグルする（自動判定の救済・任意拡張子での利用）。</summary>
    public void ToggleMode()
    {
        var doc = _docs.Active;
        if (doc is null) return;
        doc.State.CsvMode = !doc.State.CsvMode;
        _announcer.Say(doc.State.CsvMode ? "CSVモード オン" : "CSVモード オフ");
    }

    private static string EdgeMessage(Direction dir) => dir switch
    {
        Direction.Left => CsvAnnounceFormatter.LeftEdge,
        Direction.Right => CsvAnnounceFormatter.RightEdge,
        Direction.Up => CsvAnnounceFormatter.TopEdge,
        _ => CsvAnnounceFormatter.BottomEdge,
    };
}
```

**Step 3: ビルド確認**

Run: `dotnet build src/yEdit.App/yEdit.App.csproj`
Expected: 成功（0 警告）。※ `Document.Editor` が `ScintillaHost`、`DocumentManager.Active` が `Document?` であることに依存。

**Step 4: Commit**

```bash
git add src/yEdit.App/DocumentState.cs src/yEdit.App/CsvController.cs
git commit -m "CSV: DocumentState.CsvMode と CsvController を追加"
```

---

## Task 8: MainForm 配線（検出・キー・メニュー）

**Files:**
- Modify: `src/yEdit.App/MainForm.cs`

**Step 1: using とフィールド追加**

冒頭の using 群に追記:

```csharp
using yEdit.Core.Csv;
```

フィールド宣言（`_grep` の近く、`MainForm.cs:14` 付近）へ追記:

```csharp
    private CsvController _csv = null!; // コンストラクタで生成
```

**Step 2: コンストラクタで生成**（`_announcer = new Announcer(...)` の直後、`MainForm.cs:50` 付近）

```csharp
        _csv = new CsvController(_docs, _announcer);
```

**Step 3: LoadInto に検出を追加**（`UpdateStatus();` の直後、`MainForm.cs:574` 付近。`loaded.HadReplacementChar` 判定の前）

```csharp
            doc.State.CsvMode = false;
            if (CsvFile.IsCsvPath(path))
            {
                var csv = CsvParser.Parse(loaded.Text);
                if (csv.Ok) { doc.State.CsvMode = true; _announcer.Say("CSVモード オン"); }
                else { _announcer.Say("CSVとして解析できませんでした。テキストとして開きます"); }
            }
```

**Step 4: ProcessCmdKey にキーを追加**（`ProcessCmdKey` 本体の最初、既存 `switch` の前。`MainForm.cs:165` 付近）

```csharp
        // CSV モードのアクティブタブのみ Ctrl+Shift+矢印 / Ctrl+Shift+C を横取り。
        // OFF の時はこのブロックを素通りし、通常の Scintilla 挙動を温存する。
        if (_docs.Active?.State.CsvMode == true)
        {
            switch (keyData)
            {
                case Keys.Control | Keys.Shift | Keys.Up: _csv.Move(Direction.Up); return true;
                case Keys.Control | Keys.Shift | Keys.Down: _csv.Move(Direction.Down); return true;
                case Keys.Control | Keys.Shift | Keys.Left: _csv.Move(Direction.Left); return true;
                case Keys.Control | Keys.Shift | Keys.Right: _csv.Move(Direction.Right); return true;
                case Keys.Control | Keys.Shift | Keys.C: _csv.ReadColumnHeader(); return true;
            }
        }
```

**Step 5: CSV メニューを追加**（`BuildMenu` 内、`md`（マークダウン）メニュー定義の後・`help` の前。`MainForm.cs:252` 付近）

```csharp
        var csv = new ToolStripMenuItem("CSV(&C)");
        var csvToggle = new ToolStripMenuItem("CSVモード切替(&T)", null, (_, _) => _csv.ToggleMode());
        var csvUp = new ToolStripMenuItem("上のセル(&U)", null, (_, _) => _csv.Move(Direction.Up))
        { ShortcutKeyDisplayString = "Ctrl+Shift+↑" };
        var csvDown = new ToolStripMenuItem("下のセル(&D)", null, (_, _) => _csv.Move(Direction.Down))
        { ShortcutKeyDisplayString = "Ctrl+Shift+↓" };
        var csvLeft = new ToolStripMenuItem("左のセル(&L)", null, (_, _) => _csv.Move(Direction.Left))
        { ShortcutKeyDisplayString = "Ctrl+Shift+←" };
        var csvRight = new ToolStripMenuItem("右のセル(&R)", null, (_, _) => _csv.Move(Direction.Right))
        { ShortcutKeyDisplayString = "Ctrl+Shift+→" };
        var csvHeader = new ToolStripMenuItem("列見出しを読み上げ(&C)", null, (_, _) => _csv.ReadColumnHeader())
        { ShortcutKeyDisplayString = "Ctrl+Shift+C" };
        csv.DropDownItems.Add(csvToggle);
        csv.DropDownItems.Add(new ToolStripSeparator());
        csv.DropDownItems.AddRange(new ToolStripItem[] { csvUp, csvDown, csvLeft, csvRight, csvHeader });
        // 開く度に活性更新。移動系は CSV モード時のみ有効。トグルは常に有効＋チェック表示。
        csv.DropDownOpening += (_, _) =>
        {
            bool on = _docs.Active?.State.CsvMode == true;
            csvUp.Enabled = csvDown.Enabled = csvLeft.Enabled = csvRight.Enabled = csvHeader.Enabled = on;
            csvToggle.Checked = on;
        };
```

メニュー追加行（`MainForm.cs:257` 付近）を更新:

```csharp
        menu.Items.AddRange(new ToolStripItem[] { file, edit, search, read, md, csv, help });
```

**Step 6: ビルド確認**

Run: `dotnet build src/yEdit.App/yEdit.App.csproj`
Expected: 成功（0 警告）。

**Step 7: Commit**

```bash
git add src/yEdit.App/MainForm.cs
git commit -m "CSV: MainForm に検出・キー・CSVメニューを配線"
```

---

## Task 9: 仕上げ（全テスト・手動スモーク）

**Step 1: 全 Core テスト**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: 既存 201 + 新規（CsvFile 7 / CsvParser 8 / CsvNavigation 8 / CsvAnnounceFormatter 5 = 約28）すべて PASS、0 失敗。

**Step 2: アプリのビルド**

Run: `dotnet build src/yEdit.App/yEdit.App.csproj`
Expected: 成功・0 警告。

**Step 3: 手動スモーク（@superpowers:verification-before-completion）**

サンプル `sample.csv` を用意（例: `氏名,年齢\n田中,20\n鈴木,30`）して起動し確認:
- 開いた瞬間「CSVモード オン」がアナウンスされる
- Ctrl+Shift+↓/↑/←/→ でセルが選択され「内容 R行C列」が読まれる（空セルは「空 …」）
- 端で「左端です／右端です／先頭行です／最終行です」
- Ctrl+Shift+C で現在列の見出し（先頭セル）が読まれる
- 引用符内カンマ・改行を含む CSV で論理行・セル境界が正しい
- 不正な `.csv`（引用符未終端）で「解析できませんでした…」＝通常テキストとして開く
- CSV(&C) メニューで手動トグル ON/OFF、活性状態が連動

**Step 4: 実機 SR 検証の申し送り**

実機 SR（NVDA / PC-Talker）での読み上げ確認は既存の実機 SR 検証フェーズに合流。`memory/markdown-preview-followups.md` と同様に申し送りメモを残す。

**Step 5: ブランチ統合**

`@superpowers:finishing-a-development-branch` に従い、`feature/add_csv_mode` を main へ no-ff マージ（既存フロー [[phase-work-git-flow]]）。マージ前に [[review-by-separate-agent]] に従い別エージェントのコードレビューを行う。

---

## YAGNI（v1スコープ外・将来）

- 区切りのカンマ固定（TSV/セミコロン自動判定は将来）
- 不揃い行は末尾クランプ（desired column 保持なし）
- ナビ毎の全文再パース（大容量はキャッシュ最適化が将来課題）
- 高度な表編集 UI（セル編集ダイアログ等）は作らない

# P6: App 層一発置き換え + Scintilla/v1 系一括撤去 実装計画

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** P5 完了時点(自作 EditorControl・UIA v2 プロバイダ・実機中間検証合格・722 テスト緑)の資産を使い、App 層編集エンジン結線を `ScintillaHost` → `EditorControl` へ**一発置き換え**する。同時に Scintilla / v1 UIA / UiaProbe / UTF-16 全文サポート / v1 用 tools を一括撤去する。ネイティブ DLL 同梱ゼロで release zip を出せる状態にする。

**Architecture:** P6 のフェーズ実装順は「A: `EditorControl` に App 層互換 API 先出し(compat shim を Editor 層に閉じる)」→「B: Core.Text 層の UTF-16 廃止+ Stream I/O 化」→「C: Document/DocumentManager 型変更」→「D: MainForm/FileController/SearchController/BackupCoordinator 書き換え」→「E: CSV/禁則/MD/行ジャンプ/文字情報 配線」→「F: SR/UIA 切り分け猶予(SrContext=false 固定)+ App 層 Announcer に `WordNavigatedEvent` 配線」→「G: 撤去作業(Scintilla5.NET PackageReference / ScintillaHost.cs / Sci.cs / v1 用 tools 削除)」→「H: 統合・別エージェントレビュー・設計書追記」。**Announcer は smoke `UiaSmokeAnnouncer` 削除+ App 層 Announcer に `WordNavigated`/`CaretEnteredEmptyLine` を配線**(§設計 §2-5-3)。**SR/UIA 撤去(SrRoute.Nvda/CsvFocusSink/ServeUiaProvider の完全削除)は P7 送り**=P6 では「無効化のみで残す」で猶予。

**Tech Stack:** C# / .NET 9 / xUnit / WinForms(`Control` 派生・`WndProc` 拡張)/ WPF Automation(既存 P5 v2 プロバイダを継続使用)/ P/Invoke(`user32.dll`)/ 既存 `tests/yEdit.Core.Tests` + `tests/yEdit.Editor.Tests` + `tests/yEdit.Core.Bench` + `tools/*.ps1`(v2 用のみ残す)。

**前提:**
- 作業は worktree `<repo>\.worktrees\custom-editcontrol-design`(ブランチ `feature/custom-editcontrol-design`)
- **main には一切触れない**(全フェーズを本ブランチに閉じ、P7 合格後に一括マージ)
- P5 完了時点の EditorControl 公開 API(P3〜P5 で追加した Modified / CanUndo / CanRedo / Undo / Redo / Cut / Copy / Paste / SelectAll / CurrentLine / GetColumn / ReplaceCharRange / EolMode / Overtype / ReadOnly / SetSavePoint / EmptyUndoBuffer / CaretEnteredEmptyLine / WordNavigated / RaiseUiaSelectionEvents / IUiaTextHost 全19メンバ)は無変更。**新規 App 層互換 API は追加のみ**(破壊的変更禁止)
- v1(`IUiaTextHostLegacy` / `TextControlProvider` / `TextProviderImpl` / `TextRangeProvider` / `TextNavigation` / `UiaTextControl`)は **P6 では触らない**。P6 で撤去するのは `ScintillaHost.cs` / `Sci.cs` / `Scintilla5.NET` PackageReference のみ(v1 UIA は P7 送り=切り分け猶予)
- 各タスク完了ごとにコミット。全タスク後に別エージェントレビュー(Task 18 内)
- 既存テスト 722 件(Core 540 / Editor 182)は全タスクで緑を維持

---

## 0. 設計固定事項(全タスク共通の不変条件)

実装全体で守る。レビュー観点でもある。

1. **EditorControl 公開 API 追加のみ**:P3〜P5 で追加した既存 API 名/シグネチャは無変更。App 層互換の Text/SnapshotText/SelectCharRange(length版)/MoveCaretCharOffset/CurrentPosition/ApplyLineEnding/ConvertEols/LineCount/GoToLine/SavePointLeft/SavePointReached/UpdateUI は新規追加
2. **v1 系は無変更**(P5 §0-1 と同じ):`ScintillaHost.cs` / `Sci.cs` / `Scintilla5.NET` の削除以外に v1 触れず(v1 UIA 4 ファイル+UiaProbe は P7 送り)
3. **UTF-16 廃止**:Core.Text 層の SelectableEncodings から 1200/1201 除去。EncodingDetector から UTF-16 BOM 判定削除。App/tests に UTF-16 依存が残っていないことを機械的確認(grep)
4. **Stream I/O 化**:TextFileService.Load を byte 全読み→string 全文化から Stream ベース(UTF-8 は変換ゼロで `TextBufferBuilder.Add` にチャンク流し)へ。SJIS/EUC-JP は Decoder でチャンク単位変換
5. **SearchController 閾値二層化**:設計書 §2-8 の 64MB 閾値。閾値以下は現行 SnapshotText 経由維持、閾値超は `TextSnapshot.GetText(pos, len)` の窓 API で局所照合。regex は行単位限定(P0/P5 の壊れる契約解消)
6. **UI 変更は BeginInvoke**:P5 と同じ(InvokeRequired ガード)
7. **Announcer 配線**:App 層 Announcer に `WordNavigated` を購読させる(空行と同構造)。smoke `UiaSmokeAnnouncer.cs` は Task 17 で削除
8. **SR/UIA 切り分け猶予**:`SrContext.UseNativeReading` を常に false 化(SrRoute.Nvda 経路実質死)。SrRoute enum / SrRouteSelector / CsvFocusSink は**残す**(P7 実機総合検証後に完全撤去)
9. **各 Task 完了時に 1 コミット**。コミットメッセージ先頭は `P6: Task N ` で統一
10. **build 0 警告維持**:各 Task 完了時に `dotnet build` して Warning/Error 件数 0 を確認
11. **683 → 722 → 全緑維持**:各 Task 完了時に Core.Tests / Editor.Tests を実行して緑を確認(P6 は App 層置換のため App 層に新規テストは追加しない=WinForms smoke で目視+P6 完了時の全機能手動チェックリスト §7)
12. **リリース CI**:`release.yml` は Scintilla/Lexilla を明示参照していない=`Scintilla5.NET` PackageReference を Editor csproj から削除するだけで publish 出力に DLL が入らない(workflow 変更不要)

---

## Phase A: EditorControl 互換 API 先出し(Task 1〜5)

App 層置換で必要な受け口を EditorControl 側に用意する。追加のみ・既存 API 無変更・v1 系無変更。

## Task 1: `Text` プロパティ + `ReplaceSource(TextBuffer)`

**目的**: FileController(ロード/保存/バックアップ復元)が使う `doc.Editor.Text = "..."` パターンに対応。SetSource は 1 度限りだが、ReplaceSource は再バインドを許す=ファイル開き直しで Document ごとにコントロールを作り直す実装コストを削減。

**対象**:
- Modify: `src/yEdit.Editor/EditorControl.cs`(Text プロパティ + ReplaceSource + _buffer 参照解除 + イベント購読解除)
- Create: `tests/yEdit.Editor.Tests/EditorControlTextPropertyTests.cs`

**Step 1: 失敗するテストを書く**

`tests/yEdit.Editor.Tests/EditorControlTextPropertyTests.cs`(新規):

```csharp
using System.Windows.Forms;
using yEdit.Core.Buffers;
using yEdit.Editor;
using Xunit;

namespace yEdit.Editor.Tests;

public class EditorControlTextPropertyTests
{
    [Fact]
    public void Text_Get_ReturnsBufferSnapshot()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("hello"));
            Assert.Equal("hello", ctrl.Text);
        });
    }

    [Fact]
    public void Text_Set_ReplacesBufferAndResetsCaret()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("original"));
            ctrl.SetCaretCharOffset(4);
            ctrl.Text = "brand new content";
            Assert.Equal("brand new content", ctrl.Text);
            Assert.Equal(0, ctrl.CaretCharOffset);   // 差し替えでキャレット先頭
            Assert.False(ctrl.Modified);              // 差し替え直後は unmodified
        });
    }

    [Fact]
    public void Text_SetEmpty_ClearsBuffer()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("content"));
            ctrl.Text = string.Empty;
            Assert.Equal(string.Empty, ctrl.Text);
        });
    }

    [Fact]
    public void ReplaceSource_ExistingBuffer_Succeeds()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("first"));
            var buf2 = TextBuffer.FromString("second");
            ctrl.ReplaceSource(buf2);
            Assert.Equal("second", ctrl.Text);
        });
    }
}
```

**Step 2: 失敗を確認**

```
dotnet test tests/yEdit.Editor.Tests --filter FullyQualifiedName~EditorControlTextPropertyTests
```

Expected: build error(`Text` プロパティが存在しない=Control.Text は shadow で用意する必要あり)。

**Step 3: EditorControl.cs に Text/ReplaceSource を追加**

既存 `public void SetSource(TextBuffer buffer)` の直後(EditorControl.cs:170 付近)に:

```csharp
/// <summary>
/// P6 Task 1: 本文全体を string で読み書きする互換 API。
/// getter は現在の TextBuffer スナップショットから全文を返す(内部 GetText と同じ経路)。
/// setter は新規 TextBuffer を組み立てて <see cref="ReplaceSource"/> に流す。
/// SetSource 前 / _buffer=null で getter は空文字列を返し、setter は初回 SetSource として扱う。
/// </summary>
/// <remarks>
/// Control.Text と同名だが、Control.Text は本文非公開原則(§0-6 / P5 Task 7)により
/// WM_GETTEXT/WM_GETTEXTLENGTH で応答しない=シャドウ new が必要。
/// </remarks>
public new string Text
{
    get => _buffer?.Current.GetText(0, _buffer.Current.CharLength) ?? string.Empty;
    set
    {
        var buf = TextBuffer.FromString(value ?? string.Empty);
        if (_buffer is null) SetSource(buf);
        else ReplaceSource(buf);
    }
}

/// <summary>
/// P6 Task 1: 既存 TextBuffer を新しいものに差し替える(ファイル開き直し・バックアップ復元用)。
/// SetSource が 1 度限りなのに対し、これは任意回呼べる=Document ごとに EditorControl を
/// 作り直すのを避ける。
/// キャレット/選択/スクロール/スナップショットキャッシュを全てリセットする。
/// </summary>
public void ReplaceSource(TextBuffer buffer)
{
    ArgumentNullException.ThrowIfNull(buffer);
    _buffer = buffer;
    _caret = 0;
    _anchor = 0;
    _topLine = 0;
    _scrollX = 0;
    _lastCaretLine = 0;
    _desiredXpx = -1;
    UpdateVerticalScrollbar();
    UpdateHorizontalScrollbar();
    if (_hasFocus)
    {
        PositionCaret();
    }
    Invalidate();
    CacheSnapshot();  // P5 RPC スレッド用スナップショット更新
}
```

**Step 4: テスト通過を確認**

```
dotnet test tests/yEdit.Editor.Tests --filter FullyQualifiedName~EditorControlTextPropertyTests
```

Expected: 4 passed。

**Step 5: 既存テスト全緑 + build 0 警告**

```
dotnet build yEdit.sln
dotnet test tests/yEdit.Core.Tests
dotnet test tests/yEdit.Editor.Tests
```

Expected: 0 warnings / Core.Tests 540 passed / Editor.Tests 182+4=186 passed。

**Step 6: コミット**

```bash
git add src/yEdit.Editor/EditorControl.cs tests/yEdit.Editor.Tests/EditorControlTextPropertyTests.cs
git commit -m "P6: Task 1 EditorControl.Text プロパティ + ReplaceSource 追加(App 層互換 API 先出し)"
```

**完了条件**:
- `Text { get; set; }` シャドウプロパティで本文の read/write
- `ReplaceSource(TextBuffer)` で既存 buffer を差し替え
- テスト 4 件緑
- build 0 警告

---

## Task 2: `SnapshotText` プロパティ + `SelectCharRange(start, length)` エイリアス + `MoveCaretCharOffset(int)` エイリアス

**目的**: App 層(SearchController×4/BackupCoordinator×4/Document×1/MainForm×3)が使う `ed.SnapshotText` と、App 層と CsvController が使う長さベースの `SelectCharRange(start, length)` / `MoveCaretCharOffset(offset)` を先出し。既存 API のエイリアスなので実装は 1 行。

**対象**:
- Modify: `src/yEdit.Editor/EditorControl.cs`
- Create: `tests/yEdit.Editor.Tests/EditorControlCompatApiTests.cs`

**Step 1: 失敗するテストを書く**

```csharp
using System.Windows.Forms;
using yEdit.Core.Buffers;
using yEdit.Editor;
using Xunit;

namespace yEdit.Editor.Tests;

public class EditorControlCompatApiTests
{
    [Fact]
    public void SnapshotText_ReturnsFullText()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("hello world"));
            Assert.Equal("hello world", ctrl.SnapshotText);
        });
    }

    [Fact]
    public void SelectCharRange_LengthVersion_SelectsRange()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("hello world"));
            ctrl.SelectCharRange(6, 5);   // "world"
            Assert.Equal((6, 11), ctrl.GetSelectionCharRange());
        });
    }

    [Fact]
    public void MoveCaretCharOffset_MovesCaret()
    {
        Sta.Run(() =>
        {
            using var ctrl = new EditorControl();
            ctrl.SetSource(TextBuffer.FromString("hello world"));
            ctrl.MoveCaretCharOffset(5);
            Assert.Equal(5, ctrl.CaretCharOffset);
        });
    }
}
```

**Step 2: 失敗を確認**

Expected: build error(3 メンバ未定義)。

**Step 3: EditorControl.cs に 3 API を追加**

`SnapshotText` は既存 `internal GetText()` の public 版:

```csharp
/// <summary>
/// P6 Task 2: 現在の TextBuffer スナップショットから全文を返す(App 層互換)。
/// 大容量ファイルでは 64MB 閾値二層化(<see cref="SearchController"/>)で回避されるが、
/// 呼び出し側でメモリ配慮が必要な場面もある(App 層側で判定=§設計 §2-8)。
/// </summary>
public string SnapshotText => _buffer?.Current.GetText(0, _buffer.Current.CharLength) ?? string.Empty;

/// <summary>P6 Task 2: 長さベースの選択設定エイリアス(App 層互換)。</summary>
public void SelectCharRange(int start, int length)
    => SetSelectionCharRange(start, start + Math.Max(0, length));

/// <summary>P6 Task 2: <see cref="SetCaretCharOffset"/> のエイリアス(App 層互換)。</summary>
public void MoveCaretCharOffset(int offset) => SetCaretCharOffset(offset);
```

**Step 4〜6**: テスト通過 → 全テスト緑 → コミット

```bash
git add src/yEdit.Editor/EditorControl.cs tests/yEdit.Editor.Tests/EditorControlCompatApiTests.cs
git commit -m "P6: Task 2 SnapshotText / SelectCharRange(length) / MoveCaretCharOffset 互換 API 追加"
```

Expected: Core.Tests 540 passed / Editor.Tests 186+3=189 passed。

**完了条件**: 3 API 追加、テスト 3 件緑、build 0 警告。

---

## Task 3: `LineCount` + `GoToLine(int)` + `CurrentPosition` エイリアス

**目的**: MainForm.cs:433,457-462 の `ed.Lines.Count` / `ed.Lines[target-1].Goto()` / `ed.CurrentPosition` を、EditorControl の `LineCount` / `GoToLine(int)` / `CurrentPosition` に置き換えるための受け口。

**対象**:
- Modify: `src/yEdit.Editor/EditorControl.cs`
- Modify: `tests/yEdit.Editor.Tests/EditorControlCompatApiTests.cs`(既存へ追記)

**Step 1: テストを追加**

`EditorControlCompatApiTests.cs` に以下を追加:

```csharp
[Fact]
public void LineCount_ReturnsBufferLineCount()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("aaa\nbbb\nccc"));
        Assert.Equal(3, ctrl.LineCount);
    });
}

[Fact]
public void GoToLine_MovesCaretToLineStart()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("aaa\nbbb\nccc"));
        ctrl.GoToLine(2);   // 0-based=2 → 3行目 "ccc"
        Assert.Equal(8, ctrl.CaretCharOffset);
    });
}

[Fact]
public void CurrentPosition_MatchesCaret()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("hello"));
        ctrl.SetCaretCharOffset(3);
        Assert.Equal(3, ctrl.CurrentPosition);
    });
}
```

**Step 2: EditorControl.cs に追加**

```csharp
/// <summary>P6 Task 3: 現在のバッファ論理行数(App 層互換=`Lines.Count` 相当)。</summary>
public int LineCount => _buffer?.Current.LineCount ?? 0;

/// <summary>P6 Task 3: 指定 0-based 行の先頭にキャレット移動(App 層互換=`Lines[i].Goto()` 相当)。</summary>
public void GoToLine(int line)
{
    if (_buffer is null) return;
    var snap = _buffer.Current;
    int clamped = Math.Clamp(line, 0, snap.LineCount - 1);
    SetCaretCharOffset(snap.GetLineStart(clamped));
}

/// <summary>P6 Task 3: `CaretCharOffset` の別名(App 層互換=Scintilla の `CurrentPosition`)。</summary>
public int CurrentPosition => _caret;
```

**Step 3〜5**: テスト通過 → 全テスト緑 → コミット

```bash
git add src/yEdit.Editor/EditorControl.cs tests/yEdit.Editor.Tests/EditorControlCompatApiTests.cs
git commit -m "P6: Task 3 LineCount / GoToLine / CurrentPosition 互換 API 追加"
```

Expected: Editor.Tests 189+3=192 passed。

---

## Task 4: `SavePointLeft` / `SavePointReached` / `UpdateUI` イベント発火

**目的**: DocumentManager.cs:57-60 が購読する 3 イベント。TextBuffer には `Modified` プロパティしかなく、変化通知イベントが必要。

- `SavePointLeft`: `MarkSaved` 直後の Modified=false → true 遷移で発火
- `SavePointReached`: `MarkSaved` 呼び出し時に発火(Modified=false)
- `UpdateUI`: キャレット/選択/表示範囲の変化ごとに発火(P3 の `SetCaretCharOffset`/`SetSelectionCharRange`/`AfterEdit` 末尾)

**対象**:
- Modify: `src/yEdit.Editor/EditorControl.cs`
- Modify: `tests/yEdit.Editor.Tests/EditorControlCompatApiTests.cs`(既存へ追記)

**Step 1: テスト追加**

```csharp
[Fact]
public void SavePointReached_Fires_WhenMarkSaved()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("hello"));
        ctrl.ReplaceCharRange(0, 5, "xxx");  // Modified=true
        int fired = 0;
        ctrl.SavePointReached += (_, _) => fired++;
        ctrl.SetSavePoint();
        Assert.Equal(1, fired);
        Assert.False(ctrl.Modified);
    });
}

[Fact]
public void SavePointLeft_Fires_WhenModifiedAfterSave()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("hello"));
        int fired = 0;
        ctrl.SavePointLeft += (_, _) => fired++;
        ctrl.ReplaceCharRange(0, 5, "xxx");   // Modified=false → true
        Assert.Equal(1, fired);
    });
}

[Fact]
public void UpdateUI_Fires_OnCaretMove()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("hello"));
        int fired = 0;
        ctrl.UpdateUI += (_, _) => fired++;
        ctrl.SetCaretCharOffset(3);
        Assert.True(fired >= 1);
    });
}
```

**Step 2: EditorControl.cs に追加**

```csharp
// P6 Task 4: App 層互換イベント。TextBuffer の Modified 状態遷移と UI 更新契機を通知する。

/// <summary>P6 Task 4: `SetSavePoint`(=MarkSaved)呼び出し時に発火(App 層は「保存済み表示」に切替)。</summary>
public event EventHandler? SavePointReached;

/// <summary>P6 Task 4: 保存後最初の編集で Modified=false→true 遷移時に発火(App 層は「変更あり」表示に切替)。</summary>
public event EventHandler? SavePointLeft;

/// <summary>P6 Task 4: キャレット/選択/表示範囲変化時に発火(App 層のステータスバー更新用)。</summary>
public event EventHandler? UpdateUI;

private bool _wasModified;   // 直前の Modified 状態(SavePointLeft 検出用)
```

`SetSavePoint()` を書き換え:
```csharp
public void SetSavePoint()
{
    _buffer?.MarkSaved();
    _wasModified = false;
    SavePointReached?.Invoke(this, EventArgs.Empty);
}
```

`AfterEdit()` 末尾:
```csharp
private void AfterEdit()
{
    // ... 既存処理 ...
    CacheSnapshot();
    RaiseUia(...);
    if (RaiseUiaSelectionEvents) RaiseUia(...);
    // P6 Task 4: Modified 遷移と UI 更新通知
    if (!_wasModified && Modified) SavePointLeft?.Invoke(this, EventArgs.Empty);
    _wasModified = Modified;
    UpdateUI?.Invoke(this, EventArgs.Empty);
}
```

`SetCaretCharOffset` / `SetSelectionCharRange` / `MoveCaretWithSelection` / `SetSelectionAnchored` の末尾に:
```csharp
UpdateUI?.Invoke(this, EventArgs.Empty);
```

**Step 3〜5**: テスト通過 → 全緑 → コミット

```bash
git commit -m "P6: Task 4 SavePointLeft/SavePointReached/UpdateUI イベント発火配線"
```

Expected: Editor.Tests 192+3=195 passed。

---

## Task 5: `ConvertEols(LineEnding)` 実装

**目的**: FileController.cs:208 の EOL 一括変換(保存時)。EolMode 変更だけでは既存本文の `\r\n`/`\r`/`\n` は書き換わらないため、バッファを再構築する。

**対象**:
- Modify: `src/yEdit.Editor/EditorControl.cs`
- Create: `tests/yEdit.Editor.Tests/EditorControlConvertEolsTests.cs`

**Step 1: テスト**

```csharp
[Fact]
public void ConvertEols_ToCrlf_ReplacesLoneLfs()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("aaa\nbbb\nccc"));
        ctrl.ConvertEols(LineEnding.Crlf);
        Assert.Equal("aaa\r\nbbb\r\nccc", ctrl.SnapshotText);
        Assert.Equal(LineEnding.Crlf, ctrl.EolMode);
    });
}

[Fact]
public void ConvertEols_ToLf_ReplacesCrlfs()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("aaa\r\nbbb\r\nccc"));
        ctrl.ConvertEols(LineEnding.Lf);
        Assert.Equal("aaa\nbbb\nccc", ctrl.SnapshotText);
    });
}

[Fact]
public void ConvertEols_ToCr_ReplacesAll()
{
    Sta.Run(() =>
    {
        using var ctrl = new EditorControl();
        ctrl.SetSource(TextBuffer.FromString("aaa\r\nbbb\nccc\r"));
        ctrl.ConvertEols(LineEnding.Cr);
        Assert.Equal("aaa\rbbb\rccc\r", ctrl.SnapshotText);
    });
}
```

**Step 2: EditorControl.cs に追加**

```csharp
/// <summary>
/// P6 Task 5: 本文中の改行を <paramref name="eol"/> に一括変換する(App 層互換)。
/// 既存本文の `\r\n` / `\r` / `\n` を検出→指定 EOL に置換した文字列で
/// <see cref="ReplaceSource(TextBuffer)"/>。EolMode も更新する。
/// </summary>
public void ConvertEols(LineEnding eol)
{
    if (_buffer is null) return;
    string src = SnapshotText;
    string target = eol switch
    {
        LineEnding.Crlf => "\r\n",
        LineEnding.Lf => "\n",
        LineEnding.Cr => "\r",
        _ => "\n",
    };
    // 一旦 LF に正規化してから target に置換=CRLF/CR/LF 混在を統一処理
    string normalized = src.Replace("\r\n", "\n").Replace("\r", "\n");
    string converted = target == "\n" ? normalized : normalized.Replace("\n", target);
    if (converted == src) { EolMode = eol; return; }   // no-op fast path
    ReplaceSource(TextBuffer.FromString(converted));
    EolMode = eol;
}
```

**Step 3〜5**: テスト通過 → 全緑 → コミット

```bash
git commit -m "P6: Task 5 ConvertEols 実装(バッファ再構築による EOL 一括変換)"
```

Expected: Editor.Tests 195+3=198 passed。

---

## Phase B: Core.Text 層の UTF-16 廃止 + Stream I/O 化(Task 6〜7)

## Task 6: EncodingCatalog / EncodingDetector から UTF-16 削除

**目的**: 設計書 §1 の「UTF-8/Shift_JIS/EUC-JP の3種のみ」を実装反映。`SelectableEncodings` から 1200/1201 を除去し、Detector から UTF-16 BOM 判定・名前マップも削除。

**対象**:
- Modify: `src/yEdit.Core/Text/EncodingCatalog.cs`
- Modify: `src/yEdit.Core/Text/EncodingDetector.cs`
- Modify: `tests/yEdit.Core.Tests/Text/EncodingCatalogTests.cs` / `EncodingDetectorTests.cs`(既存があれば)

**Step 1: 既存の SelectableEncodings と UTF-16 関連コードを特定**

```
grep -n "1200\|1201\|utf-16\|UTF-16\|UnicodeEncoding" src/yEdit.Core/Text/
```

**Step 2: EncodingCatalog.cs から UTF-16 3 件を削除**

`SelectableEncodings` 配列から 1200(UTF-16 LE)と 1201(UTF-16 BE)のエントリを削除。

**Step 3: EncodingDetector.cs から UTF-16 BOM 判定削除**

`if (bytes.Length >= 2 && bytes[0] == 0xFF && bytes[1] == 0xFE) return utf-16-le;` 等の分岐を削除。UTF-8 BOM(EF BB BF)判定は残す。

**Step 4: 既存テスト更新**

UTF-16 の期待値を含むテストを削除または SJIS/EUC-JP テストに置換。

**Step 5: build + 全テスト + コミット**

```bash
git commit -m "P6: Task 6 EncodingCatalog/EncodingDetector から UTF-16 削除(3種のみ)"
```

Expected: Core.Tests 540-N+M passed(削除分と置換分の差)/ Editor.Tests 198 passed / 0 警告。

**完了条件**: SelectableEncodings に UTF-8/SJIS(932)/EUC-JP(51932)の3種のみ、Detector に UTF-16 判定なし。

---

## Task 7: TextFileService の Stream I/O 化

**目的**: 現行の `File.ReadAllBytes → Encoding.GetString → EditorControl.Text = string` は 1GB ファイルで OOM。設計 §2-2 の「UTF-8 チャンク直載せ」に置換。UTF-8 は変換ゼロで `TextBufferBuilder.AppendUtf8Chunk(byte[])` に流し、SJIS/EUC-JP は `Decoder` でチャンク単位に UTF-16 化してから `TextBuffer.FromString` に落とす(SJIS/EUC-JP は日本語で高々数百 MB 想定=許容)。

**対象**:
- Modify: `src/yEdit.Core/Text/TextFileService.cs`
- Modify: `tests/yEdit.Core.Tests/Text/TextFileServiceTests.cs`

**Step 1: TextBufferBuilder の chunk API 存在確認**

```
grep -n "AppendUtf8Chunk\|AppendBytes\|Add(byte" src/yEdit.Core/Buffer/
```

無ければ Task 7 前半で追加(TextBufferBuilder に `AppendUtf8Chunk(ReadOnlySpan<byte>)` を実装)。

**Step 2: TextFileService.Load を Stream ベースに書き換え**

```csharp
public static TextBuffer LoadAsBuffer(string path, Encoding encoding)
{
    using var stream = File.OpenRead(path);
    if (encoding.CodePage == 65001) // UTF-8
    {
        // UTF-8 は変換ゼロ経路
        var builder = new TextBufferBuilder();
        Span<byte> buf = stackalloc byte[64 * 1024];   // 64KB チャンク
        int n;
        while ((n = stream.Read(buf)) > 0)
            builder.AppendUtf8Chunk(buf.Slice(0, n));
        return builder.Build();
    }
    else
    {
        // SJIS/EUC-JP は Decoder でチャンク変換
        using var reader = new StreamReader(stream, encoding);
        string content = reader.ReadToEnd();
        return TextBuffer.FromString(content);
    }
}
```

**Step 3: Save も Stream ベースに**

```csharp
public static void Save(string path, TextBuffer buffer, Encoding encoding, LineEnding eol)
{
    string eolStr = eol switch { LineEnding.Crlf => "\r\n", LineEnding.Cr => "\r", _ => "\n" };
    string content = buffer.Current.GetText(0, buffer.Current.CharLength);
    // EOL 変換は EditorControl.ConvertEols で事前に済ませる契約=ここは既存値を保存
    File.WriteAllBytes(path, encoding.GetBytes(content));
}
```

**Step 4: 既存テスト調整 + 新規テスト(Stream I/O 経路)**

Load/Save のラウンドトリップテストで 3 エンコーディング全て確認。UTF-16 テストは削除。

**Step 5: コミット**

```bash
git commit -m "P6: Task 7 TextFileService Stream I/O 化(UTF-8 チャンク直載せ+SJIS/EUC-JP Decoder 変換)"
```

**完了条件**: Load/Save が Stream 経路、3 エンコーディングのラウンドトリップ全緑、UTF-16 テスト削除、build 0 警告。

**申し送り**: 1GB ベンチは P7 で実測(現時点で smoke 上の目視で OK ならバックログへ)。

---

## Phase C: Document/DocumentManager の型置換(Task 8〜9)

## Task 8: `Document.Editor` 型を `ScintillaHost` → `EditorControl` に変更

**目的**: 設計書 §「壊れる契約は SnapshotText/Text のみ」通り、`Document.Editor` の宣言型を変える。App 層の全 `doc.Editor.XXX` 経路が Task 1〜5 の互換 API で通ることを確認。

**対象**:
- Modify: `src/yEdit.App/Document.cs`(型変更 + 使用箇所)
- Modify: `src/yEdit.App/CsvFocusSink.cs`(コメント更新のみ・機能は残す=Task 15)

**Step 1: Document.cs の型変更**

```csharp
// 変更前
public ScintillaHost Editor { get; }
public Document(ScintillaHost editor, ...)

// 変更後
public EditorControl Editor { get; }
public Document(EditorControl editor, ...)
```

`Editor.Modified` / `Editor.SnapshotText` 等の使用箇所は互換 API があるので無変更。

**Step 2: ビルド確認(まだ MainForm 側は ScintillaHost を new しているので失敗する想定)**

Expected: `MainForm.cs:71` で ScintillaHost を Document に渡すコンパイルエラー。Task 9 で解消。

**Step 3: コミットしない**(Task 9 と統合コミット)

---

## Task 9: `MainForm.CreateEditor` を `EditorControl` に置換

**目的**: Task 8 で導入したコンパイルエラーを解消。`ScintillaHost` を new している MainForm.cs:71 を `EditorControl` に置換。ApplySrAdaptation 呼び出しは削除(EditorControl は SR 二系統機構を持たない=UIA v2 単一経路)。

**対象**:
- Modify: `src/yEdit.App/MainForm.cs`(CreateEditor + Editor.XXX 経路)

**Step 1: CreateEditor の書き換え**

```csharp
// 変更前
private ScintillaHost CreateEditor()
{
    var ed = new ScintillaHost { Dock = DockStyle.Fill };
    _srCtx.ApplySrAdaptation(ed);   // SR 二系統
    return ed;
}

// 変更後
private EditorControl CreateEditor()
{
    var ed = new EditorControl { Dock = DockStyle.Fill };
    // SR 適応は EditorControl 単一経路(UIA v2)=ApplySrAdaptation 呼び出し不要
    // SetSource は Document 側で TextBuffer を渡すタイミングで呼ぶ
    return ed;
}
```

**Step 2: doc.Editor.XXX 経路の確認**

grep で `doc.Editor.` を全リストアップし、Task 1〜5 の互換 API で通るか確認:
- `.Modified` / `.CanUndo` / `.CanRedo` / `.Undo` / `.Redo` / `.Cut` / `.Copy` / `.Paste` / `.SelectAll` / `.SnapshotText` / `.Text` / `.CurrentPosition` / `.CaretCharOffset` / `.CurrentLine` / `.GetColumn(...)` / `.SelectCharRange(...)` / `.ReplaceCharRange(...)` / `.MoveCaretCharOffset(...)` / `.EolMode` / `.Overtype` / `.ReadOnly` / `.EmptyUndoBuffer` / `.SetSavePoint` / `.LineCount` / `.GoToLine(...)` / `.HighlightCharRange` / `.ClearHighlight` / `.EnsureVisibleCharRange` / `.PointFromCharOffset` / `.LineHeightPx` / `.WrapColumns` / `.ShowLineNumbers` / `.ShowWhitespace` / `.HighlightCurrentLine` / `.RaiseUiaSelectionEvents` / `.CaretEnteredEmptyLine` / `.SavePointLeft` / `.SavePointReached` / `.UpdateUI`

全て EditorControl に存在(P3〜P5 + Task 1〜5)。

**Step 3: ビルドで残エラーを潰す**

Scintilla 特有 API(`ed.Lines[i].Goto()`, `ed.Lines.Count`, `ed.CurrentPosition` 等)は Task 3 の `GoToLine` / `LineCount` / `CurrentPosition` に置換。

**Step 4: コミット**

```bash
git add src/yEdit.App/Document.cs src/yEdit.App/MainForm.cs
git commit -m "P6: Task 8/9 Document.Editor 型 → EditorControl + MainForm.CreateEditor 置換(App 層一発差替の核)"
```

Expected: App プロジェクトビルド成功、smoke launcher 起動可(App の UI 目視)、build 0 警告。

---

## Phase D: FileController/SearchController/BackupCoordinator 書き換え(Task 10〜12)

## Task 10: FileController を EditorControl API + Stream I/O に置換

**目的**: `doc.Editor.Text = loaded.Text`(現行=全文 string 経由)を、Task 7 の `TextFileService.LoadAsBuffer` + `doc.Editor.ReplaceSource(buffer)` パターンに置換。

**対象**:
- Modify: `src/yEdit.App/FileController.cs`

**Step 1〜3**: 書き換え → smoke 起動確認 → コミット

```bash
git commit -m "P6: Task 10 FileController を Stream I/O + ReplaceSource に書き換え(1GB 級 OOM 回避)"
```

**完了条件**: File>Open/Save/Save As/バックアップ復元が全てチャンク Stream I/O で動く。

---

## Task 11: SearchController の 64MB 閾値二層化

**目的**: 設計書 §2-8「壊れる契約=SnapshotText/Text のみ」の 64MB 閾値。閾値以下→現行 SnapshotText 経路、閾値超→`TextSnapshot.GetText(pos, len)` の窓 API で局所照合。regex は行単位限定。

**対象**:
- Modify: `src/yEdit.App/SearchController.cs`

**Step 1: 閾値定数追加**

```csharp
private const int SnapshotTextThresholdBytes = 64 * 1024 * 1024;   // 64MB
```

**Step 2: FindNext/FindPrev/Count/ReplaceInRange を閾値分岐**

閾値超の場合、`TextSnapshot.GetText(pos, len)` で 4KB ウィンドウを歩きながら Boyer-Moore/KMP で局所照合。設計書§2-8 の「大容量時: 検索=窓照合・CSV/Markdown無効化」通り。

**Step 3〜4**: ユニットテスト(閾値上下で挙動一致) → コミット

```bash
git commit -m "P6: Task 11 SearchController 64MB 閾値二層化(窓照合+全文 fallback)"
```

**完了条件**: 閾値以下は既存挙動不変、閾値超で窓照合が動く、Core.Tests 全緑。

---

## Task 12: BackupCoordinator の SnapshotText 経路確認

**目的**: BackupCoordinator は Task 2 の SnapshotText で機械的に通るはずだが、大容量ファイル対応の申し送りを設計書に追記(バックアップも Stream 化するか判断保留)。

**対象**:
- Modify: `src/yEdit.App/BackupCoordinator.cs`(通るか確認・不要なら型変更のみ)
- Modify: `docs/plans/2026-07-05-custom-editcontrol-design.md`(申し送り)

**Step 1〜2**: 動作確認 → 申し送り追記 → コミット

```bash
git commit -m "P6: Task 12 BackupCoordinator 動作確認(申し送り=大容量時のチャンク書き)"
```

---

## Phase E: CSV/禁則/MD/行ジャンプ/文字情報 配線確認(Task 13〜14)

## Task 13: CsvController + CsvCellEditor 配線確認

**目的**: CSV モードの Scintilla ハンドル依存を EditorControl 経路に置換。CsvFocusSink は「無効化のみで残す」(§0-8)。

**対象**:
- Modify: `src/yEdit.App/CsvController.cs`
- Modify: `src/yEdit.App/CsvCellEditor.cs`
- Modify: `src/yEdit.App/Document.cs`(FocusTarget を Editor 固定に変更・Sink 生成は残す)

**Step 1〜3**: 変更 → smoke 動作確認(CSV 開き・セル移動・F2 編集) → コミット

```bash
git commit -m "P6: Task 13 CSV モード配線(CsvFocusSink は無効化のみで残す)"
```

**完了条件**: CSV ファイルを開いて素キーコマンドでセル移動、F2 でオーバーレイ編集、Escape で復帰。

---

## Task 14: 禁則整形 / Markdown / 行ジャンプ / 文字情報 配線確認

**目的**: 残機能の EditorControl API 適合。禁則(`SnapshotText` + `SelectCharRange` + `ReplaceCharRange`)、Markdown プレビュー(`SnapshotText`)、行ジャンプ(`LineCount` + `GoToLine`)、文字情報(`CurrentLine` + `CurrentPosition` + `GetColumn` + `SnapshotText`)。

**対象**:
- Modify: `src/yEdit.App/MainForm.cs`(4 機能の該当ハンドラ)
- Modify: 個別 controller があれば同上

**Step 1〜3**: 変更 → smoke で目視 → コミット

```bash
git commit -m "P6: Task 14 禁則/MD/行ジャンプ/文字情報 の EditorControl API 適合"
```

---

## Phase F: SR/UIA 猶予対応 + Announcer 配線(Task 15〜16)

## Task 15: `SrContext.UseNativeReading` を常に false 化

**目的**: 設計 §2-6 の切り分け猶予。SrRoute.Nvda / SrRouteSelector / CsvFocusSink は残すが、実質死なせる=`UseNativeReading` が false ならすべて UIA v2 経路(EditorControl 単一)に統一される。

**対象**:
- Modify: `src/yEdit.App/Speech/SrContext.cs`

**Step 1: 定数化**

```csharp
public bool UseNativeReading => false;   // P6 猶予=常に false・P7 で SrContext ごと撤去
```

**Step 2**: build + smoke で NVDA も PC-Talker も UIA v2 経路で読めることを目視 → コミット

```bash
git commit -m "P6: Task 15 SrContext.UseNativeReading を false 固定(SR 二系統機構の実質死)"
```

---

## Task 16: App 層 Announcer に `WordNavigated` + `CaretEnteredEmptyLine` 配線

**目的**: P5 で先出しした `EditorControl.WordNavigated` を App 層 Announcer に配線(PC-Talker 単語ナビ補完)。空行「空行」発声も EditorControl.CaretEnteredEmptyLine 経路に統一。smoke `UiaSmokeAnnouncer.cs` は Task 17 で削除。

**対象**:
- Modify: `src/yEdit.App/Speech/AnnouncerFactory.cs` / `UiaAnnouncer.cs` / `PcTalkerAnnouncer.cs`
- Modify: `src/yEdit.App/DocumentManager.cs`(`ActiveWordNavigated` イベント追加)
- Modify: `src/yEdit.App/MainForm.cs`(WordNavigated 購読→Announcer.Say)

**Step 1: DocumentManager に ActiveWordNavigated イベント**

```csharp
public event EventHandler<WordNavigatedEventArgs>? ActiveWordNavigated;
```

購読・転送は SavePoint/UpdateUI と同構造。

**Step 2: MainForm で購読 + Announcer.Say**

```csharp
_docs.ActiveWordNavigated += (_, e) =>
{
    if (_srCtx.Mode == SpeechMode.PcTalker || _srCtx.Mode == SpeechMode.Uia)
    {
        var span = ((IUiaTextHost)ActiveDoc.Editor).GetTextRange(e.WordStart, e.WordEnd - e.WordStart);
        if (!string.IsNullOrWhiteSpace(span)) _announcer.Say(span.Trim());
    }
};
```

**Step 3〜4**: smoke で NVDA/PC-Talker/ナレーターの単語ナビ発声確認 → コミット

```bash
git commit -m "P6: Task 16 App 層 Announcer に WordNavigated + CaretEnteredEmptyLine 配線"
```

**完了条件**: PC-Talker で Ctrl+←→ が単語スパンを発声する(P0 で確定した補完受け口が App 層で実挙動化)。

---

## Phase G: 撤去作業(Task 17〜18)

## Task 17: Scintilla5.NET PackageReference 削除 + ScintillaHost.cs / Sci.cs 削除

**目的**: 一括撤去の実物。`Scintilla5.NET` PackageReference を Editor csproj から削除、`ScintillaHost.cs` / `Sci.cs` ファイル削除。EditorAppearance.cs(ScintillaNET を using している唯一のファイル)は Task 9 で機能置換済(EditorControl.ApplyAppearance に委譲)なので、残っている `using ScintillaNET;` を削除。

**対象**:
- Delete: `src/yEdit.Editor/ScintillaHost.cs`
- Delete: `src/yEdit.Editor/Sci.cs`
- Modify: `src/yEdit.Editor/yEdit.Editor.csproj`(`<PackageReference Include="Scintilla5.NET" ...>` 削除)
- Modify: `src/yEdit.App/EditorAppearance.cs`(`using ScintillaNET;` 削除・実装は既に EditorControl.ApplyAppearance に置換済み)
- Delete: `tools/verify-uia-sci.ps1` / `walk-test-sci.ps1`(v1 用・**要**)
- Modify: `tools/word-sim.ps1`(`-Target ProbeExe` 分岐削除・EditorSmokeExe 固定化)

**Step 1: Scintilla 参照ゼロ確認**

```
grep -rn "Scintilla\|ScintillaHost\|Sci\." src/ tests/ --include="*.cs"
```

Sci 系は EditorControl 内部で `System.Windows.Automation` の `Sci` に衝突しないか確認。

**Step 2: 削除実行**

```bash
git rm src/yEdit.Editor/ScintillaHost.cs src/yEdit.Editor/Sci.cs
git rm tools/verify-uia-sci.ps1 tools/walk-test-sci.ps1
```

**Step 3**: `yEdit.Editor.csproj` の PackageReference 削除、EditorAppearance の using 削除、word-sim.ps1 の分岐削除。

**Step 4: build**

Expected: 0 warnings / 0 errors。Scintilla の残存参照があればここでコンパイルエラー化=潰す。

**Step 5: publish 出力の確認**

```
dotnet publish src/yEdit.App -c Release -r win-x64 --self-contained false -o publish
ls publish/runtimes/win-x64/native/ 2>/dev/null   # Scintilla.dll / Lexilla.dll が存在しないこと
```

Expected: `runtimes/win-x64/native/` に Scintilla.dll / Lexilla.dll が**無い**。

**Step 6: コミット**

```bash
git add -A
git commit -m "P6: Task 17 Scintilla5.NET/ScintillaHost.cs/Sci.cs/v1 用 tools 一括撤去(publish 出力からネイティブ DLL 消滅)"
```

**完了条件**: build 0 警告、publish 出力にネイティブ DLL なし、全 722+新規テストが緑、smoke launcher 動作。

---

## Task 18: 全機能手動チェックリスト + 別エージェント最終レビュー + 設計書§3 P6 結果 + memory 更新

**目的**: P4/P5 と同パターン。App 層全機能の手動チェックリスト作成(§設計 P6 DoD)+ 別エージェントによる最終レビュー+ Critical/Important 対応+ 設計書§3 に P6 結果表追記+メモリ更新。

**対象**:
- Create: `docs/plans/2026-07-06-p6-manual-checklist.md`(App 層全機能・SR 3種フルマトリクス縮小版)
- Modify: `docs/plans/2026-07-05-custom-editcontrol-design.md`(§3 P6 結果表)
- Modify: `%USERPROFILE%\.claude\projects\D--src-yEdit\memory\custom-editcontrol.md`
- Modify: `%USERPROFILE%\.claude\projects\D--src-yEdit\memory\MEMORY.md`(1 行)

**Step 1: 手動チェックリスト作成**

主要機能を網羅(File Open UTF-8/SJIS/EUC-JP・Save/SaveAs・Undo/Redo・検索置換・Grep・行ジャンプ・文字情報・禁則整形・Markdown プレビュー・CSV モード・タブ管理・バックアップ復元・SR 発声(NVDA/PC-Talker で単語ナビ「空行」・IME=ATOK 未確定/確定/取消/BackSpace)。20-30 項目を想定。

**Step 2: 別エージェント最終レビュー**

Task ツールで `superpowers:code-reviewer` を起動。プロンプトに以下重点確認項目:
- App 層の Scintilla 参照ゼロ確認(grep 結果 0)
- publish 出力にネイティブ DLL なし
- EditorControl 互換 API の設計整合(Text/SnapshotText/SelectCharRange 系)
- Stream I/O 化の 3 エンコーディング全経路確認
- SearchController 閾値二層化の窓照合ロジック
- Announcer 配線の WordNavigated 経路(PC-Talker 補完)
- v1 UIA コード(IUiaTextHostLegacy/TextControlProvider 等)が P6 では無変更のこと=P7 送り

**Step 3: Critical/Important 対応**

同一 Task 内で修正コミット。

**Step 4: 設計書§3 P6 結果表**

P4/P5 と同フォーマットで記載(実装/レビュー/自動 DoD/申し送り/実機検証待ちマーカー)。

**Step 5: 手動チェックリスト実施(ユーザー)**

ユーザーに `docs/plans/2026-07-06-p6-manual-checklist.md` を渡し、全項目確認。結果を設計書§3 P6 に追記。

**Step 6: メモリ更新**

`custom-editcontrol.md` に P6 完了記録追記(P5 と同形式)。`MEMORY.md` の該当行を「P6 完了・P7 待ち」に更新。

**Step 7: コミット**

```bash
git add docs/plans/2026-07-06-p6-manual-checklist.md docs/plans/2026-07-05-custom-editcontrol-design.md
git commit -m "P6: Task 18 全機能手動チェックリスト + §3 P6 結果表追記(自動 DoD 全達成・実機検証待ち)"
```

**完了条件**:
- 手動チェックリスト作成
- 別エージェント最終レビュー完了 → Critical 0 / Important 0
- 設計書§3 P6 結果表追記
- メモリ更新
- 実機手動チェックリスト実施記録追記
- build 0 警告 / 全テスト緑

---

## 7. 全体 DoD(P6 完了条件)

- Core.Tests / Editor.Tests 全緑(Task 6/7 で UTF-16 テスト削除分は減算・全体で 720+ 想定)
- build 0 警告
- publish 出力に `Scintilla.dll` / `Lexilla.dll` が無い
- App 層に `Scintilla` / `ScintillaHost` / `SciNET` / `SCI_*` 参照が**ゼロ**(grep 結果 0)
- SR 発声(NVDA/PC-Talker/ナレーター)の主要操作全てで許容範囲
  - 単文字/行/SayAll/選択/単語ナビ/空行/フォーカス復帰/IME=ATOK 未確定/確定/取消 全 OK
- 別エージェント最終レビュー Critical 0 / Important 0
- 設計書§3 P6 結果表追記
- 撤退安全性:v1 UIA コード無変更 + UiaProbe 無変更のため、`git revert 08cd0f2..HEAD` で P5 完了状態(722 テスト緑)へ戻せる(P6 撤退)
- v1 UIA / UiaProbe / SrRoute.Nvda / CsvFocusSink / SrRouteSelector は**残っている**(P7 で完全撤去)

## 8. 次フェーズへの申し送り

- **P7(実機総合検証+撤去+リリース整備・第3ゲート)**:
  - 3 SR × 主要全機能フルマトリクス
  - 合格後: `SrRoute.Nvda`/`CsvFocusSink`/`ServeUiaProvider`/`ApplySrAdaptation`/`SrRouteSelector`/`SrContext`/`IUiaTextHostLegacy`/v1 UIA Provider 4 ファイル/`yEdit.UiaProbe` プロジェクト/`tools/verify-uia.ps1`/`tools/walk-test.ps1`/`tools/dump-uia.ps1`/`tools/selftest-caret.ps1`/`docs/HANDOFF-scintilla-uia.md` を**完全撤去**
  - HANDOFF/`説明書/yEdit説明書.md` 更新(P6 で新アーキテクチャの記述に差替)
  - リリース CI 動作確認(P6 でネイティブ DLL は既に消えている=zip 動作確認のみ)
  - **DoD**: 実機 OK + 撤去後全緑 + リリース zip 起動確認 → main へ no-ff マージ

---

**関連**:
- 設計書: `docs/plans/2026-07-05-custom-editcontrol-design.md`(§3 P6 スコープ)
- P5 実装計画: `docs/plans/2026-07-06-p5-uia-connect.md`
- P5 実機チェックリスト: `docs/plans/2026-07-06-p5-uia-checklist.md`
- P6 事前調査:本セッション内の Explore 結果(App 層 Scintilla 参照全リスト+ EditorControl API ギャップ 10 項目)

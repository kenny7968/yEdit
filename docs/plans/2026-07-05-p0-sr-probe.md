# P0: SRプローブ復元・拡張 実装計画

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 削除済み UiaProbe を復元・拡張し、自作UIAテキストコントロールを NVDA/PC-Talker/ナレーターの実機で広範検証して、自作エディットコントロール路線の Go/No-Go を判定する。

**Architecture:** `757613b^` から UiaProbe(完全自作描画+UIA TextPattern、IME/両SR読み実機実証済み)を復元し、現行 `IUiaTextHost`(Name/AutomationId 追加済み)へ適合させる。SayAll 検証用にコンテンツを拡充し、SR非依存スクリプト(verify-uia/walk-test)で回帰確認後、ユーザー実機検証へ。設計書: `docs/plans/2026-07-05-custom-editcontrol-design.md`(P0節)。

**Tech Stack:** C# / .NET 9 / WinForms / UIAutomationProvider(UseWPF)/ PowerShell 5.1(検証スクリプト・ASCIIのみ)

**前提:** 作業は worktree `<repo>\.worktrees\custom-editcontrol-design`(ブランチ `feature/custom-editcontrol-design`)。P1以降の実装計画は本フェーズの Go 判定後に別途作成する。

---

### Task 1: UiaProbe の復元とソリューション登録

**Files:**
- Restore: `src/yEdit.UiaProbe/`(5ファイル: UiaTextControl.cs / MainForm.cs / NativeMethods.cs / Program.cs / yEdit.UiaProbe.csproj)
- Modify: `yEdit.sln`

**Step 1: git 履歴から復元**

```bash
git checkout 757613b^ -- src/yEdit.UiaProbe/
```

**Step 2: ソリューションに再登録**

```bash
dotnet sln yEdit.sln add src/yEdit.UiaProbe/yEdit.UiaProbe.csproj
```

**Step 3: ビルドして想定コンパイルエラーを確認**

Run: `dotnet build src/yEdit.UiaProbe -c Debug`
Expected: **FAIL**。エラーは以下の2種のみであること(それ以外が出たら調査):
- `CS0103: 'UiaDiag' が存在しません` — `UiaTextControl.cs:360` と `MainForm.cs:30`(UiaDiag は `8288cc0` で Accessibility から削除済み)
- `CS0535: 'IUiaTextHost.Name' / 'IUiaTextHost.AutomationId' を実装していません` — 両メンバは probe 削除後の `b3088d1` で追加されたため

**Step 4: コミット(復元スナップショットを固定)**

```bash
git add src/yEdit.UiaProbe yEdit.sln
git commit -m "P0: UiaProbe を 757613b^ から復元しソリューションへ再登録(この時点では未ビルド)"
```

---

### Task 2: 現行 IUiaTextHost への適合(コンパイル修正)

**Files:**
- Modify: `src/yEdit.UiaProbe/UiaTextControl.cs:360`(UiaDiag 除去)・`:471` 付近(Name/AutomationId 追加)
- Modify: `src/yEdit.UiaProbe/MainForm.cs:30`(UiaDiag.Sink 除去)・`:150-154`(TraceAppend 除去)

**Step 1: UiaTextControl.cs の UiaDiag.Log を Log デリゲートへ置換**

置換前(360行):
```csharp
        UiaDiag.Log($"[sysCaret] offset={_caret} px=({p.X},{p.Y}) setOk={ok} tid={Environment.CurrentManagedThreadId}");
```
置換後:
```csharp
        Log?.Invoke($"[sysCaret] offset={_caret} px=({p.X},{p.Y}) setOk={ok} tid={Environment.CurrentManagedThreadId}");
```
(PositionCaret は UI スレッドからのみ呼ばれるため Log ハンドラ直呼びで安全)

**Step 2: IUiaTextHost.Name / AutomationId を実装**

`int IUiaTextHost.ControlTypeId => _controlTypeId;`(471行)の直後に追加:
```csharp
    // SR がフォーカス時に読む名前。プローブであることが分かる固定文言。
    string IUiaTextHost.Name => "プローブ本文";

    // 検証スクリプト(tools/verify-uia.ps1, walk-test.ps1)が FindFirst で使う識別子。変更禁止。
    string IUiaTextHost.AutomationId => "uiaProbeDocument";
```

**Step 3: MainForm.cs の UiaDiag 配線と TraceAppend を削除**

- 30行 `UiaDiag.Sink = TraceAppend;` の行を削除(直前のログファイル初期化は残す)
- 150-154行の `TraceAppend` メソッドを削除(`WriteLogLine` は `AppendLog` が使うため残す)

**Step 4: ビルドで 0 警告を確認**

Run: `dotnet build src/yEdit.UiaProbe -c Debug`
Expected: `0 個の警告` / `0 エラー`

**Step 5: ソリューション全体のビルドと Core テストの無回帰確認**

Run: `dotnet build yEdit.sln -c Release && dotnet test tests/yEdit.Core.Tests -c Release --nologo`
Expected: build 0警告 / テスト 289 件全緑(P0 は本番コードに一切触れないため)

**Step 6: コミット**

```bash
git add src/yEdit.UiaProbe
git commit -m "P0: UiaProbe を現行 IUiaTextHost へ適合(UiaDiag除去・Name/AutomationId実装)"
```

---

### Task 3: SayAll・実機検証用コンテンツの拡充

**Files:**
- Modify: `src/yEdit.UiaProbe/MainForm.cs:34-42`(SetInitialText)

**Step 1: 初期テキストを拡充**

**制約: 先頭6行は検証スクリプトが依存するため変更禁止**(walk-test.ps1 は先頭6文字 `こ,れ,は,空白,U,I` を、verify-uia.ps1 は空行 line[2] と "UIA" 含有を期待)。既存の5行目(「二行目の日本語テキスト…」)と末尾行の間に連続読み用の段落を挿入する:

```csharp
        _editor.SetInitialText(string.Join("\n", new[]
        {
            "これは UIA テキストプロバイダの実験です。",
            "次の行は空行（改行だけの行）です。",
            "",
            "ABC abc 123 半角と　全角　スペースの混在。",
            "二行目の日本語テキスト。Hello, world!",
            // ---- ここから SayAll(連続読み)・実機検証用の追加コンテンツ ----
            "第一段落。スクリーンリーダーの連続読み（読み上げ続け）を検証するための、ある程度の長さを持つ日本語の文章です。",
            "文章は複数の文で構成され、句読点、括弧（かっこ）、「かぎ括弧」、および英単語 accessibility を含みます。",
            "",
            "第二段落。数字 12345 と記号 !#$% と全角記号 ！＃＄％ の読み方を確認します。",
            "サロゲートペアの絵文字 😀 と結合文字を含む行です。",
            "TheQuickBrownFoxJumpsOverTheLazyDog という長い英単語の単語区切り移動（Ctrl+左右）を確認します。",
            "単語 移動 は 空白 区切り の 日本語 でも 確認 します。",
            "",
            "",
            "上は連続した空行2行です。空行の読み（NVDA=ブランク／PC-Talker=無音想定）を確認します。",
            "この行はウィンドウ幅より長くなることを意図した非常に長い行です。折り返しのないプローブで水平方向の読み上げと表示がどうなるかを確認するために、意図的に文章を長く長く続けています。",
            "最終段落。ここまで連続読みが中断なく到達すれば SayAll は合格です。",
            "末尾行（改行なし）。",
        }));
```

**Step 2: ビルド+起動スモーク**

Run: `dotnet build src/yEdit.UiaProbe -c Debug` → 0警告
起動: exe を手元で起動しウィンドウ表示・全行描画・矢印移動を目視確認して閉じる(自動化不要)

**Step 3: コミット**

```bash
git add src/yEdit.UiaProbe/MainForm.cs
git commit -m "P0: SayAll・単語ナビ・絵文字・連続空行の実機検証用コンテンツを追加"
```

---

### Task 4: SR非依存検証スクリプトのワークツリー対応と回帰実行

**Files:**
- Modify: `tools/verify-uia.ps1:6`・`tools/walk-test.ps1:9`(exe パスをパラメータ化)

**Step 1: exe パスをパラメータ化**

両スクリプトの冒頭 `$exe = "<repo>\src\..."` 行を次の形へ(スクリプトは ASCII のみ維持。PS 5.1 の BOM 無し UTF-8 誤読対策):
```powershell
param([string]$Exe = "<repo>\src\yEdit.UiaProbe\bin\Debug\net9.0-windows\yEdit.UiaProbe.exe")
$exe = $Exe
```
(verify-uia.ps1 は `param` を先頭行へ移動。既定値は従来パスのまま=既存運用と互換)

**Step 2: ワークツリーのビルドに対して実行**

Run(ワークツリー直下):
```powershell
& tools\verify-uia.ps1 -Exe "$PWD\src\yEdit.UiaProbe\bin\Debug\net9.0-windows\yEdit.UiaProbe.exe"
& tools\walk-test.ps1  -Exe "$PWD\src\yEdit.UiaProbe\bin\Debug\net9.0-windows\yEdit.UiaProbe.exe"
```
Expected:
- verify-uia: 全行 `[PASS]`(要素発見 / TextPattern / DocumentRange 読取 / 多バイト選択往復 / 空行 line[2] len=0)
- walk-test: `こ,れ,は,(空白),U,I` の6種別 = **Move スパン保持の無回帰**(PC-Talker 致命バグ対策)

FAIL が出た場合はここで止め、原因を特定してから進む(コンテンツ拡充が先頭6行を壊していないかをまず疑う)。

**Step 3: コミット**

```bash
git add tools/verify-uia.ps1 tools/walk-test.ps1
git commit -m "P0: 検証スクリプトのexeパスをパラメータ化しワークツリーから実行可能に"
```

---

### Task 5: 実機検証チェックリストの作成

**Files:**
- Create: `docs/plans/2026-07-05-p0-sr-probe-checklist.md`

**Step 1: チェックリスト文書を作成**

```markdown
# P0 SRプローブ 実機検証チェックリスト

対象: yEdit.UiaProbe(自作UIAテキストコントロール)/ 判定者: ユーザー(実機)
起動: `dotnet run --project src/yEdit.UiaProbe -c Debug`(ワークツリー直下)
記録: 各項目に OK / NG / 備考。NG時は可能なら「無音/誤読/遅延」の別と操作手順を記録。

## 検証マトリクス(SRごとに全項目)

対象SR: (1) NVDA (2) PC-Talker (3) ナレーター ※(3)は参考(汎用UIA経路の代表)

| # | 項目 | 操作 | 期待 | NVDA | PC-Talker | ナレーター |
|---|---|---|---|---|---|---|
| 1 | 文字ナビ | ←→で1文字ずつ | 通過文字を読む(日本語/英数/絵文字) | | | |
| 2 | 行ナビ | ↑↓で1行ずつ | 着地行を読む | | | |
| 3 | 空行 | ↑↓で空行に着地 | NVDA=「ブランク」/PC-Talker=無音でも可(能動発声は本番App層の担当・§4.1) | | | |
| 4 | 連続空行 | 空行2連続を通過 | 各行で挙動一貫 | | | |
| 5 | 単語ナビ | Ctrl+←→ | 単語単位で読む | | | |
| 6 | 選択読み | Shift+←→/Shift+↑↓ | 選択増減分を読む | | | |
| 7 | 全選択相当 | Shift+End 等で行内選択 | 選択内容を読む | | | |
| 8 | 連続読み(SayAll) | NVDA: NVDA+↓ / 各SRの読み上げ続け操作 | 最終段落まで中断なく到達 | | | |
| 9 | タイプエコー | 英字・かなを直接入力 | 入力文字を読む | | | |
| 10 | 削除エコー | BS/Delで削除 | 削除文字(または結果)を読む | | | |
| 11 | IME変換 | ATOKで「にほんご」→変換 | 変換候補の読み・確定文字列の読み | | | |
| 12 | IME候補移動 | 変換中に↑↓/スペースで候補移動 | 候補を読む | | | |
| 13 | フォーカス復帰 | 別窓へAlt+Tabして戻る | 復帰後すぐ現在行/文字を読む(2秒待ちなし) | | | |
| 14 | 長い行 | 長行を↑↓/←→で通過 | 読み・表示に破綻なし | | | |
| 15 | 点字(任意) | 点字ディスプレイ接続時のみ | カーソル位置に追従 | | | |

## ControlType 比較(診断メニュー)

診断メニューで ControlType = Document ⇔ Edit を切替え(切替後は再フォーカス、必要ならSR再起動)、
上記 #1/#2/#8/#9 を両設定で比較。どちらが自然かを記録:

- Document: 
- Edit: 

## Go/No-Go 判定基準(設計書 §4)

- **No-Go(致命)**: いずれかの主要SR(NVDA/PC-Talker)で #1/#2(基本ナビ読み)または #8(SayAll)が不可・実用に耐えない
- **要調整(継続可)**: #3 空行(本番はApp層能動発声で補う)・#9〜12 の癖・記号読みの差異 → 備考に記録し P5 実装で対処
- **Go**: 主要2SRで #1/#2/#5/#6/#8/#11 が実用水準

## 結果

- 判定: (Go / No-Go / 要調整継続)
- 実施日: 
- SRバージョン: NVDA=       / PC-Talker=       / ナレーター=Windows標準
```

**Step 2: コミット**

```bash
git add docs/plans/2026-07-05-p0-sr-probe-checklist.md
git commit -m "P0: 実機SR検証チェックリスト(Go/No-Go判定基準つき)を追加"
```

---

### Task 6: 最終確認とゲート(ユーザー実機検証待ち)

**Step 1: 全体ビルド+テスト最終確認**

Run: `dotnet build yEdit.sln -c Release && dotnet test tests/yEdit.Core.Tests -c Release --nologo`
Expected: 0警告 / 289件全緑

**Step 2: 別エージェントによるコードレビュー**

復元+修正差分(`757613b^` との差分と新規ファイル)を対象に、レビュー観点: IUiaTextHost 適合の正しさ・検証スクリプト互換(先頭6行/AutomationId)・プローブがプロダクションコードに影響しないこと。

**Step 3: ユーザーへ実機検証を依頼(ここで停止)**

- チェックリストの場所と起動コマンドを提示
- **P0 の Go 判定が出るまで P1 以降の実装計画は作成しない**(ControlType の Document/Edit 決定・NVDA読み品質の知見が P1〜P5 の設計入力になるため)
- Go 判定後: P0 結果を設計書に追記 → P0 成果を main へ no-ff マージ検討 → P1(TextBuffer)実装計画の作成へ

---

## 備考

- プローブは本番コード(yEdit.App/Editor/Core/Accessibility)に一切手を触れない。Accessibility への参照は読み取りのみ
- IME はプローブでは既定のIMEウィンドウ構成(インライン未実装)。P0 で確認するのは「SRがIME変換を読むか」であり、インライン表示の品質は P4 のスコープ
- 点字ディスプレイが手元にない場合、#15 はスキップし P5 実機中間検証の任意項目へ繰り越し

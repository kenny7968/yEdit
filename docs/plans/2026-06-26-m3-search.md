# M3 検索・置換（単一ファイル）実装計画

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 単一ファイル内の検索・置換（リテラル/正規表現・大小・単語・静かな増分カウント・折り返しなし・置換3操作）を、SR（PC-Talker/NVDA）が件数と一致行を読める形で実装する。

**Architecture:** 照合は `yEdit.Core.Search`（純ロジック・.NET Regex・xUnit 対象・grep 再利用）。エディタ操作（選択/置換/スクロール）は `ScintillaHost` に文字オフセット指定ヘルパを追加し、**既存の `Utf16ToByte` 変換＋`SCI_*`（DirectMessage）バイト位置**で行う（ScintillaNET の文字位置 API はサロゲートで .NET string index とズレ得るため使わない）。UI は `yEdit.App` のモードレスダイアログ＋統括 `SearchController`。SR 通知は `AccessibleObject.RaiseAutomationNotification`。

**Tech Stack:** .NET 9 / C# / WinForms / Scintilla5.NET 6.1.2 / `System.Text.RegularExpressions` / xUnit。

設計根拠: `docs/plans/2026-06-26-m3-search-design.md`。

---

## 前提・規約

- 作業ブランチ: `feature/m3-search`（作成済み）。各タスク末にコミット。
- ビルド: `dotnet build yEdit.sln`（**0 警告**を維持）。
- テスト: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`。
- 会話・コミットは日本語。コミット末尾に `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- Core の照合オフセットは **UTF-16 文字位置**（`string` の index と同一空間）。`SearchOptions` は照合条件のみ（置換文字列は置換メソッドへ都度渡す＝grep 再利用のため）。

---

## Task 1: Core — SearchOptions と MatchSpan

**Files:**
- Create: `src/yEdit.Core/Search/SearchOptions.cs`
- Create: `src/yEdit.Core/Search/MatchSpan.cs`

**Step 1: SearchOptions を書く**

```csharp
namespace yEdit.Core.Search;

/// <summary>
/// 検索の照合条件。置換文字列は含めない（照合に専念し grep でも再利用するため、
/// 置換文字列は置換メソッドへ都度渡す）。
/// </summary>
public sealed record SearchOptions(
    string Pattern,
    bool MatchCase = false,
    bool WholeWord = false,
    bool UseRegex = false);
```

**Step 2: MatchSpan を書く**

```csharp
namespace yEdit.Core.Search;

/// <summary>ヒット範囲。オフセットは UTF-16 文字位置（editor の .NET string index と同一空間）。</summary>
public readonly record struct MatchSpan(int Start, int Length)
{
    public int End => Start + Length;
}
```

**Step 3: ビルドして通ることを確認**

Run: `dotnet build src/yEdit.Core/yEdit.Core.csproj`
Expected: Build succeeded, 0 Warning。

**Step 4: コミット**

```bash
git add src/yEdit.Core/Search/SearchOptions.cs src/yEdit.Core/Search/MatchSpan.cs
git commit -m "M3: Core 検索の値型（SearchOptions / MatchSpan）"
```

---

## Task 2: Core — TextSearcher の照合（Count / FindNext / FindPrev）

**Files:**
- Create: `src/yEdit.Core/Search/TextSearcher.cs`
- Test: `tests/yEdit.Core.Tests/Search/TextSearcherTests.cs`

**Step 1: 失敗するテストを書く**

```csharp
using yEdit.Core.Search;

namespace yEdit.Core.Tests.Search;

public class TextSearcherTests
{
    private static TextSearcher S(string pattern, bool matchCase = false, bool wholeWord = false, bool regex = false)
        => new(new SearchOptions(pattern, matchCase, wholeWord, regex));

    [Fact]
    public void Literal_count_counts_all_occurrences()
        => Assert.Equal(3, S("ab").Count("ab_ab_AB")); // 既定は大小無視

    [Fact]
    public void MatchCase_distinguishes_case()
        => Assert.Equal(2, S("ab", matchCase: true).Count("ab_ab_AB"));

    [Fact]
    public void WholeWord_matches_only_whole_words()
        => Assert.Equal(1, S("cat", wholeWord: true).Count("cat category cat-dog").Equals(0) ? -1 : S("cat", wholeWord: true).Count("cat category cat-dog"));

    [Fact]
    public void Regex_metachars_are_literal_when_not_regex()
        => Assert.Equal(1, S("a.c").Count("a.c abc")); // "abc" にはマッチしない

    [Fact]
    public void Regex_mode_uses_dotnet_regex()
        => Assert.Equal(2, S(@"a.c", regex: true).Count("a.c abc")); // "a.c" と "abc"

    [Fact]
    public void FindNext_returns_first_match_at_or_after_from()
    {
        var span = S("ab").FindNext("xxabxxab", 3);
        Assert.Equal(new MatchSpan(6, 2), span);
    }

    [Fact]
    public void FindNext_returns_null_past_end_no_wrap()
        => Assert.Null(S("ab").FindNext("abxx", 3)); // 折り返ししない

    [Fact]
    public void FindPrev_returns_last_match_strictly_before()
    {
        var span = S("ab").FindPrev("abxxab", 6);
        Assert.Equal(new MatchSpan(4, 2), span);
    }

    [Fact]
    public void FindPrev_returns_null_when_none_before()
        => Assert.Null(S("ab").FindPrev("abxx", 0));

    [Fact]
    public void Invalid_regex_is_reported_not_thrown()
    {
        var s = S("(", regex: true);
        Assert.False(s.IsValid);
        Assert.NotNull(s.Error);
        Assert.Equal(0, s.Count("anything")); // 不正時は 0 件・例外なし
    }

    [Fact]
    public void Cjk_offsets_are_utf16_indices()
    {
        var span = S("世界").FindNext("こんにちは世界", 0);
        Assert.Equal(new MatchSpan(5, 2), span); // "こんにちは" は5文字
    }
}
```

> 注: `WholeWord_matches_only_whole_words` は読みづらいので Step 3 実装後に
> `Assert.Equal(1, S("cat", wholeWord: true).Count("cat category cat-dog"));` に整理してよい
> （`cat`＝1件のみ。`category` は単語境界外、`cat-dog` の `cat` はハイフン境界で1件… → 実際は2件）。
> **重要**: `\b` の挙動を実装後に実値で確認し、テストの期待値を実装の正しい出力に合わせること
> （ハイフンは非単語文字なので `cat-dog` の `cat` も単語として1件＝合計2件になる見込み）。
> 期待値は「実装を走らせて確認した正しい値」に確定する。

**Step 2: テストを実行して失敗を確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: コンパイルエラー（`TextSearcher` 未定義）。

**Step 3: TextSearcher を実装**

```csharp
using System.Text;
using System.Text.RegularExpressions;

namespace yEdit.Core.Search;

/// <summary>
/// 単一ファイル検索・置換の照合エンジン。内部を .NET Regex に統一する
/// （リテラルは Regex.Escape、単語単位は \b 境界、大小無視は IgnoreCase）。
/// オフセットは UTF-16 文字位置。正規表現の不正は例外でなく IsValid/Error で返す。
/// </summary>
public sealed class TextSearcher
{
    private readonly Regex? _regex;
    private readonly bool _expand; // 正規表現モードのみ置換文字列の $ を展開

    public bool IsValid => _regex is not null;
    public string? Error { get; }

    public TextSearcher(SearchOptions options)
    {
        _expand = options.UseRegex;
        if (string.IsNullOrEmpty(options.Pattern))
        {
            Error = "検索文字列が空です。";
            return;
        }
        string body = options.UseRegex ? options.Pattern : Regex.Escape(options.Pattern);
        if (options.WholeWord) body = $@"\b(?:{body})\b";
        var opts = RegexOptions.CultureInvariant;
        if (!options.MatchCase) opts |= RegexOptions.IgnoreCase;
        try { _regex = new Regex(body, opts); }
        catch (ArgumentException ex) { Error = ex.Message; }
    }

    public int Count(string text) => _regex is null ? 0 : _regex.Matches(text).Count;

    /// <summary>from 以降で最初のヒット（折り返しなし）。</summary>
    public MatchSpan? FindNext(string text, int from)
    {
        if (_regex is null) return null;
        if (from < 0) from = 0;
        if (from > text.Length) return null;
        var m = _regex.Match(text, from);
        return m.Success ? new MatchSpan(m.Index, m.Length) : null;
    }

    /// <summary>before より厳密に前で最後のヒット（折り返しなし）。</summary>
    public MatchSpan? FindPrev(string text, int before)
    {
        if (_regex is null) return null;
        MatchSpan? last = null;
        foreach (Match m in _regex.Matches(text))
        {
            if (m.Index >= before) break;
            last = new MatchSpan(m.Index, m.Length);
        }
        return last;
    }
}
```

**Step 4: テストを実行して通ることを確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: PASS（必要なら単語境界テストの期待値を実値に確定）。

**Step 5: コミット**

```bash
git add src/yEdit.Core/Search/TextSearcher.cs tests/yEdit.Core.Tests/Search/TextSearcherTests.cs
git commit -m "M3: Core 照合エンジン（Count/FindNext/FindPrev・正規表現/大小/単語/折り返しなし）"
```

---

## Task 3: Core — Locate と置換（ReplacementAt / ReplaceInRange / ReplaceAll）

**Files:**
- Modify: `src/yEdit.Core/Search/TextSearcher.cs`
- Test: `tests/yEdit.Core.Tests/Search/TextSearcherTests.cs`

**Step 1: 失敗するテストを追加**

```csharp
    [Fact]
    public void Locate_returns_1based_ordinal_and_total()
    {
        var s = S("ab");
        Assert.Equal((2, 3), s.Locate("ab_ab_ab", new MatchSpan(3, 2)));
    }

    [Fact]
    public void Locate_returns_null_when_span_is_not_a_match()
        => Assert.Null(S("ab").Locate("ab_ab", new MatchSpan(1, 2)));

    [Fact]
    public void ReplacementAt_literal_inserts_verbatim()
        => Assert.Equal("X$1", S("ab").ReplacementAt("ab", new MatchSpan(0, 2), "X$1")); // リテラルは $ を展開しない

    [Fact]
    public void ReplacementAt_regex_expands_groups()
        => Assert.Equal("ba", S(@"(a)(b)", regex: true).ReplacementAt("ab", new MatchSpan(0, 2), "$2$1"));

    [Fact]
    public void ReplacementAt_returns_null_when_span_not_match()
        => Assert.Null(S("ab").ReplacementAt("xab", new MatchSpan(0, 2), "X"));

    [Fact]
    public void ReplaceAll_replaces_all_and_counts()
    {
        var (frag, n) = S("ab").ReplaceAll("ab_ab_ab", "X");
        Assert.Equal("X_X_X", frag);
        Assert.Equal(3, n);
    }

    [Fact]
    public void ReplaceInRange_only_within_range_fully_contained()
    {
        // text="ab ab ab"、範囲[3,5)=2件目の "ab" のみ（3件目は範囲外）
        var (frag, n) = S("ab").ReplaceInRange("ab ab ab", 3, 2, "X");
        Assert.Equal("X", frag); // 返すのは [3,5) を置換した断片
        Assert.Equal(1, n);
    }

    [Fact]
    public void ReplaceInRange_excludes_match_crossing_boundary()
    {
        // 範囲[0,1) は "ab" を途中で切るので 0 件
        var (frag, n) = S("ab").ReplaceInRange("abab", 0, 1, "X");
        Assert.Equal("a", frag);
        Assert.Equal(0, n);
    }
```

**Step 2: テストを実行して失敗を確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: コンパイルエラー（メソッド未定義）。

**Step 3: TextSearcher にメソッドを追加**

```csharp
    /// <summary>span を全ヒット中の何件目か（1始まり, total）。span がヒットでなければ null。</summary>
    public (int Ordinal, int Total)? Locate(string text, MatchSpan span)
    {
        if (_regex is null) return null;
        int ordinal = 0, total = 0; bool found = false;
        foreach (Match m in _regex.Matches(text))
        {
            total++;
            if (m.Index == span.Start && m.Length == span.Length) { ordinal = total; found = true; }
        }
        return found ? (ordinal, total) : null;
    }

    /// <summary>span が実際のヒットなら置換文字列を返す（正規表現は $1 等展開・リテラルは素のまま）。違えば null。</summary>
    public string? ReplacementAt(string text, MatchSpan span, string replacement)
    {
        if (_regex is null) return null;
        if (span.Start < 0 || span.Start > text.Length) return null;
        var m = _regex.Match(text, span.Start);
        if (!m.Success || m.Index != span.Start || m.Length != span.Length) return null;
        return Expand(m, replacement);
    }

    /// <summary>全文置換。返すのは置換後の全文（=新断片）と件数。</summary>
    public (string Fragment, int Count) ReplaceAll(string text, string replacement)
        => ReplaceInRange(text, 0, text.Length, replacement);

    /// <summary>
    /// [start, start+length) に完全に収まるヒットだけ置換し、その範囲の置換後断片と件数を返す。
    /// 範囲外・境界をまたぐヒットは対象外。エディタはこの断片で当該文字範囲を差し替える。
    /// </summary>
    public (string Fragment, int Count) ReplaceInRange(string text, int start, int length, string replacement)
    {
        int s = Math.Clamp(start, 0, text.Length);
        int end = Math.Clamp(start + length, s, text.Length);
        if (_regex is null) return (text.Substring(s, end - s), 0);

        var sb = new StringBuilder();
        int count = 0, pos = s;
        foreach (Match m in _regex.Matches(text))
        {
            if (m.Index < s) continue;
            if (m.Index + m.Length > end) break;
            sb.Append(text, pos, m.Index - pos);
            sb.Append(Expand(m, replacement));
            pos = m.Index + m.Length;
            count++;
        }
        sb.Append(text, pos, end - pos);
        return (sb.ToString(), count);
    }

    private string Expand(Match m, string replacement) => _expand ? m.Result(replacement) : replacement;
```

**Step 4: テストを実行して通ることを確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: PASS（全テスト）。

**Step 5: コミット**

```bash
git add src/yEdit.Core/Search/TextSearcher.cs tests/yEdit.Core.Tests/Search/TextSearcherTests.cs
git commit -m "M3: Core 置換と件数特定（ReplacementAt/ReplaceInRange/ReplaceAll/Locate）"
```

---

## Task 4: Editor — Sci 定数追加と ScintillaHost の検索ヘルパ

WinForms/ネイティブ依存のため単体テストは付けない（ビルド0警告＋後続の手動/実機検証で担保）。位置変換は既存の `Utf16ToByte`/`Clamp16` を再利用し、ScintillaNET の文字位置 API は使わない（サロゲートでのズレ回避）。

**Files:**
- Modify: `src/yEdit.Editor/Sci.cs`
- Modify: `src/yEdit.Editor/ScintillaHost.cs`

**Step 1: Sci.cs に定数を追加**

`SCI_SETSEL = 2160;` の近くに追記:

```csharp
    public const int SCI_SCROLLCARET = 2169;        // キャレットを可視範囲へスクロール
    public const int SCI_SETTARGETSTART = 2190;     // 置換ターゲット開始（バイト）
    public const int SCI_SETTARGETEND = 2192;       // 置換ターゲット終了（バイト）
    public const int SCI_REPLACETARGET = 2194;      // (length, char*) ターゲットを置換（UTF-8・1アンドゥ）
```

**Step 2: ScintillaHost.cs に公開ヘルパを追加**

`IUiaTextHost` 実装群の手前（`// ==================== IUiaTextHost` の直前あたり）に、検索用の公開 API としてまとめて追加:

```csharp
    // ==================== 検索・置換ヘルパ（UI スレッド・文字オフセット） ====================
    // Core は UTF-16 文字オフセットで照合する。ここで既存の Utf16ToByte を介し Scintilla の
    // バイト位置へ変換して選択/置換する（ScintillaNET の文字位置 API はサロゲートでズレ得るため不使用）。

    /// <summary>照合対象テキスト（UI スレッドで保持する UTF-16 スナップショット）。</summary>
    public string SnapshotText => _snapshot;

    /// <summary>現在の選択範囲（UTF-16 文字オフセット, Start&lt;=End）。</summary>
    public (int Start, int End) GetSelectionCharRange()
    {
        int s = _selStart, e = _selEnd;
        return (Math.Min(s, e), Math.Max(s, e));
    }

    /// <summary>文字オフセット範囲を選択しキャレットを可視化する（選択移動で SR が一致行を読む）。</summary>
    public void SelectCharRange(int start, int length)
    {
        if (InvokeRequired) { BeginInvoke(new Action(() => SelectCharRange(start, length))); return; }
        int bs = Utf16ToByte(Clamp16(start));
        int be = Utf16ToByte(Clamp16(start + length));
        DirectMessage(Sci.SCI_SETSEL, (nint)bs, (nint)be);
        DirectMessage(Sci.SCI_SCROLLCARET);
        RefreshSelection();
    }

    /// <summary>文字オフセット範囲を replacement で置換する（SCI_REPLACETARGET = 1 アンドゥ）。</summary>
    public void ReplaceCharRange(int start, int length, string replacement)
    {
        if (InvokeRequired) { BeginInvoke(new Action(() => ReplaceCharRange(start, length, replacement))); return; }
        int bs = Utf16ToByte(Clamp16(start));
        int be = Utf16ToByte(Clamp16(start + length));
        DirectMessage(Sci.SCI_SETTARGETSTART, (nint)bs);
        DirectMessage(Sci.SCI_SETTARGETEND, (nint)be);
        byte[] repl = Encoding.UTF8.GetBytes(replacement);
        nint buf = Marshal.AllocHGlobal(repl.Length + 1);
        try
        {
            if (repl.Length > 0) Marshal.Copy(repl, 0, buf, repl.Length);
            Marshal.WriteByte(buf, repl.Length, 0);
            DirectMessage(Sci.SCI_REPLACETARGET, (nint)repl.Length, buf);
        }
        finally { Marshal.FreeHGlobal(buf); }
        RefreshSnapshot();
        RefreshSelection();
    }
```

**Step 3: ビルドして 0 警告を確認**

Run: `dotnet build src/yEdit.Editor/yEdit.Editor.csproj`
Expected: Build succeeded, 0 Warning。

**Step 4: コミット**

```bash
git add src/yEdit.Editor/Sci.cs src/yEdit.Editor/ScintillaHost.cs
git commit -m "M3: Editor に文字オフセット指定の選択/置換ヘルパとスナップショット公開を追加"
```

---

## Task 5: App — FindReplaceDialog（検索モード）と SearchController（検索系）

モードレスダイアログと統括クラスを新設し、まず**検索（次/前/増分カウント/通知）**を通す。置換は Task 6。

**Files:**
- Create: `src/yEdit.App/SearchController.cs`
- Create: `src/yEdit.App/FindReplaceDialog.cs`

**Step 1: SearchController（検索系）を書く**

```csharp
using System.Windows.Forms.Automation;
using yEdit.Core.Search;
using yEdit.Editor;

namespace yEdit.App;

/// <summary>
/// 検索・置換の統括。Core 照合と ScintillaHost の選択/置換を仲介し、結果を
/// ダイアログのステータス＋UIA 通知で SR に伝える。対象はアクティブ文書を毎回解決する。
/// </summary>
public sealed class SearchController
{
    private readonly DocumentManager _docs;
    private readonly Form _owner;
    private FindReplaceDialog? _dialog;

    public SearchController(DocumentManager docs, Form owner)
    {
        _docs = docs;
        _owner = owner;
    }

    private ScintillaHost? ActiveEditor => _docs.Active?.Editor;

    public void OpenFind() => Open(replaceMode: false);
    public void OpenReplace() => Open(replaceMode: true);

    private void Open(bool replaceMode)
    {
        _dialog ??= new FindReplaceDialog(this);
        _dialog.SetMode(replaceMode);
        if (!_dialog.Visible) _dialog.Show(_owner);
        _dialog.Activate();
        _dialog.FocusPattern();
        UpdateCount(); // 開いた時点の件数を表示
    }

    private SearchOptions? CurrentOptions()
    {
        var d = _dialog;
        if (d is null || string.IsNullOrEmpty(d.Pattern)) return null;
        return new SearchOptions(d.Pattern, d.MatchCase, d.WholeWord, d.UseRegex);
    }

    /// <summary>増分カウント（移動しない）。正規表現エラーはステータスのみ更新（通知はしない）。</summary>
    public void UpdateCount()
    {
        var d = _dialog;
        if (d is null) return;
        var opts = CurrentOptions();
        if (opts is null) { d.SetStatus(""); return; }
        var searcher = new TextSearcher(opts);
        if (!searcher.IsValid) { d.SetStatus("正規表現が正しくありません"); return; }
        string text = ActiveEditor?.SnapshotText ?? "";
        int n = searcher.Count(text);
        d.SetStatus(n == 0 ? "見つかりません" : $"{n} 件");
    }

    public void FindNext() => Find(forward: true);
    public void FindPrev() => Find(forward: false);

    private void Find(bool forward)
    {
        var ed = ActiveEditor;
        var opts = CurrentOptions();
        if (ed is null || opts is null) return;
        var searcher = new TextSearcher(opts);
        if (!searcher.IsValid) { Announce("正規表現が正しくありません"); return; }

        string text = ed.SnapshotText;
        var (selStart, selEnd) = ed.GetSelectionCharRange();
        MatchSpan? hit = forward
            ? searcher.FindNext(text, selEnd > selStart ? selEnd : selStart) // 選択末尾の次から
            : searcher.FindPrev(text, selStart);

        if (hit is null) { Announce("これ以上見つかりません"); return; }

        ed.SelectCharRange(hit.Value.Start, hit.Value.Length);
        var loc = searcher.Locate(text, hit.Value);
        Announce(loc is { } l ? $"{l.Total} 件中 {l.Ordinal} 件目" : "");
        _dialog?.SetStatus(loc is { } l2 ? $"{l2.Total} 件中 {l2.Ordinal} 件目" : "");
    }

    /// <summary>ステータス Label 経由で SR にライブ通知（NVDA 対応厚・PC-Talker は実機確認）。</summary>
    internal void Announce(string message)
    {
        _dialog?.RaiseNotification(message);
    }

    internal ScintillaHost? Target => ActiveEditor;
}
```

> 注: `Find` の起点は「選択末尾の次から（前方）/ 選択頭から（後方）」。現在ヒットを選択中に
> もう一度 F3 で**次**へ進めるよう、前方は `selEnd` を起点にする。`FindNext(text, selEnd)` が
> 同位置を返さないのは `Match(text, selEnd)` が selEnd 以降を探すため。

**Step 2: FindReplaceDialog を書く（検索モードで動く最小形）**

```csharp
using System.Windows.Forms.Automation;

namespace yEdit.App;

/// <summary>
/// モードレスの検索・置換ダイアログ。入力収集とステータス表示に徹し、操作は
/// SearchController 経由。検索モード/置換モードでフィールド表示を切替える。
/// </summary>
public sealed class FindReplaceDialog : Form
{
    private readonly SearchController _controller;

    private readonly TextBox _pattern = new();
    private readonly TextBox _replacement = new();
    private readonly Label _replacementLabel = new() { Text = "置換後の文字列(&R):", AutoSize = true };
    private readonly CheckBox _matchCase = new() { Text = "大文字と小文字を区別(&C)", AutoSize = true };
    private readonly CheckBox _wholeWord = new() { Text = "単語単位(&W)", AutoSize = true };
    private readonly CheckBox _useRegex = new() { Text = "正規表現(&E)", AutoSize = true };
    private readonly CheckBox _inSelection = new() { Text = "選択範囲のみ(&S)", AutoSize = true };
    private readonly Button _next = new() { Text = "次を検索(&N)", AutoSize = true };
    private readonly Button _prev = new() { Text = "前を検索(&P)", AutoSize = true };
    private readonly Button _replaceOne = new() { Text = "置換して次を検索(&L)", AutoSize = true };
    private readonly Button _replaceAll = new() { Text = "すべて置換(&A)", AutoSize = true };
    private readonly Button _close = new() { Text = "閉じる", AutoSize = true };
    private readonly Label _status = new() { AutoSize = true, Text = "" };

    public FindReplaceDialog(SearchController controller)
    {
        _controller = controller;
        Text = "検索";
        FormBorderStyle = FormBorderStyle.FixedToolWindow; // モードレスツールウィンドウ
        StartPosition = FormStartPositionCenterParentSafe();
        ShowInTaskbar = false;
        KeyPreview = true;
        AutoSize = true;
        AutoSizeMode = AutoSizeMode.GrowAndShrink;

        BuildLayout();

        _next.Click += (_, _) => _controller.FindNext();
        _prev.Click += (_, _) => _controller.FindPrev();
        _replaceOne.Click += (_, _) => _controller.ReplaceOne();
        _replaceAll.Click += (_, _) => _controller.ReplaceAll();
        _close.Click += (_, _) => Hide();
        _pattern.TextChanged += (_, _) => _controller.UpdateCount();
        _matchCase.CheckedChanged += (_, _) => _controller.UpdateCount();
        _wholeWord.CheckedChanged += (_, _) => _controller.UpdateCount();
        _useRegex.CheckedChanged += (_, _) => _controller.UpdateCount();
    }

    // 入力アクセサ（SearchController が参照）
    public string Pattern => _pattern.Text;
    public string Replacement => _replacement.Text;
    public bool MatchCase => _matchCase.Checked;
    public bool WholeWord => _wholeWord.Checked;
    public bool UseRegex => _useRegex.Checked;
    public bool InSelection => _inSelection.Checked;

    public void FocusPattern() { _pattern.Focus(); _pattern.SelectAll(); }

    public void SetMode(bool replaceMode)
    {
        Text = replaceMode ? "置換" : "検索";
        _replacementLabel.Visible = replaceMode;
        _replacement.Visible = replaceMode;
        _replaceOne.Visible = replaceMode;
        _replaceAll.Visible = replaceMode;
        _inSelection.Visible = replaceMode;
    }

    public void SetStatus(string text) => _status.Text = text;

    /// <summary>ステータス Label の UIA プロバイダから通知を上げて SR に読ませる。</summary>
    public void RaiseNotification(string message)
    {
        if (string.IsNullOrEmpty(message)) return;
        SetStatus(message);
        try
        {
            _status.AccessibilityObject.RaiseAutomationNotification(
                AutomationNotificationKind.ActionCompleted,
                AutomationNotificationProcessing.MostRecent,
                message);
        }
        catch { /* 通知非対応環境では視覚表示＋選択移動の自動読みにフォールバック */ }
    }

    protected override bool ProcessCmdKey(ref Message msg, Keys keyData)
    {
        switch (keyData)
        {
            case Keys.Escape: Hide(); return true;
            case Keys.F3: _controller.FindNext(); return true;
            case Keys.Shift | Keys.F3: _controller.FindPrev(); return true;
            case Keys.Enter when _pattern.Focused: _controller.FindNext(); return true;
        }
        return base.ProcessCmdKey(ref msg, keyData);
    }

    // 閉じる(×)で破棄せず隠す（再利用）。
    protected override void OnFormClosing(FormClosingEventArgs e)
    {
        if (e.CloseReason == CloseReason.UserClosing)
        {
            e.Cancel = true;
            Hide();
            return;
        }
        base.OnFormClosing(e);
    }

    private void BuildLayout()
    {
        // TableLayoutPanel で段組み。簡潔さ優先（見た目の精緻化は M7）。
        var root = new TableLayoutPanel { Dock = DockStyle.Fill, AutoSize = true, ColumnCount = 2, Padding = new Padding(8) };
        var patternLabel = new Label { Text = "検索する文字列(&F):", AutoSize = true };
        root.Controls.Add(patternLabel, 0, 0);
        _pattern.Width = 260; root.Controls.Add(_pattern, 1, 0);
        root.Controls.Add(_replacementLabel, 0, 1);
        _replacement.Width = 260; root.Controls.Add(_replacement, 1, 1);

        var opts = new FlowLayoutPanel { AutoSize = true, FlowDirection = FlowDirection.LeftToRight };
        opts.Controls.AddRange(new Control[] { _matchCase, _wholeWord, _useRegex, _inSelection });
        root.Controls.Add(opts, 0, 2); root.SetColumnSpan(opts, 2);

        var buttons = new FlowLayoutPanel { AutoSize = true, FlowDirection = FlowDirection.LeftToRight };
        buttons.Controls.AddRange(new Control[] { _next, _prev, _replaceOne, _replaceAll, _close });
        root.Controls.Add(buttons, 0, 3); root.SetColumnSpan(buttons, 2);

        root.Controls.Add(_status, 0, 4); root.SetColumnSpan(_status, 2);
        _status.AccessibleName = "検索結果";

        Controls.Add(root);
        AcceptButton = _next;
    }

    private static FormStartPosition FormStartPositionCenterParentSafe() => FormStartPosition.CenterParent;
}
```

> 注: `ReplaceOne` / `ReplaceAll` は Task 6 で `SearchController` に実装する。Task 5 時点では
> 検索モードのみ動けばよいので、ビルドを通すため `SearchController` に**空のスタブ**
> `public void ReplaceOne() {}` `public void ReplaceAll() {}` を置いておき、Task 6 で中身を実装する。

**Step 3: SearchController に置換スタブを追加（ビルド用）**

`Find` の下に:

```csharp
    public void ReplaceOne() { /* Task 6 */ }
    public void ReplaceAll() { /* Task 6 */ }
```

**Step 4: ビルドして 0 警告を確認**

Run: `dotnet build yEdit.sln`
Expected: Build succeeded, 0 Warning（`MainForm` 未配線でもビルドは通る）。

**Step 5: コミット**

```bash
git add src/yEdit.App/SearchController.cs src/yEdit.App/FindReplaceDialog.cs
git commit -m "M3: 検索ダイアログ（モードレス）と統括クラス・検索系（次/前/増分カウント/UIA通知）"
```

---

## Task 6: App — 置換（置換して次を検索 / すべて置換 / 選択範囲のみ）

**Files:**
- Modify: `src/yEdit.App/SearchController.cs`

**Step 1: スタブを実装に置き換える**

```csharp
    /// <summary>現在の選択が今のヒットなら置換し、次へ。違えばまず次を検索（標準の置換動作）。</summary>
    public void ReplaceOne()
    {
        var ed = ActiveEditor;
        var opts = CurrentOptions();
        var d = _dialog;
        if (ed is null || opts is null || d is null) return;
        var searcher = new TextSearcher(opts);
        if (!searcher.IsValid) { Announce("正規表現が正しくありません"); return; }

        string text = ed.SnapshotText;
        var (selStart, selEnd) = ed.GetSelectionCharRange();
        var span = new MatchSpan(selStart, selEnd - selStart);
        string? repl = selEnd > selStart ? searcher.ReplacementAt(text, span, d.Replacement) : null;

        if (repl is null)
        {
            FindNext(); // まだヒットを選択していない → 次を検索（置換は次回）
            return;
        }
        ed.ReplaceCharRange(span.Start, span.Length, repl);
        // 置換後の本文で、挿入文字列の直後から次を検索
        var ed2Text = ed.SnapshotText;
        var next = searcher.FindNext(ed2Text, span.Start + repl.Length);
        if (next is null) { Announce("置換しました。これ以上見つかりません"); d.SetStatus("これ以上見つかりません"); return; }
        ed.SelectCharRange(next.Value.Start, next.Value.Length);
        var loc = searcher.Locate(ed2Text, next.Value);
        Announce(loc is { } l ? $"置換しました。{l.Total} 件中 {l.Ordinal} 件目" : "置換しました");
    }

    /// <summary>全文（または選択範囲のみ）を一括置換し件数を通知する。</summary>
    public void ReplaceAll()
    {
        var ed = ActiveEditor;
        var opts = CurrentOptions();
        var d = _dialog;
        if (ed is null || opts is null || d is null) return;
        var searcher = new TextSearcher(opts);
        if (!searcher.IsValid) { Announce("正規表現が正しくありません"); return; }

        string text = ed.SnapshotText;
        int rangeStart, rangeLen;
        if (d.InSelection)
        {
            var (s, e) = ed.GetSelectionCharRange();
            if (e <= s) { Announce("選択範囲がありません"); d.SetStatus("選択範囲がありません"); return; }
            rangeStart = s; rangeLen = e - s;
        }
        else { rangeStart = 0; rangeLen = text.Length; }

        var (fragment, count) = searcher.ReplaceInRange(text, rangeStart, rangeLen, d.Replacement);
        if (count == 0) { Announce("見つかりません"); d.SetStatus("見つかりません"); return; }
        ed.ReplaceCharRange(rangeStart, rangeLen, fragment);
        Announce($"{count} 件置換しました");
        d.SetStatus($"{count} 件置換しました");
    }
```

**Step 2: ビルドして 0 警告を確認**

Run: `dotnet build yEdit.sln`
Expected: Build succeeded, 0 Warning。

**Step 3: コミット**

```bash
git add src/yEdit.App/SearchController.cs
git commit -m "M3: 置換実装（置換して次を検索/すべて置換/選択範囲のみ・件数通知・1アンドゥ）"
```

---

## Task 7: App — MainForm への配線（メニュー / Ctrl+F・Ctrl+H / F3・Shift+F3）

**Files:**
- Modify: `src/yEdit.App/MainForm.cs`

**Step 1: SearchController フィールドを追加し生成する**

`MainForm` のフィールド群（`_docs` の近く）に:

```csharp
    private SearchController _search = null!; // コンストラクタで生成
```

コンストラクタ内、`_docs` 生成後・`NewFile()` の前あたりに:

```csharp
        _search = new SearchController(_docs, this);
```

**Step 2: 編集メニューに検索系を追加**

`BuildMenu()` の「編集」メニュー、`すべて選択` の後に区切りと項目を追加:

```csharp
        edit.DropDownItems.Add(new ToolStripSeparator());
        AddMenuItem(edit, "検索(&F)...", (_, _) => _search.OpenFind(), Keys.Control | Keys.F);
        AddMenuItem(edit, "次を検索(&N)", (_, _) => _search.FindNext(), Keys.F3);
        AddMenuItem(edit, "前を検索(&P)", (_, _) => _search.FindPrev(), Keys.Shift | Keys.F3);
        AddMenuItem(edit, "置換(&H)...", (_, _) => _search.OpenReplace(), Keys.Control | Keys.H);
```

> 注: メニューショートカットで `F3`/`Shift+F3` が拾えるが、エディタにフォーカスがある時に
> 子コントロールへ食われないか実機で確認する。食われる場合は Step 3 の `ProcessCmdKey` 併用で確実化。

**Step 3: ProcessCmdKey に F3/Shift+F3 のフォールバックを追加**

既存の `ProcessCmdKey` の `switch (keyData)` に分岐を追加（タブ切替の case 群と並べる）:

```csharp
            case Keys.F3: _search.FindNext(); return true;
            case Keys.Shift | Keys.F3: _search.FindPrev(); return true;
```

> メニューのショートカットと二重発火しないよう、`ProcessCmdKey` が先に `true` を返せばメニュー側は
> 呼ばれない（同一フレームで二回呼ばれないこと）を実機で確認。二重発火する場合はメニュー側の
> `ShortcutKeys` から `F3`/`Shift+F3` を外し、`ProcessCmdKey` 一本にする（表示テキストにキーは併記）。

**Step 4: ビルドして 0 警告を確認**

Run: `dotnet build yEdit.sln`
Expected: Build succeeded, 0 Warning。

**Step 5: コミット**

```bash
git add src/yEdit.App/MainForm.cs
git commit -m "M3: MainForm 配線（検索/置換メニュー・Ctrl+F/Ctrl+H・F3/Shift+F3）"
```

---

## Task 8: 仕上げ — ビルド0警告・手動スモーク・実機 SR 検証（DoD）

**Files:** （コード変更があれば該当ファイル）

**Step 1: ソリューション全体をビルド（0 警告）とテスト**

```bash
dotnet build yEdit.sln
dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj
```
Expected: Build 0 Warning、全テスト PASS。

**Step 2: 手動スモーク（視覚確認）**

`dotnet run --project src/yEdit.App` で起動し:
- `Ctrl+F` で検索ダイアログ → 文字列入力で件数表示が更新される（移動しない）。
- `F3`/`Shift+F3` で前後のヒットへ移動・選択される。末尾/先頭で「これ以上見つかりません」。
- 大小/単語/正規表現の各チェックで件数・ヒットが変わる。不正な正規表現で「正しくありません」。
- `Ctrl+H` で置換欄が出る。「置換して次を検索」「すべて置換（件数）」「選択範囲のみ」。
- ダイアログを開いたままタブ切替 → 次操作が新アクティブに効く。`Esc` で閉じ、閉じても `F3` が効く。
- 置換は `Ctrl+Z` 1 回で戻る（すべて置換も1アンドゥ）。

**Step 3: 実機 SR 受け入れ（DoD の核 / PC-Talker・NVDA）**

設計 §5 のチェックリストを PC-Talker と NVDA の両方で確認する。特に **§3.4 の `RaiseAutomationNotification` による件数/不在の読み上げ**が各 SR で通るかを確認し、結果を記録する:
- 通る → そのまま採用。
- PC-Talker で通らない → フォールバック（ステータス視覚表示＋選択移動による行読み）で最低限成立することを確認し、申し送りに記載（必要なら M6 の Announcer で改善）。

**Step 4: 申し送りをメモリへ記録**

`m3-followups.md`（非ブロッカーの宿題：通知の SR 別実機結果、単語境界の CJK 制約、増分カウントの大ファイル最適化 など）を作成し `MEMORY.md` に1行追加。`production-build-started.md` を M3 完了・次は M4（grep）に更新。

**Step 5: 別エージェントでコードレビュー → main へ no-ff マージ**

メモリ `review-by-separate-agent` / `phase-work-git-flow` 準拠。レビュー指摘対応後:

```bash
git checkout main
git merge --no-ff feature/m3-search -m "M3: 検索・置換（単一ファイル）をマージ"
```

---

## 完了の定義（DoD）

- Core 検索エンジンの xUnit が全 PASS（リテラル/正規表現/大小/単語/件数/前後/置換/範囲/CJK/不正正規表現）。
- ビルド 0 警告。
- 手動スモークの全項目が動作。
- 実機 SR（PC-Talker / NVDA）で設計 §5 の受け入れが通る（通知のフォールバック結果を記録）。
- 別エージェントレビュー反映済み、main へ no-ff マージ済み。

# P4: EditorControl IME(WM_IME_* 自前処理)実装計画

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** P3 完了時点(編集操作フル装備・663 テスト緑)の `EditorControl` に IMM32 経路(`WM_IME_*` / `Imm*`)を自前実装し、未確定文字列は overlay 描画(`TextBuffer` に触れず内部フィールドで inline 合成)、確定文字列は既存 `OnKeyPress` と同じ helper に流し込み 1 変換=1 Splice=1 Undo に潰す。ATOK 実機 7 項目チェックリストで DoD 判定。

**Architecture:** 未確定文字列の状態値型 `ImeCompositionState` を純ロジック(`yEdit.Core.Editing`)に置き xUnit で状態遷移テスト、EditorControl は薄い `WndProc` ハンドラのみ持つ。既存 `OnKeyPress` は private helper `InsertConfirmedText(string)` へ委譲するリファクタで挙動不変(P3 §0-9 温存契約=既存 663 テスト緑を維持)、IME 経路は `WM_IME_COMPOSITION`(`GCS_RESULTSTR`)から同じ helper を呼ぶ=1 経路。overlay 描画は `OnPaint` の本文描画直後に PixelMapper 座標で inline 合成、`ImmSetCandidateWindow`/`ImmSetCompositionFont` で候補窓とフォントを IME へ通知。設計書=`docs/plans/2026-07-06-p4-ime-design.md`。

**Tech Stack:** C# / .NET 9 / xUnit / WinForms(`Control` 派生・`WndProc` override)/ IMM32 P/Invoke(`imm32.dll`)/ 既存 `tests/yEdit.Core.Tests` + `tests/yEdit.Editor.Tests` + `tests/yEdit.Editor.Smoke` に追加。

**前提:**
- 作業は worktree `<repo>\.worktrees\custom-editcontrol-design`(ブランチ `feature/custom-editcontrol-design`)
- **main には一切触れない**(全フェーズを本ブランチに閉じ、P7 合格後に一括マージ。設計書§3 運用)
- P4 は **ScintillaHost / App 層に変更を加えない**(P6 で一発置換の予定・並行運用しない)
- P1/P2/P3 の公開 API(`TextBuffer`/`TextSnapshot`/`EditorControl` 既存 API)は変更しない=新規追加のみ。**破壊的変更禁止**
- 各タスク完了ごとにコミット。全タスク後に別エージェントレビュー(Task 15)
- 既存テスト 663 件は全タスクで緑を維持

---

## 0. 設計固定事項(全タスク共通の不変条件)

実装全体で守る。レビュー観点でもある。

1. **未確定文字列は TextBuffer に触れない**。`_ime` フィールド(`ImeCompositionState`)に保持し、`OnPaint` で本文描画後に inline 合成する(overlay 方式・設計書§2)
2. **確定文字列は 1 Splice=1 Undo**。`WM_IME_COMPOSITION(GCS_RESULTSTR)` は `InsertConfirmedText(string)` 1 度だけ呼ぶ(=coalescing を効かせない・単発 Splice)
3. **P3 §0-9 温存契約**: `OnKeyPress` の外側挙動は不変。実装は `InsertConfirmedText` に委譲(Task 3)し、既存テスト全緑を維持
4. **IME 経路は `WndProc` で横取り**(P3 で override 無し=新規)。`OnKeyDown`/`OnKeyPress` は書き換えない
5. **メッセージ処理後は `m.Result = IntPtr.Zero` で return**。既定処理を回す必要があるのは `WM_IME_SETCONTEXT` のみ(そこは lParam を書き換えて `base.WndProc(ref m)` を呼ぶ)
6. **`Imm*` は必ず `try/finally` で `ImmReleaseContext`**(ハンドルリーク防止)。`ImmGetContext` が `IntPtr.Zero` を返す環境(サーバ Core・古い環境)では全 IME 処理を no-op
7. **強制取消は 1 経路**(`CancelCompositionAndDefault`): ReadOnly 化・LostFocus・外部編集 API・MouseDown で共通に呼ぶ。中身は `ImmNotifyIME(hIMC, NI_COMPOSITIONSTR, CPS_CANCEL, 0)` + `_ime = default`
8. **サロゲート**は未確定文字列内部でも守る=`ImeCompositionState.CursorPos` が low surrogate 位置なら high 側にスナップ(§0 P3 と同方針)
9. **UIA イベント発火は P5**(P3 §0-7 継続)。P4 で未確定文字列を UIA プロバイダから露出しない(=IME 自身の候補読みに任せる方針・P0 実機結果)
10. **本番 App 層は触れない**。P4 の全変更は `yEdit.Editor` プロジェクト内(+ テスト+ smoke + 純ロジックの `yEdit.Core.Editing`)

---

## 1. 公開 API サーフェス(P4 追加分)

P1/P2/P3 で確定済みの API は不変。P4 は **public API を追加しない**(=すべて `WndProc` + private helper で完結)。

**新規 internal 型**(`yEdit.Core.Editing`):

```csharp
namespace yEdit.Core.Editing;

/// <summary>
/// IME 未確定文字列の状態値型(P4)。EditorControl の overlay 描画に消費される。
/// 純ロジックのため xUnit(Windows メッセージなし)で状態遷移テスト可能。
/// </summary>
public readonly record struct ImeCompositionState(
    int Start,           // 未確定文字列の挿入位置(_caret の凍結値)
    string Text,         // 未確定文字列(GCS_COMPSTR)
    int CursorPos,       // Text 内の IME キャレット位置(GCS_CURSORPOS)
    byte[] Attrs,        // 文字ごとの属性(GCS_COMPATTR):INPUT/TARGET_CONVERTED 等
    int[] Clauses)       // 節境界オフセット(GCS_COMPCLAUSE)
{
    public static ImeCompositionState Empty { get; } = new(0, "", 0, [], []);
    public bool IsActive => Text.Length > 0;
}

/// <summary>
/// IMM32 の GCS_COMPATTR 属性値(WinUser.h 由来)。
/// </summary>
public static class ImeAttribute
{
    public const byte Input             = 0x00;  // 未変換入力中
    public const byte TargetConverted   = 0x01;  // 変換対象節(反転表示すべき)
    public const byte Converted         = 0x02;  // 変換済(未確定)
    public const byte TargetNotConverted = 0x03;
    public const byte InputError        = 0x04;
    public const byte FixedConverted    = 0x05;  // 確定変換
}
```

**新規 EditorControl private helper**:

```csharp
private ImeCompositionState _ime = ImeCompositionState.Empty;
private bool IsComposing => _ime.IsActive;

// P3 の OnKeyPress と IME 経路(GCS_RESULTSTR)の共通挿入 helper
private void InsertConfirmedText(string text);

// WndProc から呼ばれる薄いハンドラ
private void OnImeStartComposition();
private void OnImeComposition(long gcsFlags);
private void OnImeEndComposition();
private void OnImeSetContext(ref Message m);

// 強制取消(縁ケース用)
private void CancelCompositionAndDefault();
```

---

## 2. 共通コマンド

```powershell
# Core 純ロジック(高速ループ)
dotnet test tests/yEdit.Core.Tests -c Release --nologo --filter "FullyQualifiedName~Editing.ImeComposition"
# EditorControl(WinForms・STA)
dotnet test tests/yEdit.Editor.Tests -c Release --nologo
# 全体回帰(各タスク末尾で実行)
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected(全体回帰): build 0 警告 / 既存 663 件 + P4 新規が全緑。

---

### Task 1: NativeMethods 拡張(WM_IME_* / Imm* P/Invoke)

**Files:**
- Modify: `src/yEdit.Editor/NativeMethods.cs`(定数と P/Invoke 追加)

Windows の IMM32 経路で使う定数と関数を全部追加する。ATOK 特有ではなく標準 IMM32 の範囲。以後の Task はここで宣言した名前だけを使う。

**Step 1: 定数群**

```csharp
// NativeMethods.cs に追加

// Windows メッセージ(WinUser.h)
public const int WM_IME_STARTCOMPOSITION = 0x010D;
public const int WM_IME_ENDCOMPOSITION   = 0x010E;
public const int WM_IME_COMPOSITION      = 0x010F;
public const int WM_IME_SETCONTEXT       = 0x0281;
public const int WM_IME_NOTIFY           = 0x0282;

// WM_IME_SETCONTEXT lParam ビット(既定 UI 抑止用)
public const int ISC_SHOWUICOMPOSITIONWINDOW = 0x80000000 >> 0;   // = 0x80000000 のダウンキャスト。負値を int で表現するため以下を使う。
// 実際には ISC_SHOWUICOMPOSITIONWINDOW = 0x80000000。C# は int リテラル 0x80000000 が uint 扱いなので unchecked((int)0x80000000) を使う。
// 定義例:
//   public const int ISC_SHOWUICOMPOSITIONWINDOW = unchecked((int)0x80000000);

// ImmGetCompositionString の dwIndex(GCS_ フラグ)
public const int GCS_COMPSTR    = 0x0008;
public const int GCS_COMPATTR   = 0x0010;
public const int GCS_COMPCLAUSE = 0x0020;
public const int GCS_CURSORPOS  = 0x0080;
public const int GCS_RESULTSTR  = 0x0800;

// ImmNotifyIME の dwAction / dwIndex(NI_COMPOSITIONSTR 用)
public const int NI_COMPOSITIONSTR = 0x0015;
public const int CPS_COMPLETE      = 0x0001;
public const int CPS_CANCEL        = 0x0004;

// COMPOSITIONFORM.dwStyle
public const int CFS_DEFAULT     = 0x0000;
public const int CFS_POINT       = 0x0002;
public const int CFS_CANDIDATEPOS = 0x0040;
```

**Step 2: 構造体**

```csharp
[StructLayout(LayoutKind.Sequential)]
public struct POINT
{
    public int x;
    public int y;
}

[StructLayout(LayoutKind.Sequential)]
public struct RECT
{
    public int left;
    public int top;
    public int right;
    public int bottom;
}

[StructLayout(LayoutKind.Sequential)]
public struct CANDIDATEFORM
{
    public int dwIndex;
    public int dwStyle;
    public POINT ptCurrentPos;
    public RECT rcArea;
}

// フォント伝達に使う LOGFONT(WinForms Font.ToLogFont に渡す)
[StructLayout(LayoutKind.Sequential, CharSet = CharSet.Unicode)]
public struct LOGFONT
{
    public int lfHeight;
    public int lfWidth;
    public int lfEscapement;
    public int lfOrientation;
    public int lfWeight;
    public byte lfItalic;
    public byte lfUnderline;
    public byte lfStrikeOut;
    public byte lfCharSet;
    public byte lfOutPrecision;
    public byte lfClipPrecision;
    public byte lfQuality;
    public byte lfPitchAndFamily;
    [MarshalAs(UnmanagedType.ByValTStr, SizeConst = 32)]
    public string lfFaceName;
}
```

**Step 3: P/Invoke**

```csharp
[DllImport("imm32.dll")]
public static extern nint ImmGetContext(nint hWnd);

[DllImport("imm32.dll")]
[return: MarshalAs(UnmanagedType.Bool)]
public static extern bool ImmReleaseContext(nint hWnd, nint hIMC);

// ImmGetCompositionString: バッファ長を返す(先に大きさ問い合わせ→バッファ確保→本呼び出し のパターン)
// lpBuf に IntPtr.Zero を渡すと必要バイト数を返す(自身のバッファは要らない)。
[DllImport("imm32.dll")]
public static extern int ImmGetCompositionStringW(nint hIMC, int dwIndex, nint lpBuf, int dwBufLen);

[DllImport("imm32.dll")]
[return: MarshalAs(UnmanagedType.Bool)]
public static extern bool ImmSetCandidateWindow(nint hIMC, ref CANDIDATEFORM lpCandidate);

[DllImport("imm32.dll")]
[return: MarshalAs(UnmanagedType.Bool)]
public static extern bool ImmSetCompositionFontW(nint hIMC, ref LOGFONT lplf);

[DllImport("imm32.dll")]
[return: MarshalAs(UnmanagedType.Bool)]
public static extern bool ImmNotifyIME(nint hIMC, int dwAction, int dwIndex, int dwValue);
```

**Step 4: ビルド確認**

```powershell
dotnet build yEdit.sln -c Release
```

Expected: 0 警告。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 1 NativeMethods 拡張(WM_IME_* / Imm* / CANDIDATEFORM / LOGFONT)"
```

---

### Task 2: ImeCompositionState 純ロジック + xUnit テスト

**Files:**
- Create: `src/yEdit.Core/Editing/ImeCompositionState.cs`
- Create: `src/yEdit.Core/Editing/ImeAttribute.cs`
- Create: `tests/yEdit.Core.Tests/Editing/ImeCompositionStateTests.cs`

未確定状態の値型と、`GCS_COMPATTR`/`GCS_COMPCLAUSE` のバイト列パースを純ロジックとして書く。Windows メッセージなしで xUnit テスト可能=状態遷移とサロゲート境界を CI で回帰する。

**Step 1: 失敗するテスト**

```csharp
// tests/yEdit.Core.Tests/Editing/ImeCompositionStateTests.cs
using yEdit.Core.Editing;
using Xunit;

namespace yEdit.Core.Tests.Editing;

public class ImeCompositionStateTests
{
    [Fact]
    public void Empty_HasNoActiveComposition()
    {
        Assert.False(ImeCompositionState.Empty.IsActive);
        Assert.Equal("", ImeCompositionState.Empty.Text);
    }

    [Fact]
    public void WithText_MarksActive()
    {
        var s = new ImeCompositionState(Start: 5, Text: "あい", CursorPos: 2, Attrs: [0, 0], Clauses: [0, 2]);
        Assert.True(s.IsActive);
        Assert.Equal(5, s.Start);
        Assert.Equal(2, s.CursorPos);
    }

    // Attrs パース: バイト列をそのまま byte[] へコピーする素朴ケース(GCS_COMPATTR が返すのは
    // 未確定文字列 UTF-16 code unit 数分の 1 バイト属性配列)。
    [Fact]
    public void ParseAttrs_ReturnsCopyOfSourceBytes()
    {
        var src = new byte[] { 0x00, 0x00, 0x01, 0x01, 0x00 };
        var parsed = ImeCompositionState.ParseAttrs(src);
        Assert.Equal(src, parsed);
    }

    // Clauses パース: DWORD(int32) little-endian の配列。
    // GCS_COMPCLAUSE が返すのは「節開始位置の連続 + 末尾の全長」形式。
    [Fact]
    public void ParseClauses_DecodesInt32LittleEndian()
    {
        // 3 clauses: [0, 2, 4] → 0x00,0x00,0x00,0x00, 0x02,0x00,0x00,0x00, 0x04,0x00,0x00,0x00
        var bytes = new byte[]
        {
            0x00, 0x00, 0x00, 0x00,
            0x02, 0x00, 0x00, 0x00,
            0x04, 0x00, 0x00, 0x00,
        };
        var parsed = ImeCompositionState.ParseClauses(bytes);
        Assert.Equal(new[] { 0, 2, 4 }, parsed);
    }

    [Fact]
    public void ParseAttrs_EmptyInput_ReturnsEmpty()
    {
        Assert.Empty(ImeCompositionState.ParseAttrs([]));
        Assert.Empty(ImeCompositionState.ParseClauses([]));
    }

    // サロゲート境界: CursorPos が low surrogate 位置を指したら high 側にスナップ。
    // "あ😀い" = 'あ' (1) + '😀' surrogate pair (2) + 'い' (1) = 4 UTF-16 code units
    [Fact]
    public void SnapCursorPos_MovesFromLowSurrogateToHigh()
    {
        string text = "あ😀い";      // Length = 4
        // CursorPos = 2 (high surrogate) → 保持
        // CursorPos = 3 (low surrogate) → 2 にスナップ
        Assert.Equal(2, ImeCompositionState.SnapCursorPos(text, 3));
        Assert.Equal(2, ImeCompositionState.SnapCursorPos(text, 2));
        Assert.Equal(0, ImeCompositionState.SnapCursorPos(text, 0));
        Assert.Equal(4, ImeCompositionState.SnapCursorPos(text, 4));   // 末尾
        Assert.Equal(1, ImeCompositionState.SnapCursorPos(text, 1));
    }

    // Clauses のバイト長不整合(4 の倍数でない): 切り捨てで tolerate する
    [Fact]
    public void ParseClauses_IgnoresTrailingPartialDword()
    {
        var bytes = new byte[] { 0x00, 0x00, 0x00, 0x00, 0x02, 0x00 };  // 6 bytes = 1 full + 2 半端
        Assert.Equal(new[] { 0 }, ImeCompositionState.ParseClauses(bytes));
    }
}
```

**Step 2: 実装**

```csharp
// src/yEdit.Core/Editing/ImeAttribute.cs
namespace yEdit.Core.Editing;

public static class ImeAttribute
{
    public const byte Input             = 0x00;
    public const byte TargetConverted   = 0x01;
    public const byte Converted         = 0x02;
    public const byte TargetNotConverted = 0x03;
    public const byte InputError        = 0x04;
    public const byte FixedConverted    = 0x05;
}
```

```csharp
// src/yEdit.Core/Editing/ImeCompositionState.cs
namespace yEdit.Core.Editing;

public readonly record struct ImeCompositionState(
    int Start,
    string Text,
    int CursorPos,
    byte[] Attrs,
    int[] Clauses)
{
    public static ImeCompositionState Empty { get; } = new(0, "", 0, [], []);
    public bool IsActive => Text.Length > 0;

    /// <summary>GCS_COMPATTR のバイト列をそのままコピー(未確定文字列 UTF-16 code unit ごと 1 バイト)。</summary>
    public static byte[] ParseAttrs(ReadOnlySpan<byte> src)
    {
        if (src.Length == 0) return [];
        var buf = new byte[src.Length];
        src.CopyTo(buf);
        return buf;
    }

    /// <summary>GCS_COMPCLAUSE のバイト列を int32 (little-endian) 配列としてデコード。
    /// 半端バイト(4 の倍数でない末尾)は切り捨てる。</summary>
    public static int[] ParseClauses(ReadOnlySpan<byte> src)
    {
        int n = src.Length / 4;
        if (n == 0) return [];
        var buf = new int[n];
        for (int i = 0; i < n; i++)
        {
            int off = i * 4;
            buf[i] = src[off] | (src[off + 1] << 8) | (src[off + 2] << 16) | (src[off + 3] << 24);
        }
        return buf;
    }

    /// <summary>CursorPos がサロゲート pair の low 位置を指していたら high 位置にスナップ。</summary>
    public static int SnapCursorPos(string text, int cursor)
    {
        if (cursor <= 0 || cursor >= text.Length) return Math.Clamp(cursor, 0, text.Length);
        if (char.IsLowSurrogate(text[cursor]) && char.IsHighSurrogate(text[cursor - 1]))
            return cursor - 1;
        return cursor;
    }
}
```

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Core.Tests -c Release --nologo --filter "FullyQualifiedName~Editing.ImeComposition"
```

Expected: 全緑。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 既存 663 + 新規全緑。

**Step 5: コミット**

```powershell
git add src/yEdit.Core/Editing/ImeCompositionState.cs src/yEdit.Core/Editing/ImeAttribute.cs tests/yEdit.Core.Tests/Editing/ImeCompositionStateTests.cs
git commit -m "P4: Task 2 ImeCompositionState 純ロジック(Attrs/Clauses パース + サロゲートスナップ)"
```

---

### Task 3: OnKeyPress を InsertConfirmedText へ委譲(挙動不変リファクタ)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnKeyPress` を 2 行に潰し `InsertConfirmedText` を新設)
- Test: 既存 663 テストが全緑を維持することで挙動不変を検証(新規テスト不要)

P4 の IME 経路(`GCS_RESULTSTR`)は同じ「選択削除→Insert→AfterEdit」を必要とする。P3 §0-9 の「OnKeyPress は書き換えない」約束を守るため、実装本体を private helper に切り出し、`OnKeyPress` と IME 経路の両方から同じ関数を呼ぶ。挙動不変が本タスクの唯一の DoD(=既存テスト緑)。

**Step 1: 現状の OnKeyPress を確認**

`src/yEdit.Editor/EditorControl.cs:1531-1579` を Read で確認(既に読んでいる)。次の 3 分岐:
1. 選択あり → `_buffer.Replace(s, en-s, ins)`
2. Overtype + 選択なし → 後続 1 文字を潰して `_buffer.Replace(_caret, overwriteLen, ins)`
3. 通常挿入 → `_buffer.Insert(_caret, ins)`

**Step 2: private helper 抽出**

```csharp
/// <summary>
/// 確定文字列を挿入する共通経路(P4)。P3 の <see cref="OnKeyPress"/> と
/// P4 の <see cref="WM_IME_COMPOSITION"/> (GCS_RESULTSTR) の両方から呼ばれる。
/// 選択があれば削除→挿入。<see cref="Overtype"/> ON では直後 1 文字を潰す
/// (改行はスキップ・サロゲートペアは 2 code units を潰す)。
/// <see cref="ReadOnly"/> ON では no-op。
/// </summary>
/// <remarks>
/// - <paramref name="text"/> の長さは制限なし(IME 確定は通常 1〜数文字だが仕様上長文もあり)
/// - <paramref name="text"/> が空文字なら no-op(ESC 取消時の GCS_RESULTSTR=空を安全化)
/// - <c>_desiredXpx</c> は -1 リセット(P3 §0-6)、AfterEdit で追従スクロール
/// </remarks>
private void InsertConfirmedText(string text)
{
    if (_buffer is null || ReadOnly) return;
    if (string.IsNullOrEmpty(text)) return;

    var (s, en) = GetSelectionCharRange();
    if (s != en)
    {
        _buffer.Replace(s, en - s, text);
        _caret = _anchor = s + text.Length;
    }
    else if (Overtype)
    {
        var snap = _buffer.Current;
        int overwriteLen = 0;
        if (_caret < snap.CharLength)
        {
            char nc = snap.GetChar(_caret);
            if (nc != '\r' && nc != '\n')
            {
                overwriteLen = (char.IsHighSurrogate(nc) && _caret + 1 < snap.CharLength
                                && char.IsLowSurrogate(snap.GetChar(_caret + 1))) ? 2 : 1;
            }
        }
        _buffer.Replace(_caret, overwriteLen, text);
        _caret = _anchor = _caret + text.Length;
    }
    else
    {
        _buffer.Insert(_caret, text);
        _caret = _anchor = _caret + text.Length;
    }

    _desiredXpx = -1;
    AfterEdit();
}
```

**Step 3: OnKeyPress を委譲に置換(制御文字ガードは温存)**

```csharp
protected override void OnKeyPress(KeyPressEventArgs e)
{
    base.OnKeyPress(e);
    if (_buffer is null || ReadOnly) return;
    char ch = e.KeyChar;

    // 制御文字(0x00〜0x1F, 0x7F)は無視。編集用途は OnKeyDown 経路で処理する(§0-9 温存)。
    if (ch < 0x20 || ch == 0x7F) return;

    InsertConfirmedText(ch.ToString());
    e.Handled = true;
}
```

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / **既存 663 件全緑**(P4 での新規追加はなし=挙動不変の証拠)。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 3 OnKeyPress を InsertConfirmedText へ委譲(挙動不変・IME 経路と共通化)"
```

---

### Task 4: WndProc override + WM_IME_SETCONTEXT で既定 UI 抑止

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`WndProc` override 新設)
- Test: `tests/yEdit.Editor.Tests/EditorControlImeTests.cs` を新設

`WndProc` を override し、`WM_IME_SETCONTEXT` を横取りして lParam の `ISC_SHOWUICOMPOSITIONWINDOW` ビットをマスクする(=既定の composition ウィンドウ描画を止めて自前描画に切り替える)。他の `WM_IME_*` は次以降の Task で埋める(現段階では default 挙動)。

**Step 1: 失敗するテスト**

```csharp
// tests/yEdit.Editor.Tests/EditorControlImeTests.cs
namespace yEdit.Editor.Tests;

public class EditorControlImeTests
{
    // WndProc override が呼ばれ、WM_IME_SETCONTEXT の lParam から
    // ISC_SHOWUICOMPOSITIONWINDOW ビットが落ちて base に流されることを確認する。
    // WndProc は protected なため internal ヘルパー ProcessMessage(ref Message) を用意し
    // InternalsVisibleTo でテストから呼べるようにする。
    [Fact]
    public void WndProc_ImeSetContext_ClearsShowUICompositionWindowBit() => Sta.Run(() =>
    {
        using var c = new EditorControl();
        using var f = new Form { Visible = false };
        f.Controls.Add(c);
        var _ = f.Handle;

        var m = new Message
        {
            HWnd = c.Handle,
            Msg = NativeMethods.WM_IME_SETCONTEXT,
            WParam = 1,
            LParam = new nint(NativeMethods.ISC_SHOWUICOMPOSITIONWINDOW | 0x0F),
        };
        c.__TestProcessMessage(ref m);
        // 期待: base に渡す前に ISC_SHOWUICOMPOSITIONWINDOW ビットが落ちている(=lParam に反映済)
        Assert.Equal(0x0F, m.LParam.ToInt64() & 0xFFFFFFFFL);
    });
}
```

**Step 2: 実装**

`src/yEdit.Editor/EditorControl.cs` に追加:

```csharp
protected override void WndProc(ref Message m)
{
    switch (m.Msg)
    {
        case NativeMethods.WM_IME_SETCONTEXT:
            OnImeSetContext(ref m);
            return;
        // 以下は Task 5〜8 で埋める
        // case NativeMethods.WM_IME_STARTCOMPOSITION: ...
        // case NativeMethods.WM_IME_COMPOSITION: ...
        // case NativeMethods.WM_IME_ENDCOMPOSITION: ...
    }
    base.WndProc(ref m);
}

/// <summary>
/// WM_IME_SETCONTEXT: IME の既定 composition ウィンドウ描画を止めて自前描画に切り替える(P4)。
/// lParam の ISC_SHOWUICOMPOSITIONWINDOW ビットを落として base.WndProc に流す。
/// </summary>
private void OnImeSetContext(ref Message m)
{
    long lp = m.LParam.ToInt64();
    lp &= ~(long)NativeMethods.ISC_SHOWUICOMPOSITIONWINDOW;
    m.LParam = new nint(lp);
    base.WndProc(ref m);
}

// テスト用ヘルパ(internal・EditorControlImeTests から呼ぶ)
internal void __TestProcessMessage(ref Message m) => WndProc(ref m);
```

`yEdit.Editor.csproj` に `InternalsVisibleTo` を追加:

```xml
<ItemGroup>
  <InternalsVisibleTo Include="yEdit.Editor.Tests" />
</ItemGroup>
```

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Editor.Tests -c Release --nologo --filter "FullyQualifiedName~EditorControlImeTests"
```

Expected: 1 件緑。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 4 WndProc override + WM_IME_SETCONTEXT で既定 UI 抑止"
```

---

### Task 5: WM_IME_STARTCOMPOSITION 受信(選択削除 + `_ime` 初期化)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnImeStartComposition` + `_ime` フィールド追加)
- Test: `tests/yEdit.Editor.Tests/EditorControlImeTests.cs`

未確定期間の開始。選択があれば削除して先頭位置を `_ime.Start` に。`ImmSetCandidateWindow` の初期位置設定は Task 12 に集約するので、ここでは `_ime` フィールドの初期化のみ行う。

**Step 1: 失敗するテスト**

```csharp
[Fact]
public void ImeStartComposition_ClearsSelectionAndInitializesImeStart() => Sta.Run(() =>
{
    using var c = new EditorControl();
    using var f = new Form { Visible = false };
    f.Controls.Add(c);
    var _ = f.Handle;
    c.SetSource(TextBuffer.FromString("abcdef"));
    c.SetSelectionCharRange(1, 4);   // 選択 "bcd"

    var m = new Message
    {
        HWnd = c.Handle,
        Msg = NativeMethods.WM_IME_STARTCOMPOSITION,
    };
    c.__TestProcessMessage(ref m);

    Assert.Equal("aef", c.__TestSnapshotText());   // 選択削除
    Assert.Equal(1, c.CaretCharOffset);            // キャレット = 選択先頭
    Assert.Equal(1, c.__TestImeStart());
    Assert.False(c.__TestIsComposing());           // 未確定文字列はまだ空
});

[Fact]
public void ImeStartComposition_NoSelection_KeepsCaretAndInitializesImeStart() => Sta.Run(() =>
{
    using var c = MakeControl("abcdef");
    c.SetCaretCharOffset(3);

    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);

    Assert.Equal(3, c.CaretCharOffset);
    Assert.Equal(3, c.__TestImeStart());
});
```

`MakeControl` は Task 4 で用意した簡易ヘルパ(=`new EditorControl()` + `Form` に載せて Handle 実現 + `SetSource`)を extract。

**Step 2: 実装**

`_ime` フィールドとハンドラを追加(EditorControl.cs):

```csharp
using yEdit.Core.Editing;    // ImeCompositionState

// フィールド追加
private ImeCompositionState _ime = ImeCompositionState.Empty;
private bool IsComposing => _ime.IsActive;

// テスト用アクセサ(internal)
internal int __TestImeStart() => _ime.Start;
internal bool __TestIsComposing() => IsComposing;
internal string __TestImeText() => _ime.Text;
internal string __TestSnapshotText() => _buffer?.Current.ToString() ?? "";

// WndProc に case を追加
case NativeMethods.WM_IME_STARTCOMPOSITION:
    OnImeStartComposition();
    m.Result = IntPtr.Zero;
    return;

private void OnImeStartComposition()
{
    if (_buffer is null || ReadOnly) return;
    var (s, en) = GetSelectionCharRange();
    if (s != en)
    {
        // 選択削除(1 Splice=1 Undo・以後の未確定は overlay=Undo に積まれない)
        _buffer.Replace(s, en - s, "");
        _caret = _anchor = s;
        _desiredXpx = -1;
        AfterEdit();
    }
    _ime = ImeCompositionState.Empty with { Start = _caret };
    Invalidate();
}
```

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Editor.Tests -c Release --nologo --filter "FullyQualifiedName~ImeStartComposition"
```

Expected: 2 件緑。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 5 WM_IME_STARTCOMPOSITION 受信(選択削除 + _ime 初期化)"
```

---

### Task 6: WM_IME_COMPOSITION の GCS_COMPSTR 受信(未確定文字列取得)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnImeComposition` の COMPSTR 分岐 + `ReadImeString` ヘルパ)
- Test: `tests/yEdit.Editor.Tests/EditorControlImeTests.cs`

`WM_IME_COMPOSITION` の lParam(GCS_ フラグ束)を見て、`GCS_COMPSTR`/`GCS_COMPATTR`/`GCS_COMPCLAUSE`/`GCS_CURSORPOS` を `_ime` に反映する。**このタスクでは COMPSTR/COMPATTR/COMPCLAUSE/CURSORPOS の 4 種すべて Imm* 呼び出しに載せる**(節ハイライトの描画本体は Task 10・キャレット位置反映は Task 11 で扱う=`_ime` フィールドへの取り込みだけ本タスクで済ませる)。

**Step 1: 失敗するテスト**

`WndProc` はリアル IME に依存するので、テストは `_ime` を直接読み取れる internal メソッド `__TestApplyComposition(text, cursorPos, attrs, clauses)` を通じてハンドラの状態遷移だけ検証する。Windows API を経由するテストは Task 14 の smoke で行う(=自動化しない)。

```csharp
[Fact]
public void ApplyComposition_UpdatesImeFields() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    c.SetCaretCharOffset(1);

    // STARTCOMPOSITION 相当
    var m1 = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m1);

    // COMPOSITION 相当を internal 経由で流す
    c.__TestApplyComposition("あい", cursorPos: 2, attrs: [0, 0], clauses: [0, 2]);

    Assert.True(c.__TestIsComposing());
    Assert.Equal("あい", c.__TestImeText());
    Assert.Equal(1, c.__TestImeStart());   // STARTCOMPOSITION で凍結した _caret
});

[Fact]
public void ApplyComposition_EmptyText_ReturnsToEmpty() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あ", cursorPos: 1, attrs: [0], clauses: [0, 1]);
    c.__TestApplyComposition("", cursorPos: 0, attrs: [], clauses: []);

    Assert.False(c.__TestIsComposing());
});
```

**Step 2: 実装**

```csharp
// WndProc に case を追加
case NativeMethods.WM_IME_COMPOSITION:
    OnImeComposition(m.LParam.ToInt64());
    m.Result = IntPtr.Zero;
    return;

/// <summary>
/// WM_IME_COMPOSITION: lParam の GCS_ フラグ束を見て未確定/確定文字列を反映する。
/// Task 6 では COMPSTR/COMPATTR/COMPCLAUSE/CURSORPOS の受信のみ。GCS_RESULTSTR は Task 7。
/// </summary>
private void OnImeComposition(long gcsFlags)
{
    if (_buffer is null || ReadOnly) return;
    nint hIMC = NativeMethods.ImmGetContext(Handle);
    if (hIMC == IntPtr.Zero) return;
    try
    {
        if ((gcsFlags & NativeMethods.GCS_COMPSTR) != 0)
        {
            string compStr = ReadImeString(hIMC, NativeMethods.GCS_COMPSTR);
            byte[] attrs = (gcsFlags & NativeMethods.GCS_COMPATTR) != 0
                ? ImeCompositionState.ParseAttrs(ReadImeBytes(hIMC, NativeMethods.GCS_COMPATTR))
                : [];
            int[] clauses = (gcsFlags & NativeMethods.GCS_COMPCLAUSE) != 0
                ? ImeCompositionState.ParseClauses(ReadImeBytes(hIMC, NativeMethods.GCS_COMPCLAUSE))
                : [];
            int cursor = (gcsFlags & NativeMethods.GCS_CURSORPOS) != 0
                ? ImeCompositionState.SnapCursorPos(compStr, ReadImeInt(hIMC, NativeMethods.GCS_CURSORPOS))
                : 0;
            ApplyComposition(compStr, cursor, attrs, clauses);
        }
        // GCS_RESULTSTR は Task 7 で扱う
    }
    finally
    {
        NativeMethods.ImmReleaseContext(Handle, hIMC);
    }
}

/// <summary>ハンドラ本体(Task 6)。テストは __TestApplyComposition 経由でここを呼ぶ。</summary>
private void ApplyComposition(string text, int cursorPos, byte[] attrs, int[] clauses)
{
    _ime = new ImeCompositionState(
        Start: _ime.IsActive ? _ime.Start : _caret,   // 多重メッセージ自己防衛(§0 4-9)
        Text: text,
        CursorPos: cursorPos,
        Attrs: attrs,
        Clauses: clauses);
    Invalidate();
}

internal void __TestApplyComposition(string text, int cursorPos, byte[] attrs, int[] clauses)
    => ApplyComposition(text, cursorPos, attrs, clauses);

// Imm* からの文字列/バイト列読み出しヘルパ
private static string ReadImeString(nint hIMC, int gcsFlag)
{
    int byteLen = NativeMethods.ImmGetCompositionStringW(hIMC, gcsFlag, IntPtr.Zero, 0);
    if (byteLen <= 0) return "";
    nint buf = Marshal.AllocHGlobal(byteLen);
    try
    {
        NativeMethods.ImmGetCompositionStringW(hIMC, gcsFlag, buf, byteLen);
        return Marshal.PtrToStringUni(buf, byteLen / 2) ?? "";
    }
    finally { Marshal.FreeHGlobal(buf); }
}

private static byte[] ReadImeBytes(nint hIMC, int gcsFlag)
{
    int byteLen = NativeMethods.ImmGetCompositionStringW(hIMC, gcsFlag, IntPtr.Zero, 0);
    if (byteLen <= 0) return [];
    nint buf = Marshal.AllocHGlobal(byteLen);
    try
    {
        NativeMethods.ImmGetCompositionStringW(hIMC, gcsFlag, buf, byteLen);
        var arr = new byte[byteLen];
        Marshal.Copy(buf, arr, 0, byteLen);
        return arr;
    }
    finally { Marshal.FreeHGlobal(buf); }
}

private static int ReadImeInt(nint hIMC, int gcsFlag)
{
    // GCS_CURSORPOS は「戻り値そのものが値」(バッファは使わない)
    int v = NativeMethods.ImmGetCompositionStringW(hIMC, gcsFlag, IntPtr.Zero, 0);
    return v;
}
```

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Editor.Tests -c Release --nologo --filter "FullyQualifiedName~ApplyComposition"
```

Expected: 2 件緑。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 6 WM_IME_COMPOSITION GCS_COMPSTR 受信(_ime へ反映 + Imm ヘルパ)"
```

---

### Task 7: WM_IME_COMPOSITION の GCS_RESULTSTR 受信(1 Splice=1 Undo)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnImeComposition` に RESULTSTR 分岐追加)
- Test: `tests/yEdit.Editor.Tests/EditorControlImeTests.cs`

確定通知。`_ime` を先にクリアしてから `InsertConfirmedText`(Task 3)を呼ぶ=1 Splice=1 Undo。空文字列(ESC 取消時)なら何も挿入しない。

**Step 1: 失敗するテスト**

```csharp
[Fact]
public void ApplyResult_InsertsAsSingleUndo() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    c.SetCaretCharOffset(1);

    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あい", cursorPos: 2, attrs: [0, 0], clauses: [0, 2]);
    c.__TestApplyResult("漢字");

    Assert.False(c.__TestIsComposing());
    Assert.Equal("a漢字bc", c.__TestSnapshotText());
    Assert.Equal(1 + 2, c.CaretCharOffset);   // Start + 確定文字数
    Assert.True(c.CanUndo);
    c.Undo();
    Assert.Equal("abc", c.__TestSnapshotText());   // 1 Undo で元に戻る
});

[Fact]
public void ApplyResult_EmptyString_NoInsert() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    c.SetCaretCharOffset(1);
    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あ", cursorPos: 1, attrs: [0], clauses: [0, 1]);
    c.__TestApplyResult("");   // ESC 取消相当

    Assert.False(c.__TestIsComposing());
    Assert.Equal("abc", c.__TestSnapshotText());
    Assert.Equal(1, c.CaretCharOffset);
});
```

**Step 2: 実装**

```csharp
// OnImeComposition に追加(GCS_COMPSTR 分岐の直後)
if ((gcsFlags & NativeMethods.GCS_RESULTSTR) != 0)
{
    string resultStr = ReadImeString(hIMC, NativeMethods.GCS_RESULTSTR);
    ApplyResult(resultStr);
}

private void ApplyResult(string text)
{
    _ime = ImeCompositionState.Empty;   // overlay を先に外して Insert 経路と競合させない
    if (text.Length > 0) InsertConfirmedText(text);
    Invalidate();
}

internal void __TestApplyResult(string text) => ApplyResult(text);
```

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Editor.Tests -c Release --nologo --filter "FullyQualifiedName~ApplyResult"
```

Expected: 2 件緑。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 7 WM_IME_COMPOSITION GCS_RESULTSTR 受信(1 Splice=1 Undo)"
```

---

### Task 8: WM_IME_ENDCOMPOSITION + フォーカス/ReadOnly 縁ケース

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnImeEndComposition` / `CancelCompositionAndDefault` / `OnLostFocus` 拡張 / `ReadOnly` setter 拡張)
- Test: `tests/yEdit.Editor.Tests/EditorControlImeTests.cs`

`WM_IME_ENDCOMPOSITION` で `_ime` をクリア。`OnLostFocus` は IME に `CPS_COMPLETE` を通知して確定を試みる。`ReadOnly = true` への切り替え時は `CPS_CANCEL` で強制取消。

**Step 1: 失敗するテスト**

```csharp
[Fact]
public void EndComposition_ClearsIme() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    var m1 = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m1);
    c.__TestApplyComposition("あ", 1, [0], [0, 1]);
    var m2 = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_ENDCOMPOSITION };
    c.__TestProcessMessage(ref m2);
    Assert.False(c.__TestIsComposing());
});

[Fact]
public void ReadOnly_True_DuringComposition_ForceCancels() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あ", 1, [0], [0, 1]);
    Assert.True(c.__TestIsComposing());

    c.ReadOnly = true;
    Assert.False(c.__TestIsComposing());   // 強制取消で _ime クリア
});

[Fact]
public void LostFocus_DuringComposition_ClearsIme() => Sta.Run(() =>
{
    using var f = new Form { Visible = false };
    using var c = new EditorControl();
    using var other = new Button();
    f.Controls.Add(c);
    f.Controls.Add(other);
    var _ = f.Handle;
    c.SetSource(TextBuffer.FromString("abc"));
    c.Select();

    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あ", 1, [0], [0, 1]);

    other.Select();   // フォーカス移動
    Assert.False(c.__TestIsComposing());
});
```

**Step 2: 実装**

```csharp
// WndProc に case 追加
case NativeMethods.WM_IME_ENDCOMPOSITION:
    OnImeEndComposition();
    m.Result = IntPtr.Zero;
    return;

private void OnImeEndComposition()
{
    _ime = ImeCompositionState.Empty;
    Invalidate();
}

/// <summary>
/// 未確定を強制取消し、IME 側にも通知する(縁ケース共通経路)。
/// ImmNotifyIME 失敗時(IME 無効環境等)でも overlay は必ずクリアする。
/// </summary>
private void CancelCompositionAndDefault()
{
    if (!IsComposing) return;
    nint hIMC = NativeMethods.ImmGetContext(Handle);
    if (hIMC != IntPtr.Zero)
    {
        try
        {
            NativeMethods.ImmNotifyIME(hIMC, NativeMethods.NI_COMPOSITIONSTR,
                                       NativeMethods.CPS_CANCEL, 0);
        }
        finally { NativeMethods.ImmReleaseContext(Handle, hIMC); }
    }
    _ime = ImeCompositionState.Empty;
    Invalidate();
}

/// <summary>
/// フォーカス喪失時、未確定があれば確定を試みる(CPS_COMPLETE)。
/// 失敗しても WM_IME_ENDCOMPOSITION が届くので overlay は自動クリアされる。
/// </summary>
protected override void OnLostFocus(EventArgs e)
{
    // 既存の Task 10 実装(P3)を保持しつつ先頭に IME 完了を挿入する
    if (IsComposing)
    {
        nint hIMC = NativeMethods.ImmGetContext(Handle);
        if (hIMC != IntPtr.Zero)
        {
            try
            {
                NativeMethods.ImmNotifyIME(hIMC, NativeMethods.NI_COMPOSITIONSTR,
                                           NativeMethods.CPS_COMPLETE, 0);
            }
            finally { NativeMethods.ImmReleaseContext(Handle, hIMC); }
        }
        // ImmNotifyIME が届かない環境の保険=Invalidate 前に overlay を落とす
        _ime = ImeCompositionState.Empty;
    }
    base.OnLostFocus(e);
    // 既存の _hasFocus 更新等はここに続く(Task 10 の実装)
}
```

`ReadOnly` setter に `CancelCompositionAndDefault` フックを追加(P3 で auto-property の場合は明示的 setter 化):

```csharp
private bool _readOnly;
public bool ReadOnly
{
    get => _readOnly;
    set
    {
        if (_readOnly == value) return;
        _readOnly = value;
        if (value) CancelCompositionAndDefault();
    }
}
```

**注意**: P3 実装で `ReadOnly` が auto-property なら明示的 setter 化に伴い挙動テストが影響を受けないか要確認(P3 テストで `ReadOnly = true` 後の状態を assert しているもの)=setter 追加のみで挙動不変。

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Editor.Tests -c Release --nologo --filter "FullyQualifiedName~EndComposition|ReadOnly_True_DuringComposition|LostFocus_DuringComposition"
```

Expected: 3 件緑。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑(P3 の `ReadOnly` 関連テストが緑を維持することも確認)。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 8 WM_IME_ENDCOMPOSITION + LostFocus / ReadOnly 強制取消"
```

---

### Task 9: OnPaint への未確定文字列 overlay 描画(下線 + inline 合成)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnPaint` の末尾に `DrawImeOverlay` を追加)

未確定文字列を本文描画の直後に inline 合成する(下線・通常前景色)。節ハイライト(反転)は Task 10、IME 内キャレット位置反映は Task 11 で扱う=本タスクは「未確定文字列が下線付きで表示されるだけ」まで。

**Step 1: 実装方針の整理**

- `_ime.Start` の client 座標を PixelMapper(`ComputeCaretPoint` 相当)で算出
- `Graphics.DrawString`(または `TextRenderer.DrawText`)+ `Underline` フォントで 1 度に描画
- **折り返し扱わない**(未確定文字列が右端を越えても 1 行に描画=Scintilla 同挙動)
- 描画色: 前景=`ForeColor` / 下線=同色
- 描画順: 本文 → cellHighlight → キャレット行強調 → **本タスクの overlay** → システムキャレット(順序注意)

**Step 2: 実装**

`OnPaint` の末尾直前(システムキャレット処理の前)に:

```csharp
// P4 Task 9: 未確定文字列 overlay
if (IsComposing) DrawImeOverlay(e.Graphics);
```

`DrawImeOverlay` を新設:

```csharp
private void DrawImeOverlay(Graphics g)
{
    if (_buffer is null) return;
    var (x, y, _) = ComputeCaretPoint(_ime.Start);
    if (x < 0 || y < 0) return;   // 可視範囲外は no-op

    using var underlineFont = new Font(_font, _font.Style | FontStyle.Underline);
    // ClearType/背景描画は既存本文と競合しないよう TextRenderer で描画(GDI 直接=描画ズレなし)
    TextRenderer.DrawText(g, _ime.Text, underlineFont, new Point(x, y), ForeColor,
        TextFormatFlags.NoPadding | TextFormatFlags.NoPrefix);
}
```

**Step 3: 手動確認(Smoke)**

Task 14 で自動化するので、この時点では手動で:
1. `dotnet run --project tests/yEdit.Editor.Smoke -c Release` で smoke 起動
2. File>Open で任意ファイル → キャレット位置に IME で「にほんご」入力
3. 未確定文字列が下線付きで表示され、Enter で確定すると 1 Undo で消えることを目視

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 9 OnPaint に未確定文字列 overlay 描画(下線 + inline 合成)"
```

---

### Task 10: GCS_COMPATTR / GCS_COMPCLAUSE で変換対象節ハイライト

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`DrawImeOverlay` を節ごと描画に拡張)

`_ime.Clauses[i]..Clauses[i+1]` の節が「変換対象節(`Attrs[Clauses[i]]` が `TargetConverted`=0x01)」なら背景反転+下線太。

**Step 1: 実装**

```csharp
private void DrawImeOverlay(Graphics g)
{
    if (_buffer is null || _ime.Text.Length == 0) return;
    var (x, y, _) = ComputeCaretPoint(_ime.Start);
    if (x < 0 || y < 0) return;

    using var normalFont = new Font(_font, _font.Style | FontStyle.Underline);
    using var targetFont = new Font(_font, _font.Style | FontStyle.Underline | FontStyle.Bold);
    int curX = x;

    // Clauses が空なら 1 節扱い(全体を通常下線)
    if (_ime.Clauses.Length < 2)
    {
        TextRenderer.DrawText(g, _ime.Text, normalFont, new Point(curX, y), ForeColor,
            TextFormatFlags.NoPadding | TextFormatFlags.NoPrefix);
        return;
    }

    for (int i = 0; i < _ime.Clauses.Length - 1; i++)
    {
        int s = _ime.Clauses[i], e = _ime.Clauses[i + 1];
        if (e > _ime.Text.Length) e = _ime.Text.Length;
        if (s >= e) continue;
        string clause = _ime.Text[s..e];

        // 節先頭の Attr を代表値として採用
        byte attr = s < _ime.Attrs.Length ? _ime.Attrs[s] : ImeAttribute.Input;
        bool isTarget = attr == ImeAttribute.TargetConverted;

        Size sz = TextRenderer.MeasureText(g, clause, normalFont, new Size(int.MaxValue, int.MaxValue),
            TextFormatFlags.NoPadding | TextFormatFlags.NoPrefix);

        if (isTarget)
        {
            using var brush = new SolidBrush(_style.SelectionBack);
            g.FillRectangle(brush, curX, y, sz.Width, _metrics.LineHeightPx);
            TextRenderer.DrawText(g, clause, targetFont, new Point(curX, y), _style.SelectionFore,
                TextFormatFlags.NoPadding | TextFormatFlags.NoPrefix);
        }
        else
        {
            TextRenderer.DrawText(g, clause, normalFont, new Point(curX, y), ForeColor,
                TextFormatFlags.NoPadding | TextFormatFlags.NoPrefix);
        }
        curX += sz.Width;
    }
}
```

**Step 2: 手動確認**

- Smoke で「わたしはにほんじん」入力 → スペースで変換 → 節境界移動(矢印)で反転節が視認できる

**Step 3: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 4: コミット**

```powershell
git commit -am "P4: Task 10 GCS_COMPATTR/GCS_COMPCLAUSE 節ハイライト(変換対象反転 + 下線太)"
```

---

### Task 11: GCS_CURSORPOS を反映して IME 内キャレット位置に SetCaretPos

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`DrawImeOverlay` の末尾 or `PositionCaret` に IME 分岐)

未確定中はシステムキャレットを `_ime.Start + _ime.CursorPos` の座標に置く(=IME 内での caret 移動を追跡)。

**Step 1: 実装**

`PositionCaret` に IME 分岐を追加:

```csharp
private void PositionCaret()
{
    if (!_hasFocus || _buffer is null) return;

    int visualOffset = IsComposing
        ? _ime.Start + _ime.CursorPos      // Task 11: IME 内カーソル位置
        : _caret;

    // _ime.CursorPos は未確定文字列の内部 offset(0〜Text.Length)。overlay は
    // _ime.Start に inline 描画されるため、CursorPos 分の x を加算する必要がある。
    // 単純化: ComputeCaretPoint は本文しか知らないので、IME 中は overlay の描画幅を
    // TextRenderer.MeasureText で測って加算する。
    if (IsComposing)
    {
        var (x, y, _) = ComputeCaretPoint(_ime.Start);
        if (x < 0 || y < 0) return;
        string prefix = _ime.Text[.._ime.CursorPos];
        using var g = CreateGraphics();
        Size sz = TextRenderer.MeasureText(g, prefix, _font, new Size(int.MaxValue, int.MaxValue),
            TextFormatFlags.NoPadding | TextFormatFlags.NoPrefix);
        NativeMethods.SetCaretPos(x + sz.Width, y);
        return;
    }

    // 既存の非 IME 経路(P3 実装)を保持
    var (cx, cy, visible) = ComputeCaretPoint(_caret);
    if (!visible) return;
    NativeMethods.SetCaretPos(cx, cy);
}
```

**Step 2: 手動確認**

- Smoke で「にほんご」入力 → 左右矢印で IME 内キャレットが移動する(未確定文字列内でシステムキャレットが追従)

**Step 3: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 4: コミット**

```powershell
git commit -am "P4: Task 11 GCS_CURSORPOS 反映(IME 内キャレット位置に SetCaretPos)"
```

---

### Task 12: 候補窓 / フォント追従(ImmSetCandidateWindow / ImmSetCompositionFont)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(`OnImeStartComposition` / `ApplyComposition` / `PositionCaret` に IME 通知 + `SetSource` / `ApplyAppearance` にフォント通知)

`ImmSetCandidateWindow` は「候補ウィンドウを表示すべき座標」を IME に伝える。3 タイミングで呼ぶ:
1. `OnImeStartComposition`(未確定開始時)
2. `ApplyComposition`(未確定更新時=キャレット行が変わる可能性)
3. `PositionCaret`(スクロール変更等でキャレット座標が動いたとき=同じ経路に相乗り)

`ImmSetCompositionFont` は「未確定文字列に使うフォント」を IME に伝える(座標整合)。2 タイミング:
1. `SetSource`(初期化時)
2. `ApplyAppearance`(フォント変更時)

**Step 1: 実装**

```csharp
/// <summary>キャレット位置(client 座標)を screen 座標に変換して ImmSetCandidateWindow で IME に通知する。
/// 未確定中のみ有効。IME 無効環境は no-op。</summary>
private void NotifyCandidateWindow()
{
    if (!IsComposing || _buffer is null || !_hasFocus) return;
    nint hIMC = NativeMethods.ImmGetContext(Handle);
    if (hIMC == IntPtr.Zero) return;
    try
    {
        var (x, y, _) = ComputeCaretPoint(_ime.Start);
        if (x < 0 || y < 0) return;
        var form = new NativeMethods.CANDIDATEFORM
        {
            dwIndex = 0,
            dwStyle = NativeMethods.CFS_CANDIDATEPOS,
            ptCurrentPos = new NativeMethods.POINT { x = x, y = y + _metrics.LineHeightPx },
        };
        NativeMethods.ImmSetCandidateWindow(hIMC, ref form);
    }
    finally { NativeMethods.ImmReleaseContext(Handle, hIMC); }
}

private void NotifyCompositionFont()
{
    nint hIMC = NativeMethods.ImmGetContext(Handle);
    if (hIMC == IntPtr.Zero) return;
    try
    {
        // Font.ToLogFont は object を要求するので LOGFONT を boxing で渡す
        var lf = new NativeMethods.LOGFONT();
        _font.ToLogFont(lf);
        NativeMethods.ImmSetCompositionFontW(hIMC, ref lf);
    }
    finally { NativeMethods.ImmReleaseContext(Handle, hIMC); }
}
```

呼び出し追加箇所:
- `OnImeStartComposition` の末尾: `NotifyCandidateWindow();`
- `ApplyComposition` の末尾: `NotifyCandidateWindow();`
- `PositionCaret` の末尾(IME 分岐後・非 IME 経路後の共通位置): `NotifyCandidateWindow();`
- `SetSource` の末尾: `NotifyCompositionFont();`
- `ApplyAppearance` のフォント変更後: `NotifyCompositionFont();`

**Step 2: 手動確認**

- Smoke で「にほんご」入力 → 候補ウィンドウがキャレット直下(=行送りぶん下)に出る
- キャレット行を PageUp/PageDown で移動しつつ IME 開始 → 候補窓が追従する
- ApplyAppearance でフォント変更 → IME フォントが本文に揃う

**Step 3: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 4: コミット**

```powershell
git commit -am "P4: Task 12 ImmSetCandidateWindow/ImmSetCompositionFont 追従"
```

---

### Task 13: 外部編集 API のガード(未確定中の強制取消)

**Files:**
- Modify: `src/yEdit.Editor/EditorControl.cs`(各外部編集 API の先頭にガード)
- Test: `tests/yEdit.Editor.Tests/EditorControlImeTests.cs`

未確定中に外部から編集 API が呼ばれた場合、まず未確定を強制取消してから実行する(=undefined behavior 防止)。対象:
- `ReplaceCharRange`
- `SetSelectionCharRange`
- `SetCaretCharOffset`
- `MoveCaretWithSelection`
- `SetSelectionAnchored`
- `OnMouseDown`(未確定中クリック=Scintilla 互換で「取消」・§4-10)

**Step 1: 失敗するテスト**

```csharp
[Fact]
public void ReplaceCharRange_DuringComposition_CancelsFirst() => Sta.Run(() =>
{
    using var c = MakeControl("abcdef");
    c.SetCaretCharOffset(3);
    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あい", 2, [0, 0], [0, 2]);
    Assert.True(c.__TestIsComposing());

    c.ReplaceCharRange(0, 3, "XYZ");
    Assert.False(c.__TestIsComposing());
    Assert.Equal("XYZdef", c.__TestSnapshotText());
});

[Fact]
public void SetCaretCharOffset_DuringComposition_Cancels() => Sta.Run(() =>
{
    using var c = MakeControl("abc");
    var m = new Message { HWnd = c.Handle, Msg = NativeMethods.WM_IME_STARTCOMPOSITION };
    c.__TestProcessMessage(ref m);
    c.__TestApplyComposition("あ", 1, [0], [0, 1]);
    c.SetCaretCharOffset(2);
    Assert.False(c.__TestIsComposing());
});
```

**Step 2: 実装**

各 API の先頭 1 行に:

```csharp
public void ReplaceCharRange(int start, int length, string replacement)
{
    if (IsComposing) CancelCompositionAndDefault();
    // ... 既存実装
}

public void SetSelectionCharRange(int start, int end)
{
    if (IsComposing) CancelCompositionAndDefault();
    // ... 既存実装
}

public void SetCaretCharOffset(int offset)
{
    if (IsComposing) CancelCompositionAndDefault();
    // ... 既存実装
}

public void MoveCaretWithSelection(int newCaret)
{
    if (IsComposing) CancelCompositionAndDefault();
    // ... 既存実装
}

public void SetSelectionAnchored(int anchor, int caret)
{
    if (IsComposing) CancelCompositionAndDefault();
    // ... 既存実装
}

protected override void OnMouseDown(MouseEventArgs e)
{
    if (IsComposing) CancelCompositionAndDefault();
    // ... 既存実装
}
```

**Step 3: テスト実行**

```powershell
dotnet test tests/yEdit.Editor.Tests -c Release --nologo --filter "FullyQualifiedName~DuringComposition"
```

Expected: 2 件緑 + 既存の該当 API テスト全緑(=未確定なし時はガード発火せず挙動不変)。

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
```

Expected: build 0 警告 / 全緑。

**Step 5: コミット**

```powershell
git commit -am "P4: Task 13 外部編集 API ガード(未確定中の強制取消)"
```

---

### Task 14: Smoke 拡張 + ATOK 実機チェックリスト作成

**Files:**
- Modify: `tests/yEdit.Editor.Smoke/Program.cs`(タイトルバーに IME 状態表示 + `--ime` サブコマンド)
- Create: `docs/plans/2026-07-06-p4-ime-checklist.md`(P0 と同形式)

自動テストで検証できない領域(実機 IME の挙動)を、ユーザー実機チェックリストで DoD 判定する。

**Step 1: Smoke タイトルバーに IME 状態表示**

`tests/yEdit.Editor.Smoke/Program.cs` の Form に `EditorControl.Invalidated` の代わりに Paint 相当のフックで IME 状態を Text に反映:

```csharp
// EditorControl に internal ヘルパを露出(smoke 参照用・InternalsVisibleTo で smoke プロジェクトを追加)
internal bool __SmokeIsComposing() => IsComposing;
internal string __SmokeImeText() => _ime.Text;

// Smoke 側でタイマー or Paint フックで更新
var timer = new Timer { Interval = 100 };
timer.Tick += (_, _) =>
{
    form.Text = editor.__SmokeIsComposing()
        ? $"yEdit Smoke [IME: {editor.__SmokeImeText()}]"
        : "yEdit Smoke";
};
timer.Start();
```

`yEdit.Editor.csproj` に `<InternalsVisibleTo Include="yEdit.Editor.Smoke" />` を追加。

**Step 2: `--ime` サブコマンド**

```csharp
// Program.cs の switch 分岐
case "--ime":
    // 未確定文字列の色/下線が視覚的にわかる長文サンプルを開いた状態で起動
    // 例: 縦横 20 行程度の日本語テキストをメモリ上で生成
    var buf = TextBuffer.FromString(
        "IME 動作確認用サンプル\n"
        + string.Concat(Enumerable.Repeat("あいうえおかきくけこ\n", 20)));
    // 通常の起動フローと同じ Form + EditorControl を返す
    return RunSmoke(buf);
```

**Step 3: ATOK 実機チェックリストを新規作成**

`docs/plans/2026-07-06-p4-ime-checklist.md` を新設:

```markdown
# P4 IME 実機チェックリスト(ATOK)

**目的**: P4 の DoD 判定 = ATOK 実機で以下 7 項目が全 OK なら合格。

**準備**:
1. `dotnet run --project tests/yEdit.Editor.Smoke -c Release -- --ime`
2. Smoke ウィンドウにフォーカス
3. ATOK を ON

| # | 検証項目 | 期待挙動 | 結果 |
|---|---|---|------|
| 1 | 「にほんご」タイプ→変換なし確定(Enter) | 未確定表示・下線・確定で「にほんご」挿入 | |
| 2 | 「かんじ」タイプ→スペースで変換→Enter | 変換対象節が反転・確定で選択候補が挿入 | |
| 3 | 「わたしはにほんじん」タイプ→連文節変換 | 節境界が視認できる・矢印で節移動 | |
| 4 | 候補ウィンドウが表示される | キャレット直下(=行送り位置)に候補窓が出る | |
| 5 | ESC 取消 | 未確定文字列が消える・TextBuffer に何も残らない | |
| 6 | 未確定中に BackSpace | 未確定文字列が 1 文字短くなる(TextBuffer 不変) | |
| 7 | 未確定中に他ウィンドウへフォーカス移動→戻る | 未確定が確定される・戻ってきても残骸なし | |

**NG 時の切り分け**:
- Smoke タイトルバー `[IME: ...]` に未確定文字列が反映されない → WM_IME_COMPOSITION が届いていない疑い(WndProc override 経路確認)
- 反映されるが本文に描画されない → OnPaint の DrawImeOverlay 呼出漏れ(Task 9)
- 確定文字列が 2 度挿入される → GCS_RESULTSTR 分岐で `_ime = default` の順序ミス(Task 7)

**撤退判定**:
- ATOK v1 で 7 項目のうち 3 項目以上 NG → 撤退(git revert で P4 コミット群を戻し、P3 完了状態へ)
- 2 項目以下 NG → 該当 Task を再設計し継続
```

**Step 4: 全体回帰**

```powershell
dotnet build yEdit.sln -c Release; dotnet test yEdit.sln -c Release --nologo
dotnet run --project tests/yEdit.Editor.Smoke -c Release -- --ime
```

Expected: build 0 警告 / 全緑 / smoke が起動しサンプルテキストを表示。

**Step 5: コミット**

```powershell
git add tests/yEdit.Editor.Smoke docs/plans/2026-07-06-p4-ime-checklist.md src/yEdit.Editor/EditorControl.cs src/yEdit.Editor/yEdit.Editor.csproj
git commit -m "P4: Task 14 Smoke 拡張(--ime + IME 状態表示)+ ATOK 実機チェックリスト"
```

---

### Task 15: 別エージェント最終レビュー + 対応 + 設計書 §3 に P4 結果表追記

**Files:**
- Modify: `docs/plans/2026-07-05-custom-editcontrol-design.md`(§3 P4 結果表を追記)
- (レビュー指摘に応じて)`src/yEdit.Editor/EditorControl.cs` / `src/yEdit.Core/Editing/*`

**Step 1: 別エージェントレビュー依頼**

`superpowers:code-reviewer` エージェントに次のコンテキストで依頼:
- 対象コミット範囲: Task 1〜14(commit 範囲)
- 期待: P4 の DoD(ATOK 実機チェックリスト・overlay 方式・1 Splice=1 Undo)
- 焦点:
  - `WndProc` 経由処理の `m.Result = IntPtr.Zero` / `base.WndProc` 呼び分けの正当性(SETCONTEXT だけ base に流す)
  - `Imm*` 呼出の `try/finally` + `ImmReleaseContext` 完全性(ハンドルリーク)
  - 未確定中の縁ケース(ReadOnly 化・LostFocus・外部編集・MouseDown)強制取消経路の網羅
  - `_ime.Start` の凍結タイミング(STARTCOMPOSITION 時のみ更新・以降の COMPOSITION では上書きしない)
  - サロゲート境界(`SnapCursorPos` の適用範囲)
  - `InsertConfirmedText` 経由の 1 Splice=1 Undo が coalescing で他挿入と融合しないか
  - Overlay 描画とキャレット行強調・cellHighlight の描画順序(反転節が上書きされない)
  - `ImmSetCompositionFont` の LOGFONT 変換(`Font.ToLogFont` boxing / CharSet)
  - `PositionCaret` の IME 分岐で `CreateGraphics` を毎回作る性能影響(タイピング応答性)

**Step 2: レビュー指摘の Critical/Important を Task 15 内で修正**

```powershell
git commit -am "P4: Task 15 レビュー対応(...)"
```

**Step 3: 設計書 §3 に P4 結果表を追記**

`docs/plans/2026-07-05-custom-editcontrol-design.md` の P3 結果直下に P4 結果を書く:
- テスト数 663 → N 全緑
- ATOK 実機チェックリスト 7 項目結果
- 新規純ロジックテスト(`ImeCompositionStateTests`)
- 追加/変更 API(=公開は増えていない=`ImeCompositionState` のみ internal 追加)
- 設計判断のポイント(overlay 方式・InsertConfirmedText 共通化・強制取消 1 経路)
- 別エージェントレビュー結果(Critical/Important カウント)
- P5/P6 への申し送り

**Step 4: メモリ更新**

`%USERPROFILE%\.claude\projects\D--src-yEdit\memory\custom-editcontrol.md` に P4 完了記載を追加(P3 完了節と同フォーマット)。

**Step 5: コミット**

```powershell
git commit -am "P4: 設計書 §3 に P4 結果を追記(DoD 達成 / ATOK 実機 7 項目 OK)"
```

---

## DoD(P4 完了条件)

1. **build 0 警告 / 全テスト緑**(既存 663 件 + P4 新規)
2. **ATOK 実機チェックリスト 7 項目**が全 OK(`docs/plans/2026-07-06-p4-ime-checklist.md`)
3. **overlay 方式**が動作:未確定文字列は `TextBuffer` に触れず表示され、確定時のみ 1 Splice=1 Undo
4. **既存 `OnKeyPress` 挙動不変**(P3 §0-9 温存契約 = Task 3 のリファクタ後も既存テスト全緑)
5. **強制取消経路**が網羅(ReadOnly 化・LostFocus・外部編集 API・MouseDown)
6. **既存 API 契約**(P1/P2/P3 の公開 API)を破壊しない(P4 では public API を追加していない)
7. **別エージェントレビュー**の Critical=0 / Important=0(または Task 15 で消化)

---

## 申し送り(P5 以降で対応)

- **未確定文字列の UIA プロバイダ露出**: P0 実機結果より v1 は不要(IME 自身の候補読みに任せる)。P5 で新 EditorControl 上に UIA プロバイダを載せて実機再検証し、必要と判断されたら追加
- **再変換**(`IMR_RECONVERTSTRING` / `IMR_CONFIRMRECONVERTSTRING`): 設計書§2-3 で v1 スコープ外を明記。ATOK では実装しないと辞書登録・カーソル位置文字の逆変換が動かない=v2 以降で対応判断
- **TSF(TextServices Framework)経路**: v1 スコープ外。音声入力・手書き入力を扱う場合に v2 で検討
- **ATOK 特有拡張**(辞書登録キー連携・パスワードモード等): v1 スコープ外
- **未確定中マウスクリック時の挙動**: v1 は「取消」で統一(§4-10)。Scintilla の実機挙動確認後に「確定」(`CPS_COMPLETE`)へ変更する余地あり
- **`PositionCaret` の CreateGraphics 毎回生成**: Task 11 で TextRenderer.MeasureText のために `CreateGraphics()` を毎回作る=タイピング応答性への影響を Task 15 レビューでベンチ(既存の --typing ベンチ相当)を撮り、必要なら `_metrics` へ MeasureRun 拡張(未確定文字列は都度計測を要するので簡単ではない)
- **未確定中の折り返し**: 現状は「折り返さない」(Scintilla 同挙動)。長文未確定でウィンドウ右端を突き抜けるケースの UX 改善は v2 検討

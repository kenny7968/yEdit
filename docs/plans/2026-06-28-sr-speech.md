# SR適応 音声出力層 実装計画

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** PC-Talker利用時に無音だった能動アナウンスを、SR適応の音声出力層（PC-Talker は `PCTKUSR.dll` の `SoundMessage`、それ以外は既存UIA通知）に載せ替え、全アナウンス（CSV/検索/grep/モード切替/照会）を一括で読めるようにする。

**Architecture:** 集約点 `SrNotify.Raise` の内部のみを差し替える。純ロジックの `SpeechRouter`／`ISpeechChannel` を `yEdit.Core`（WinForms非依存・テスト可能）に置き、実I/Oの `PcTalkerSpeech`（遅延束縛P/Invoke）と `UiaNotificationSpeech`（現状のUIA通知）を `yEdit.App` に置く。PC-Talker稼働時のみ PcTalker を先に試し、失敗時はUIAへフォールバック。視覚 `label.Text` 更新は無条件で温存。

**Tech Stack:** C# / .NET 9 / WinForms / xUnit。`PCTKUSR.dll`（PC-Talker稼働機のWindowsフォルダに既在）への `LoadLibrary`/`GetProcAddress` 遅延束縛P/Invoke。

設計根拠: `docs/plans/2026-06-28-sr-speech-design.md`

---

## 前提・参照

- 集約点は1箇所: `src/yEdit.App/SrNotify.cs`。呼び出し側（`Announcer` / `FindReplaceDialog` / `GrepDialog`）は**無改修**。
- 既存SR判定: `src/yEdit.Editor/ScreenReaders.cs` の `IsPcTalkerRunning()`（`public static bool`）を流用。
- ビルド: `dotnet build yEdit.sln`（App は net9.0-windows＝Windows上でビルド）。
- Coreテスト: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`。

---

## Task 1: SpeechRouter（純ロジック・Core）をTDDで作る

**Files:**
- Test: `tests/yEdit.Core.Tests/Speech/SpeechRouterTests.cs`（Create）
- Create: `src/yEdit.Core/Speech/ISpeechChannel.cs`
- Create: `src/yEdit.Core/Speech/SpeechRouter.cs`

**Step 1: 失敗するテストを書く**

`tests/yEdit.Core.Tests/Speech/SpeechRouterTests.cs`:

```csharp
using yEdit.Core.Speech;

namespace yEdit.Core.Tests.Speech;

public class SpeechRouterTests
{
    private sealed class FakeChannel : ISpeechChannel
    {
        public bool Result;
        public int Calls;
        public string? Last;
        public string Name => "fake";
        public bool TrySpeak(string message) { Calls++; Last = message; return Result; }
    }

    [Fact]
    public void PcTalker_running_and_speaks_does_not_use_fallback()
    {
        var pct = new FakeChannel { Result = true };
        var uia = new FakeChannel { Result = true };
        var router = new SpeechRouter(pct, () => true);

        router.Speak("こんにちは", uia);

        Assert.Equal(1, pct.Calls);
        Assert.Equal("こんにちは", pct.Last);
        Assert.Equal(0, uia.Calls); // 先頭で成功したらフォールバックしない
    }

    [Fact]
    public void PcTalker_running_but_fails_falls_back_to_uia()
    {
        var pct = new FakeChannel { Result = false };
        var uia = new FakeChannel { Result = true };
        var router = new SpeechRouter(pct, () => true);

        router.Speak("x", uia);

        Assert.Equal(1, pct.Calls);
        Assert.Equal(1, uia.Calls); // PcTalkerが未発声ならUIAへ
    }

    [Fact]
    public void PcTalker_not_running_uses_uia_only()
    {
        var pct = new FakeChannel { Result = true };
        var uia = new FakeChannel { Result = true };
        var router = new SpeechRouter(pct, () => false);

        router.Speak("x", uia);

        Assert.Equal(0, pct.Calls); // 非稼働ならPcTalkerは呼ばない
        Assert.Equal(1, uia.Calls);
    }
}
```

**Step 2: テストを実行して失敗を確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: コンパイルエラー（`ISpeechChannel` / `SpeechRouter` が未定義）でFAIL。

**Step 3: 最小実装を書く**

`src/yEdit.Core/Speech/ISpeechChannel.cs`:

```csharp
namespace yEdit.Core.Speech;

/// <summary>
/// 1つの音声出力経路。TrySpeak は「この経路で発声を完結できたら true、
/// 未対応/失敗なら false」を返す。false のときルータが次の経路へフォールバックする。
/// </summary>
public interface ISpeechChannel
{
    string Name { get; }
    bool TrySpeak(string message);
}
```

`src/yEdit.Core/Speech/SpeechRouter.cs`:

```csharp
namespace yEdit.Core.Speech;

/// <summary>
/// SR適応の音声出力ルータ。PC-Talker 稼働中のみ専用経路を先に試し、
/// 未発声なら UIA 通知（fallback）へ。判定は注入された delegate で行い、単体テスト可能にする。
/// </summary>
public sealed class SpeechRouter
{
    private readonly ISpeechChannel _pcTalker;
    private readonly Func<bool> _isPcTalkerRunning;

    public SpeechRouter(ISpeechChannel pcTalker, Func<bool> isPcTalkerRunning)
    {
        _pcTalker = pcTalker;
        _isPcTalkerRunning = isPcTalkerRunning;
    }

    /// <summary>message を発声する。fallback は呼び出し時の Label に束縛された UIA 経路。</summary>
    public void Speak(string message, ISpeechChannel fallback)
    {
        if (_isPcTalkerRunning() && _pcTalker.TrySpeak(message)) return;
        fallback.TrySpeak(message);
    }
}
```

**Step 4: テストを実行して成功を確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: 全テストPASS（既存テストも緑のまま）。

**Step 5: コミット**

```bash
git add src/yEdit.Core/Speech/ tests/yEdit.Core.Tests/Speech/
git commit -m "SR音声出力のルータ(SpeechRouter/ISpeechChannel)をCoreに追加"
```

---

## Task 2: PcTalkerSpeech（PCTKUSR.dll 遅延束縛P/Invoke・App）

実I/O（ネイティブ）でユニットテスト対象外。検証はビルド成功＋後段の実機検証。

**Files:**
- Create: `src/yEdit.App/Speech/PcTalkerSpeech.cs`

**Step 1: 実装を書く**

`src/yEdit.App/Speech/PcTalkerSpeech.cs`:

```csharp
using System.Runtime.InteropServices;
using yEdit.Core.Speech;

namespace yEdit.App.Speech;

/// <summary>
/// PC-Talker（高知システム開発）への直接発声。共有DLL PCTKUSR.dll の
/// SoundMessage(LPCWSTR, int) を遅延束縛で呼ぶ。DLLはPC-Talker本体がWindowsフォルダに導入するため
/// 同梱不要。非PC-Talker機ではDLL/関数が見つからず TrySpeak は false（→ルータがUIAへフォールバック）。
/// 静的 [DllImport] は非PC-Talker機で DllNotFoundException を誘発し得るため避け、LoadLibrary/GetProcAddress を使う。
/// </summary>
public sealed class PcTalkerSpeech : ISpeechChannel
{
    public string Name => "PC-Talker";

    [UnmanagedFunctionPointer(CallingConvention.Winapi)]
    private delegate int SoundMessageDelegate(
        [MarshalAs(UnmanagedType.LPWStr)] string text, int flags);

    private static SoundMessageDelegate? _soundMessage;
    private static bool _resolved;

    private static SoundMessageDelegate? Resolve()
    {
        if (_resolved) return _soundMessage;
        _resolved = true;
        try
        {
            nint h = LoadLibraryW("PCTKUsr.dll");
            if (h == 0) return _soundMessage = null;
            nint p = GetProcAddress(h, "SoundMessage");
            if (p == 0) return _soundMessage = null;
            _soundMessage = Marshal.GetDelegateForFunctionPointer<SoundMessageDelegate>(p);
        }
        catch { _soundMessage = null; }
        return _soundMessage;
    }

    public bool TrySpeak(string message)
    {
        var fn = Resolve();
        if (fn is null) return false;
        try { fn(message, 0); return true; }  // flags=0。割り込み等の調整は実機検証で
        catch { return false; }
    }

    [DllImport("kernel32.dll", CharSet = CharSet.Unicode, SetLastError = true)]
    private static extern nint LoadLibraryW(string lpFileName);

    [DllImport("kernel32.dll", CharSet = CharSet.Ansi, SetLastError = true)]
    private static extern nint GetProcAddress(nint hModule, string procName);
}
```

**Step 2: ビルドして成功を確認**

Run: `dotnet build yEdit.sln`
Expected: ビルド成功・警告0。

**Step 3: コミット**

```bash
git add src/yEdit.App/Speech/PcTalkerSpeech.cs
git commit -m "PC-Talker直接発声(PCTKUSR.dll SoundMessage遅延束縛)を追加"
```

---

## Task 3: UiaNotificationSpeech（既存UIA通知をchannel化・App）

**Files:**
- Create: `src/yEdit.App/Speech/UiaNotificationSpeech.cs`

**Step 1: 実装を書く**

`src/yEdit.App/Speech/UiaNotificationSpeech.cs`:

```csharp
using System.Windows.Forms.Automation;
using yEdit.Core.Speech;

namespace yEdit.App.Speech;

/// <summary>
/// 既存の UIA 通知（RaiseAutomationNotification）を channel 化したもの。
/// NVDA・その他SR、および PC-Talker フォールバックを担う常設の最終手段。
/// 呼び出し毎に通知元 Label に束縛して生成する。
/// </summary>
public sealed class UiaNotificationSpeech : ISpeechChannel
{
    private readonly Label _label;
    public UiaNotificationSpeech(Label label) => _label = label;

    public string Name => "UIA";

    public bool TrySpeak(string message)
    {
        try
        {
            _label.AccessibilityObject.RaiseAutomationNotification(
                AutomationNotificationKind.ActionCompleted,
                AutomationNotificationProcessing.MostRecent,
                message);
            return true;
        }
        catch { return false; } // 通知非対応環境では false（視覚表示にフォールバック）
    }
}
```

**Step 2: ビルドして成功を確認**

Run: `dotnet build yEdit.sln`
Expected: ビルド成功・警告0。

**Step 3: コミット**

```bash
git add src/yEdit.App/Speech/UiaNotificationSpeech.cs
git commit -m "UIA通知をISpeechChannel(UiaNotificationSpeech)に切り出し"
```

---

## Task 4: SrNotify をルータ配線へ差し替える（呼び出し側は無改修）

**Files:**
- Modify: `src/yEdit.App/SrNotify.cs`

**Step 1: SrNotify を書き換える**

`src/yEdit.App/SrNotify.cs` の全文を以下に置換:

```csharp
using yEdit.App.Speech;
using yEdit.Core.Speech;
using yEdit.Editor;

namespace yEdit.App;

/// <summary>
/// SR への能動通知の共通実装。Label を視覚表示しつつ、SR適応の音声出力層へ委譲する。
/// PC-Talker 稼働時は PCTKUSR.dll で直接発声し、それ以外/失敗時は UIA 通知へフォールバック。
/// 実機 SR 調整時の変更点を 1 箇所に集約する（Announcer / FindReplaceDialog / GrepDialog から共有）。
/// </summary>
internal static class SrNotify
{
    private static readonly SpeechRouter _router =
        new(new PcTalkerSpeech(), ScreenReaders.IsPcTalkerRunning);

    public static void Raise(Label label, string message)
    {
        if (string.IsNullOrEmpty(message)) return;
        label.Text = message; // 視覚フィードバックは無条件（晴眼/弱視も第一級）
        _router.Speak(message, new UiaNotificationSpeech(label));
    }
}
```

**Step 2: ビルドして成功を確認**

Run: `dotnet build yEdit.sln`
Expected: ビルド成功・警告0。`Announcer`/`FindReplaceDialog`/`GrepDialog` は無改修で通る。

**Step 3: Coreテストが緑のままか確認**

Run: `dotnet test tests/yEdit.Core.Tests/yEdit.Core.Tests.csproj`
Expected: 全PASS（既存＋新規SpeechRouterTests）。

**Step 4: コミット**

```bash
git add src/yEdit.App/SrNotify.cs
git commit -m "SrNotifyをSR適応ルータ配線へ差し替え(PC-Talker→UIAフォールバック)"
```

---

## Task 5: 開発機スモーク確認（PC-Talker不在環境）

PC-Talker非稼働の開発機で**退行が無いこと**を確認（自動テスト対象外の配線/フォールバックの健全性）。

**手順:**
1. `dotnet build yEdit.sln` でビルドした yEdit を起動。
2. 任意の `.csv` を開く（CSVモードON）。
3. `Ctrl+Shift+C`／`Ctrl+Shift+↑↓←→` を操作し、**クラッシュしない**ことを確認
   （PcTalkerSpeech が DLL 不在で false → UIA フォールバック、例外は飛ばない）。
4. NVDA があれば、これらが従来どおり読めることを確認。

**確認できたら**、結果を簡潔にメモ（コミット不要。コードに変更が無ければスキップ）。

---

## Task 6: 実機SR検証（PC-Talker / NVDA）— DoDの核・要実機

> このタスクは PC-Talker 実機が必要。`docs/plans/2026-06-28-sr-speech-design.md` §8〜9 のチェックリストを実施し、結果を記録する。

**PC-Talker:**
- [ ] `Ctrl+Shift+C`（列見出し）が**聞こえる**（従来は無音）。
- [ ] `Ctrl+Shift+↑/↓`（上下セル）でセル値＋行列が聞こえる。
- [ ] `Ctrl+Shift+←/→`（左右セル）が聞こえる。
- [ ] 検索件数／grep結果／CSVモード切替のアナウンスが聞こえる。
- [ ] **二重読み**（物理行の付随読み＋SoundMessage）の有無を確認。あれば `flags` 調整 or 抑制を別タスク化。
- [ ] `SoundMessage` の署名/`flags=0` の挙動が想定どおりか（無音/例外なら設計§9-1へ差し戻し）。

**NVDA:**
- [ ] 上記すべてが従来どおり聞こえる（UIA温存の退行が無い）。

**結果の記録先:** メモリ `csv-mode-followups.md` / `markdown-preview-followups.md` と同様に、実機検証フェーズの記録へ追記。

---

## 既知の割り切り（今回スコープ外）

- NVDA への `nvdaControllerClient.dll` 採用は見送り（現状UIAで足りる／同梱DLLを増やさない）。
- `SoundMessage` 以外のPC-Talker API（停止/優先度/読み調整）は使わない。
- 設定での発声手段切替UIは作らない（自動判定＋フォールバックで完結）。
- JAWS等その他SR専用APIは対象外（UIAフォールバックで読める範囲）。

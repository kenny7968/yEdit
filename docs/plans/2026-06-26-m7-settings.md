# M7 設定ダイアログ／外観 — 実装計画

**Goal:** アクセシブルな設定ダイアログ（フォント・配色テーマ・既定文字コード/改行）と最近のファイル露出。Core は純ロジックでテスト可能。

**Architecture:** `yEdit.Core`（`AppearanceThemes`/`RecentFilesList`＋`AppSettings`拡張）＋ `yEdit.App`（`EditorAppearance`/`SettingsDialog`/MainForm 配線）。設計根拠: `docs/plans/2026-06-26-m7-settings-design.md`。

**Tech Stack:** .NET 9 / C# / WinForms / Scintilla5.NET / xUnit。

## 前提・規約
- 作業ブランチ: `feature/m7-settings`。各タスク末にコミット（日本語・末尾 Co-Authored-By）。
- ビルド 0 警告維持。`dotnet test tests/yEdit.Core.Tests`。

---

## Task 1: Core — AppearanceThemes
**Files:** `src/yEdit.Core/Settings/AppearanceTheme.cs`
プリセット 4 種 ＋ `ById`（未知→default）。ビルド確認 → コミット。

## Task 2: Core — RecentFilesList
**Files:** `src/yEdit.Core/Text/RecentFilesList.cs`
`Add(current, path, max)`：先頭追加・PathKey 重複排除・上限。ビルド確認 → コミット。

## Task 3: Core — AppSettings 拡張
**Files:** `src/yEdit.Core/Settings/AppSettings.cs`
`Theme`(string="default")・`RecentFiles`(List<string>)。既存テスト緑確認 → コミット。

## Task 4: Core テスト
**Files:** `tests/yEdit.Core.Tests/Settings/AppearanceThemeTests.cs`, `tests/yEdit.Core.Tests/Text/RecentFilesListTests.cs`
設計 §5 の各観点。`dotnet test` 緑 → コミット。

## Task 5: App — EditorAppearance
**Files:** `src/yEdit.App/EditorAppearance.cs`
`Apply(ScintillaHost, AppSettings)`：フォント＋テーマ色→StyleClearAll→キャレット色。ビルド確認 → コミット。

## Task 6: App — SettingsDialog
**Files:** `src/yEdit.App/SettingsDialog.cs`
フォント(FontDialog)・配色テーマ・既定文字コード・既定改行。ラベル＋ニーモニック＋TabIndex。公開プロパティで結果を返す。ビルド確認 → コミット。

## Task 7: MainForm 配線
**Files:** `src/yEdit.App/MainForm.cs`
- `CreateEditor` を `EditorAppearance.Apply` へ置換（FontSize 適用）。
- ファイルメニュー「最近のファイル(&Y)」サブメニュー＋`RebuildRecentMenu`、「設定(&P)...」。
- `LoadInto` 成功時に `RegisterRecent`。設定 OK で全タブ適用＋保存。
ビルド 0 警告・全テスト緑 → コミット。

## Task 8: マージ前レビュー
別エージェント 5 レンズ（正確性/SR/スレッド安全/外観適用・設定永続化/統合）＋敵対的検証。指摘反映 → main へ no-ff マージ。
